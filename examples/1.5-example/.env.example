# 🌌 Gravito Application Config
PORT=3000
APP_NAME="Gravito Demo"
APP_VERSION="1.0.0"
APP_ENV=local

# 🗄️ Database Config
DB_CONNECTION=sqlite
DB_DATABASE=database.sqlite
# For MySQL/PostgreSQL:
# DB_HOST=localhost
# DB_PORT=3306
# DB_USERNAME=root
# DB_PASSWORD=

# 📨 Mail Config
# Choices: log, smtp, ses
MAIL_MAILER=log
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="no-reply@gravito.dev"
MAIL_FROM_NAME="${APP_NAME}"

# 📦 Queue Config
# Choices: local, redis, database
QUEUE_DRIVER=local

# 🧪 Redis Config (if using redis queue/cache)
REDIS_HOST=localhost
REDIS_PORT=6379
