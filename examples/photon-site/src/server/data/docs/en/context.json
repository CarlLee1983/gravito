{
  "id": "04",
  "title": "FastContext",
  "content": "<p>The <code>FastContext</code> is a pooled resource designed to minimize memory allocation per request. It utilizes a <strong>Lazy Parsing</strong> strategy, meaning headers and query strings are only processed when your code actually requests them.</p><h2>High-Performance Input Access</h2><pre><code>app.get('/api/search/:category', (c) => {\n  // 1. Path Parameters (Pre-parsed by router)\n  const category = c.req.param('category')\n\n  // 2. Query Strings (Lazy-parsed on first access)\n  const query = c.req.query('q')\n\n  // 3. Headers (Case-insensitive lookup)\n  const userAgent = c.req.header('User-Agent')\n\n  return c.json({ category, query, userAgent })\n})</code></pre><h2>Standard Responses</h2><p>Return high-speed responses using optimized helpers:</p><ul><li><code>c.json(data)</code>: High-speed serialization.</li><li><code>c.text(str)</code>: Zero-copy string response.</li><li><code>c.html(html)</code>: Server-side rendering support.</li></ul>",
  "meta": { "lastUpdated": "2026-01-15", "complexity": "MID_LEVEL", "category": "MEMORY" }
}
