{
  "id": "21",
  "title": "Ecosystem Integrations",
  "content": "<p>Photon's zero-shim architecture makes it easy to integrate with the broader JavaScript ecosystem. We provide official Orbits for common tasks and patterns for third-party tools.</p><h2>Cloudflare Workers (`@gravito/orbit-cloudflare`)</h2><p>The Cloudflare Orbit provides deep integration with Workers environment bindings. It automatically injects KV namespaces, R2 buckets, and Durable Objects into the Gravito Service Container.</p><pre><code>import { PlanetCore } from '@gravito/core'\nimport { OrbitCloudflare } from '@gravito/orbit-cloudflare'\n\nawait core.orbit(OrbitCloudflare)\n\n// Access KV in your handler via ctx.get()\napp.get('/kv', (ctx) => {\n  const MY_KV = ctx.get('MY_KV')\n  return ctx.json({ status: 'injected' })\n})</code></pre><h2>Authentication (Better Auth / Auth.js)</h2><p>While we recommend <strong>@gravito/sentinel</strong>, you can use any framework-agnostic auth library. Better Auth integrates seamlessly as a standard middleware.</p><pre><code>import { auth } from './auth'\n\napp.on(['POST', 'GET'], '/api/auth/*', (c) => {\n  return auth.handler(c.req.raw)\n})</code></pre><h2>Full-stack Validation (`@gravito/impulse-bridge`)</h2><p>The Impulse Bridge synchronizes your backend <code>FormRequest</code> rules with the frontend (Vue/React). This allows for instant client-side validation using the exact same rules defined on the server.</p><pre><code>import { ImpulseBridge } from '@gravito/impulse-bridge'\n\napp.get('/register', (ctx) => {\n  // Share RegisterRequest blueprint with Inertia\n  ImpulseBridge.share(ctx, 'register', RegisterRequest)\n  return ctx.inertia('Register')\n})</code></pre><h2>GraphQL Orbit</h2><p><strong>@gravito/graphql</strong> provides a zero-config GraphQL server powered by <strong>Yoga</strong>. It mounts directly into your Photon route tree.</p><pre><code>import { OrbitGraphQL } from '@gravito/graphql'\n\nconst graphQL = new OrbitGraphQL({\n  schema: mySchema,\n  path: '/graphql'\n})\n\napp.orbit(graphQL)</code></pre><h2>Stripe Webhooks</h2><p>Handling Stripe signatures is reliable in Photon. Since we don't manipulate the raw body by default, signature verification always matches.</p><pre><code>app.post('/webhook/stripe', async (c) => {\n  const signature = c.req.header('stripe-signature')\n  const body = await c.req.text()\n  \n  const event = stripe.webhooks.constructEvent(body, signature, secret)\n  // Process event...\n  return c.json({ received: true })\n})</code></pre><h2>Monitoring (Sentry)</h2><p>You can integrate Sentry using a global middleware that wraps the execution context.</p><pre><code>import * as Sentry from '@sentry/bun'\n\napp.use(async (c, next) => {\n  try {\n    await next()\n  } catch (err) {\n    Sentry.captureException(err)\n    throw err\n  }\n})</code></pre>",
  "meta": {
    "lastUpdated": "2026-01-15",
    "complexity": "ADVANCED",
    "category": "ECOSYSTEM"
  }
}