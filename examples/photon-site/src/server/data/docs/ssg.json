{
    "id": "ssg",
    "title": "Static Site Generation (SSG)",
    "content": "Prism provides a built-in **Static Site Generator (SSG)** that allows you to pre-render your Gravito application into static HTML files. This is ideal for blogs, documentation, and performance-critical landing pages.\n\n### Why use SSG?\n\n- **🚀 Lightning Speed**: Serve static files directly from a CDN or Nginx without any runtime overhead.\n- **🔍 Perfect SEO**: Search engines receive fully rendered HTML instantly.\n- **💰 Cost Effective**: Static hosting (GitHub Pages, Vercel, Cloudflare Pages) is often free or very cheap.\n\n### Basic Usage\n\nTo export your site, you can create a simple script using the `StaticSiteGenerator` class:\n\n```typescript\nimport { PlanetCore } from '@gravito/core'\nimport { OrbitPrism, StaticSiteGenerator } from '@gravito/prism'\nimport config from './gravito.config'\n\n// 1. Boot the core\nconst core = await PlanetCore.boot(config)\n\n// 2. Initialize SSG\nconst ssg = new StaticSiteGenerator(core)\n\n// 3. Export to 'dist/static'\nawait ssg.export('./dist/static')\n```\n\n### How it Works\n\n1. **Route Crawling**: The SSG crawls all registered `GET` routes in your application.\n2. **In-Memory Rendering**: For each route, it simulates a request using the `BunNativeAdapter` (or `PhotonAdapter`), rendering the page in memory.\n3. **File System Map**: It maps URLs to a directory structure:\n   - `/` becomes `index.html`\n   - `/about` becomes `about/index.html` (improving URL compatibility)\n\n### Dynamic Routes\n\nCurrently, the basic SSG crawls static routes. For dynamic routes (e.g., `/blog/:id`), you can manually trigger exports using the `StaticSiteGenerator` API in a loop over your database records.\n\n### CLI Usage\n\nIf you are using the Gravito CLI, you can simply run:\n\n```bash\ngravito export --out ./dist/static\n```"
}