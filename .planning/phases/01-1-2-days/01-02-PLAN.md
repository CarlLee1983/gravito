---
phase: 01-1-2-days
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/photon/dist/index.js
  - packages/photon/dist/index.mjs
  - packages/signal/dist/index.mjs
  - packages/signal/dist/index.cjs
autonomous: true
gap_closure: true
requirements:
  - CORE-02
  - CORE-04

must_haves:
  truths:
    - "Photon dist/index.js 可被 import 且 Photon class 可存取"
    - "Signal dist/index.mjs 可被 import 且 OrbitSignal 等 exports 可存取"
    - "Signal dist/index.cjs 可被 require 且 OrbitSignal 正常載入"
  artifacts:
    - path: "packages/photon/dist/index.js"
      provides: "Photon ESM bundle"
      contains: "Photon"
    - path: "packages/signal/dist/index.mjs"
      provides: "Signal ESM bundle"
      contains: "OrbitSignal"
    - path: "packages/signal/dist/index.cjs"
      provides: "Signal CJS bundle"
      contains: "OrbitSignal"
  key_links:
    - from: "packages/photon/dist/index.js"
      to: "packages/photon/src/index.ts"
      via: "build output from source"
      pattern: "export.*Photon"
    - from: "packages/signal/dist/index.mjs"
      to: "packages/signal/src/index.ts"
      via: "build output from source"
      pattern: "export.*OrbitSignal"
---

<objective>
修復 Photon 和 Signal 兩個核心模組的 dist bundle，關閉 Phase 1 驗證報告中的唯一 gap。

Purpose: Hono Phase 2-3 遷移後（commit 5843541c）photon 和 signal 包的 dist artifacts 未重建，導致 npm publish 和 consumers import 會失敗。源碼和測試均正常，這是純粹的 build artifact 問題。

Output: 兩個包的 dist 目錄包含正確的 ESM/CJS bundles，所有 exports 可正常存取。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-1-2-days/01-01-SUMMARY.md
@.planning/phases/01-1-2-days/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Photon and Signal dist bundles</name>
  <files>packages/photon/dist/*, packages/signal/dist/*</files>
  <action>
    Step 1 — 確認源碼測試仍正常（前置條件檢查）：
    - 執行 `cd packages/photon && bun test` 確認 photon 測試通過
    - 執行 `cd packages/signal && bun test` 確認 signal 測試通過（預期 42 tests pass）
    - 如果任一測試失敗，停止並報告 — 不要在測試失敗時 rebuild

    Step 2 — 重建 Photon dist bundle：
    - 執行 `bun run build --filter=@gravito/photon`
    - 等待構建完成，記錄輸出

    Step 3 — 重建 Signal dist bundle：
    - 執行 `bun run build --filter=@gravito/signal`
    - 等待構建完成，記錄輸出

    Step 4 — 確認 dist 文件已生成：
    - 檢查 `packages/photon/dist/index.js` 存在且大小 > 0
    - 檢查 `packages/signal/dist/index.mjs` 存在且大小 > 0
    - 檢查 `packages/signal/dist/index.cjs` 存在且大小 > 0

    Gap 原因：Hono Phase 2-3 遷移（commit 5843541c）修改了源碼但未重新 build dist artifacts。
  </action>
  <verify>
    <automated>ls -la packages/photon/dist/index.js packages/signal/dist/index.mjs packages/signal/dist/index.cjs && echo "All dist files exist"</automated>
  </verify>
  <done>Photon 和 Signal 的 dist bundles 已重新生成，所有輸出文件存在且大小 > 0</done>
</task>

<task type="auto">
  <name>Task 2: Verify dist bundles are importable and commit</name>
  <files>packages/photon/dist/index.js, packages/signal/dist/index.mjs, packages/signal/dist/index.cjs</files>
  <action>
    Step 1 — 驗證 Photon dist bundle 可正常 import：
    - 執行 `bun -e "import * as p from './packages/photon/dist/index.js'; console.log('Photon exports:', Object.keys(p).slice(0,5).join(', '))"`
    - 預期：成功輸出 export 名稱，無 "is not declared" 錯誤

    Step 2 — 驗證 Signal ESM bundle 可正常 import：
    - 執行 `bun -e "import * as s from './packages/signal/dist/index.mjs'; console.log('Signal ESM exports:', Object.keys(s).slice(0,5).join(', '))"`
    - 預期：成功輸出 export 名稱，無 VueMjmlRenderer/TypedMailable 未聲明錯誤

    Step 3 — 驗證 Signal CJS bundle 可正常 require：
    - 執行 `bun -e "const s = require('./packages/signal/dist/index.cjs'); console.log('Signal CJS exports:', Object.keys(s).slice(0,5).join(', '))"`
    - 預期：成功輸出 export 名稱，無 import_OrbitSignal 未定義錯誤

    Step 4 — 如果所有驗證通過，提交 dist artifacts：
    - `git add packages/photon/dist/ packages/signal/dist/`
    - Commit message: `fix: [photon, signal] rebuild dist bundles after Hono Phase 2-3 migration`

    Step 5 — 更新 01-VERIFICATION.md 的 gap 狀態（如適用）。
  </action>
  <verify>
    <automated>bun -e "import * as p from './packages/photon/dist/index.js'; import * as s from './packages/signal/dist/index.mjs'; const sc = require('./packages/signal/dist/index.cjs'); console.log('Photon:', Object.keys(p).length, 'exports'); console.log('Signal ESM:', Object.keys(s).length, 'exports'); console.log('Signal CJS:', Object.keys(sc).length, 'exports'); console.log('ALL PASS')"</automated>
  </verify>
  <done>三個 dist bundle 均可正常 import/require，exports 數量 > 0，commit 已提交</done>
</task>

</tasks>

<verification>
Phase 1 gap closure 完成驗證：

1. `bun -e "import * as p from './packages/photon/dist/index.js'; console.log(Object.keys(p)[0])"` — 不報錯，輸出有效 export 名稱
2. `bun -e "import * as s from './packages/signal/dist/index.mjs'; console.log(Object.keys(s)[0])"` — 不報錯，輸出有效 export 名稱
3. `bun -e "const s = require('./packages/signal/dist/index.cjs'); console.log(Object.keys(s)[0])"` — 不報錯，輸出有效 export 名稱
4. `git log --oneline -1` — 確認 dist rebuild commit 存在
</verification>

<success_criteria>
- Photon dist/index.js 可被 import 且 Photon class 可存取（CRIT-01 resolved）
- Signal dist/index.mjs 可被 import 且所有 exports 可存取（CRIT-02 resolved）
- Signal dist/index.cjs 可被 require 且 OrbitSignal 正常載入（CRIT-02 resolved）
- dist artifacts 已 commit 至 git
</success_criteria>

<output>
After completion, create `.planning/phases/01-1-2-days/01-02-SUMMARY.md`
</output>
