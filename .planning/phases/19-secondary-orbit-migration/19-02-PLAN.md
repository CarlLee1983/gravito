---
phase: 19-secondary-orbit-migration
plan: "02"
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - packages/fortify/src/errors/FortifyError.ts
  - packages/fortify/src/errors/codes.ts
  - packages/fortify/tests/contract/fortify-errors.contract.test.ts
  - packages/astral/src/errors.ts
  - packages/astral/tests/contract/astral-errors.contract.test.ts
  - packages/quasar/src/errors/QuasarError.ts
  - packages/quasar/tests/contract/quasar-errors.contract.test.ts
  - packages/ripple/src/errors/RippleError.ts
  - packages/ripple/tests/contract/ripple-errors.contract.test.ts
autonomous: true
requirements: [MIGR-01, MIGR-02]

must_haves:
  truths:
    - "FortifyError extends AuthException and all 30+ factory methods still work"
    - "AstralError extends SystemException with Object.setPrototypeOf"
    - "QuasarError extends QueueException with namespaced error codes"
    - "RippleError extends InfrastructureException with preserved RippleDriverError"
    - "All existing tests for fortify, astral, quasar, ripple still pass"
  artifacts:
    - path: "packages/fortify/src/errors/FortifyError.ts"
      provides: "FortifyError re-parented to AuthException"
      contains: "extends AuthException"
    - path: "packages/astral/src/errors.ts"
      provides: "AstralError re-parented to SystemException"
      contains: "extends SystemException"
    - path: "packages/quasar/src/errors/QuasarError.ts"
      provides: "QuasarError re-parented to QueueException"
      contains: "extends QueueException"
    - path: "packages/ripple/src/errors/RippleError.ts"
      provides: "RippleError re-parented to InfrastructureException"
      contains: "extends InfrastructureException"
  key_links:
    - from: "packages/fortify/src/errors/FortifyError.ts"
      to: "@gravito/core AuthException"
      via: "import + extends"
      pattern: "import.*AuthException.*from.*@gravito/core"
    - from: "packages/quasar/src/errors/QuasarError.ts"
      to: "@gravito/core QueueException"
      via: "import + extends"
      pattern: "import.*QueueException.*from.*@gravito/core"
---

<objective>
Migrate 4 HIGH-priority Orbit packages (fortify, astral, quasar, ripple) to the GravitoException hierarchy by re-parenting their existing custom error classes.

Purpose: These packages have the most complex existing error classes with factory methods, custom fields, and many test assertions. Migrating them first validates the re-parent pattern before the simpler MEDIUM/LOW batches. Per D-01 Batch 1.
Output: 4 packages fully migrated with contract tests, all existing tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-secondary-orbit-migration/19-CONTEXT.md
@.planning/phases/19-secondary-orbit-migration/19-RESEARCH.md
@.planning/phases/19-secondary-orbit-migration/19-01-SUMMARY.md

<interfaces>
<!-- AuthException created in Plan 01 -->
From packages/core/src/exceptions/AuthException.ts:
```typescript
import { type ExceptionOptions, DomainException } from './DomainException'
export abstract class AuthException extends DomainException {
  constructor(status: number, code: string, options: ExceptionOptions = {})
}
```

<!-- QueueException created in Plan 01 -->
From packages/core/src/exceptions/QueueException.ts:
```typescript
import { InfrastructureException, type InfrastructureExceptionOptions } from './InfrastructureException'
export abstract class QueueException extends InfrastructureException {
  constructor(status: number, code: string, options: InfrastructureExceptionOptions = {})
}
```

<!-- SystemException (existing) -->
From packages/core/src/exceptions/SystemException.ts:
```typescript
export abstract class SystemException extends GravitoException {
  constructor(status: number, code: string, options: ExceptionOptions = {})
}
```

<!-- InfrastructureException (existing) -->
From packages/core/src/exceptions/InfrastructureException.ts:
```typescript
export abstract class InfrastructureException extends GravitoException {
  public readonly retryable: boolean
  constructor(status: number, code: string, options: InfrastructureExceptionOptions = {})
}
```

<!-- Contract test helper -->
From packages/core/tests/contract/helpers.ts:
```typescript
export interface ContractAssertOptions {
  expectedCode: string
  expectedStatus: number
  expectedInstanceOf?: Function[]
  expectRetryable?: boolean
  expectCause?: boolean
}
export function assertGravitoException(err: unknown, opts: ContractAssertOptions): void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate fortify FortifyError to extend AuthException</name>
  <files>
    packages/fortify/src/errors/FortifyError.ts,
    packages/fortify/src/errors/codes.ts,
    packages/fortify/tests/contract/fortify-errors.contract.test.ts
  </files>
  <read_first>
    packages/fortify/src/errors/FortifyError.ts,
    packages/fortify/src/errors/codes.ts,
    packages/core/src/exceptions/AuthException.ts,
    packages/core/src/exceptions/DomainException.ts,
    packages/plasma/src/errors.ts
  </read_first>
  <action>
**FortifyError migration (per D-05, RESEARCH Pitfall 2):**

1. Change `FortifyError extends Error` to `FortifyError extends AuthException`
2. Import AuthException: `import { AuthException } from '@gravito/core'` (or relative path if workspace resolution prefers it)
3. Update constructor to call `super(httpStatus, code, {})` — AuthException expects (status, code, options)
4. Add `Object.setPrototypeOf(this, new.target.prototype)` as last statement in constructor
5. Set `this.name = 'FortifyError'`
6. **CRITICAL per Pitfall 2:** Keep `httpStatus` as a deprecated getter: `/** @deprecated Use .status instead */ public get httpStatus(): number { return this.status }`
7. Remove old `public readonly httpStatus: number` field (now provided by GravitoException.status)
8. Preserve ALL 30+ factory methods exactly as they are — do not change any factory method signature or behavior
9. Preserve `public readonly details?: unknown` field

**Contract test:**
Create `packages/fortify/tests/contract/fortify-errors.contract.test.ts`:
- Import `assertGravitoException` from `../../../core/tests/contract/helpers`
- Test FortifyError.invalidCredentials() satisfies contract: code matches, status=401, instanceof AuthException + DomainException + GravitoException
- Test FortifyError httpStatus backward compat: `error.httpStatus === error.status`
- Test cause chain preservation: `new FortifyError(code, 500, details)` with cause

**Verify after migration:**
- `cd packages/fortify && bun test` — all existing 24 test files still pass
- `cd packages/fortify && bun test tests/contract/` — contract test passes
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core/packages/fortify && bun test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - FortifyError.ts contains `extends AuthException`
    - FortifyError.ts contains `Object.setPrototypeOf(this, new.target.prototype)`
    - FortifyError.ts contains `get httpStatus(): number { return this.status }`
    - FortifyError.ts does NOT contain `extends Error`
    - fortify-errors.contract.test.ts contains `assertGravitoException`
    - All fortify tests pass (0 fail)
  </acceptance_criteria>
  <done>FortifyError extends AuthException, httpStatus backward compat preserved, all 24 test files pass, contract test passes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate astral, quasar, ripple error classes</name>
  <files>
    packages/astral/src/errors.ts,
    packages/astral/tests/contract/astral-errors.contract.test.ts,
    packages/quasar/src/errors/QuasarError.ts,
    packages/quasar/tests/contract/quasar-errors.contract.test.ts,
    packages/ripple/src/errors/RippleError.ts,
    packages/ripple/tests/contract/ripple-errors.contract.test.ts
  </files>
  <read_first>
    packages/astral/src/errors.ts,
    packages/quasar/src/errors/QuasarError.ts,
    packages/ripple/src/errors/RippleError.ts,
    packages/core/src/exceptions/SystemException.ts,
    packages/core/src/exceptions/QueueException.ts,
    packages/core/src/exceptions/InfrastructureException.ts,
    packages/plasma/src/errors.ts
  </read_first>
  <action>
**AstralError migration (per D-05):**
1. Change `AstralError` parent from `Error` (or current parent) to `SystemException`
2. Import: `import { SystemException, type ExceptionOptions } from '@gravito/core'`
3. Update constructor to call `super(status, code, options)` matching SystemException signature
4. Ensure `Object.setPrototypeOf(this, new.target.prototype)` is present (research says it already has it — verify)
5. Set `this.name = 'AstralError'`
6. Add AstralErrorCodes const if not exists: `astral.config_invalid`, `astral.initialization_failed` etc. based on existing throw patterns
7. Replace any bare `throw new Error(...)` with `throw new AstralError(status, code, { message, cause })` (only 1 bare throw)
8. Create contract test at `packages/astral/tests/contract/astral-errors.contract.test.ts`

**QuasarError migration (per D-05):**
1. Change `QuasarError` parent to `QueueException`
2. Import: `import { QueueException, type InfrastructureExceptionOptions } from '@gravito/core'`
3. Update constructor to call `super(status, code, options)`
4. Add `Object.setPrototypeOf(this, new.target.prototype)`
5. Set `this.name = 'QuasarError'`
6. Ensure existing `QuasarError.code` prefix pattern maps to dot-separated namespace: `quasar.not_found`, `quasar.timeout`, `quasar.connection_failed` etc.
7. Replace 8 bare `throw new Error(...)` with QuasarError instances
8. **CRITICAL per Pitfall 6:** quasar has 0 test files — create contract test at `packages/quasar/tests/contract/quasar-errors.contract.test.ts` as part of this task

**RippleError migration (per D-05):**
1. Change `RippleError` parent to `InfrastructureException`
2. Import: `import { InfrastructureException, type InfrastructureExceptionOptions } from '@gravito/core'`
3. Update constructor to call `super(status, code, options)`
4. Ensure `Object.setPrototypeOf(this, new.target.prototype)` (research says it already has it)
5. Set `this.name = 'RippleError'`
6. Preserve `RippleDriverError` subclass — update its parent to extend `RippleError` (which now extends InfrastructureException)
7. Add RippleErrorCodes: `ripple.connection_failed`, `ripple.query_failed`, `ripple.driver_error` etc.
8. Replace 16 bare `throw new Error(...)` with RippleError instances
9. Create contract test at `packages/ripple/tests/contract/ripple-errors.contract.test.ts`

**Verify each package independently after migration (per D-03):**
- `cd packages/astral && bun test`
- `cd packages/quasar && bun test`
- `cd packages/ripple && bun test`
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && for pkg in astral quasar ripple; do echo "=== $pkg ===" && cd packages/$pkg && bun test 2>&1 | tail -3 && cd ../..; done</automated>
  </verify>
  <acceptance_criteria>
    - packages/astral/src/errors.ts contains `extends SystemException`
    - packages/quasar/src/errors/QuasarError.ts contains `extends QueueException`
    - packages/ripple/src/errors/RippleError.ts contains `extends InfrastructureException`
    - All 3 files contain `Object.setPrototypeOf(this, new.target.prototype)`
    - `grep -r "throw new Error" packages/astral/src/ packages/quasar/src/ packages/ripple/src/` returns 0 matches
    - packages/quasar/tests/contract/ directory exists (was missing before)
    - All tests pass for astral, quasar, ripple
  </acceptance_criteria>
  <done>AstralError extends SystemException, QuasarError extends QueueException, RippleError extends InfrastructureException; all bare throws replaced; contract tests pass; quasar has its first test file</done>
</task>

</tasks>

<verification>
- `cd packages/fortify && bun test` — all pass
- `cd packages/astral && bun test` — all pass
- `cd packages/quasar && bun test` — all pass (including new contract test)
- `cd packages/ripple && bun test` — all pass
- `grep -r "throw new Error" packages/fortify/src/ packages/astral/src/ packages/quasar/src/ packages/ripple/src/ | wc -l` = 0
- `bun run typecheck` — no regressions
</verification>

<success_criteria>
- FortifyError extends AuthException with httpStatus backward compat
- AstralError extends SystemException
- QuasarError extends QueueException (first tests created for quasar)
- RippleError extends InfrastructureException
- 0 bare `throw new Error()` in fortify, astral, quasar, ripple src/
- All existing tests pass, new contract tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-secondary-orbit-migration/19-02-SUMMARY.md`
</output>
