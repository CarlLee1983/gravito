---
phase: 19-secondary-orbit-migration
plan: "07"
type: execute
wave: 3
depends_on: ["19-01"]
files_modified:
  - packages/horizon/src/errors/HorizonError.ts
  - packages/horizon/src/errors/codes.ts
  - packages/horizon/tests/contract/horizon-errors.contract.test.ts
  - packages/monitor/src/errors/MonitorError.ts
  - packages/monitor/src/errors/codes.ts
  - packages/zenith/src/errors/ZenithError.ts
  - packages/zenith/src/errors/codes.ts
  - packages/launchpad/src/errors/LaunchpadError.ts
  - packages/launchpad/src/errors/codes.ts
  - packages/launchpad/tests/contract/launchpad-errors.contract.test.ts
  - packages/cli/src/errors/CliError.ts
  - packages/cli/src/errors/codes.ts
  - packages/cli/tests/contract/cli-errors.contract.test.ts
  - packages/luminosity/src/errors/LuminosityError.ts
  - packages/luminosity/src/errors/codes.ts
  - packages/luminosity/tests/contract/luminosity-errors.contract.test.ts
  - packages/prism/src/errors/PrismError.ts
  - packages/prism/src/errors/codes.ts
  - packages/prism/tests/contract/prism-errors.contract.test.ts
autonomous: true
requirements: [MIGR-01, MIGR-02]

must_haves:
  truths:
    - "All 7 DevOps packages throw GravitoException subclass with namespaced codes"
    - "No bare throw new Error() remains in horizon, monitor, zenith, launchpad, cli, luminosity, prism"
    - "luminosity has two ErrorCode groups: luminosity.storage.* and luminosity.config.*"
    - "All existing tests pass"
  artifacts:
    - path: "packages/horizon/src/errors/codes.ts"
      provides: "HorizonErrorCodes namespace"
      contains: "horizon."
    - path: "packages/luminosity/src/errors/codes.ts"
      provides: "LuminosityErrorCodes namespace"
      contains: "luminosity."
  key_links:
    - from: "packages/luminosity/src/errors/LuminosityError.ts"
      to: "@gravito/core InfrastructureException"
      via: "import + extends"
      pattern: "import.*InfrastructureException.*from.*@gravito/core"
---

<objective>
Migrate Batch 4 (DevOps domain) — 7 MEDIUM packages to GravitoException hierarchy.

Purpose: Per D-01 Batch 4, these are scheduling/monitoring/tooling packages. All follow the MEDIUM pattern (D-06). luminosity is special — has mixed I/O (S3) and utility (SEO) code requiring two ErrorCode groups (per Research open question #3).
Output: 7 packages migrated, 89 total bare throws replaced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-secondary-orbit-migration/19-CONTEXT.md
@.planning/phases/19-secondary-orbit-migration/19-RESEARCH.md
@.planning/phases/19-secondary-orbit-migration/19-01-SUMMARY.md

<interfaces>
<!-- SystemException (existing) -->
From packages/core/src/exceptions/SystemException.ts:
```typescript
export abstract class SystemException extends GravitoException {
  constructor(status: number, code: string, options: ExceptionOptions = {})
}
```

<!-- InfrastructureException (existing) -->
From packages/core/src/exceptions/InfrastructureException.ts:
```typescript
export abstract class InfrastructureException extends GravitoException {
  public readonly retryable: boolean
  constructor(status: number, code: string, options: InfrastructureExceptionOptions = {})
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate horizon, monitor, zenith, launchpad (scheduling/monitoring)</name>
  <files>
    packages/horizon/src/errors/HorizonError.ts,
    packages/horizon/src/errors/codes.ts,
    packages/horizon/tests/contract/horizon-errors.contract.test.ts,
    packages/monitor/src/errors/MonitorError.ts,
    packages/monitor/src/errors/codes.ts,
    packages/zenith/src/errors/ZenithError.ts,
    packages/zenith/src/errors/codes.ts,
    packages/launchpad/src/errors/LaunchpadError.ts,
    packages/launchpad/src/errors/codes.ts,
    packages/launchpad/tests/contract/launchpad-errors.contract.test.ts
  </files>
  <read_first>
    packages/horizon/src/,
    packages/monitor/src/,
    packages/zenith/src/,
    packages/launchpad/src/,
    packages/core/src/exceptions/SystemException.ts
  </read_first>
  <action>
All 4 packages use `SystemException` as parent — these are scheduling/monitoring/tooling, not I/O infrastructure.

**Per-package steps (MEDIUM D-06 pattern):**

1. **horizon** (17 throws): Task scheduling. Create `HorizonError extends SystemException`. Codes: `horizon.schedule_failed`, `horizon.job_not_found`, `horizon.worker_error`, `horizon.cron_parse_error`, etc.
2. **monitor** (1 throw): System monitoring. Create `MonitorError extends SystemException`. Codes: `monitor.check_failed`. Minimal — only 1 throw.
3. **zenith** (4 throws): Deployment tooling. Create `ZenithError extends SystemException`. Codes: `zenith.deploy_failed`, `zenith.config_error`, etc.
4. **launchpad** (16 throws): Project scaffolding. Create `LaunchpadError extends SystemException`. Codes: `launchpad.scaffold_failed`, `launchpad.template_error`, `launchpad.dependency_resolution_failed`, etc.

Each concrete error class:
```typescript
import { SystemException, type ExceptionOptions } from '@gravito/core'
import type { HorizonErrorCode } from './codes'

export class HorizonError extends SystemException {
  constructor(status: number, code: HorizonErrorCode, options: ExceptionOptions = {}) {
    super(status, code, options)
    this.name = 'HorizonError'
    Object.setPrototypeOf(this, new.target.prototype)
  }
}
```

Replace all bare throws. Create contract tests for horizon (17 throws) and launchpad (16 throws).
Verify: `cd packages/<name> && bun test`
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && for pkg in horizon monitor zenith launchpad; do echo "=== $pkg ===" && grep -r "throw new Error" packages/$pkg/src/ | wc -l; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "throw new Error" packages/horizon/src/ | wc -l` returns 0
    - `grep -r "throw new Error" packages/monitor/src/ | wc -l` returns 0
    - `grep -r "throw new Error" packages/zenith/src/ | wc -l` returns 0
    - `grep -r "throw new Error" packages/launchpad/src/ | wc -l` returns 0
    - Each error class contains `extends SystemException`
    - All tests pass for all 4 packages
  </acceptance_criteria>
  <done>4 scheduling/monitoring packages migrated: 38 bare throws replaced, all extend SystemException</done>
</task>

<task type="auto">
  <name>Task 2: Migrate cli, luminosity, prism (tooling packages)</name>
  <files>
    packages/cli/src/errors/CliError.ts,
    packages/cli/src/errors/codes.ts,
    packages/cli/tests/contract/cli-errors.contract.test.ts,
    packages/luminosity/src/errors/LuminosityError.ts,
    packages/luminosity/src/errors/codes.ts,
    packages/luminosity/tests/contract/luminosity-errors.contract.test.ts,
    packages/prism/src/errors/PrismError.ts,
    packages/prism/src/errors/codes.ts,
    packages/prism/tests/contract/prism-errors.contract.test.ts
  </files>
  <read_first>
    packages/cli/src/,
    packages/luminosity/src/,
    packages/prism/src/,
    packages/core/src/exceptions/SystemException.ts,
    packages/core/src/exceptions/InfrastructureException.ts
  </read_first>
  <action>
**cli** (19 throws): Use `SystemException`. Create `CliError extends SystemException`. Codes: `cli.command_not_found`, `cli.parse_error`, `cli.execution_failed`, `cli.config_error`, etc.

**luminosity** (21 throws, mixed domain per Research open question #3):
- Create `LuminosityError extends InfrastructureException` (primary parent — S3/network paths dominate)
- Create TWO ErrorCode groups in `codes.ts`:
  ```typescript
  export const LuminosityErrorCodes = {
    // Storage/network errors (retryable)
    STORAGE_UPLOAD_FAILED: 'luminosity.storage.upload_failed',
    STORAGE_DOWNLOAD_FAILED: 'luminosity.storage.download_failed',
    STORAGE_CONNECTION_ERROR: 'luminosity.storage.connection_error',
    // Config/validation errors (not retryable)
    CONFIG_INVALID: 'luminosity.config.invalid',
    CONFIG_MISSING: 'luminosity.config.missing',
    SEO_RENDER_FAILED: 'luminosity.seo.render_failed',
    // ... derive from actual throw messages
  } as const
  ```
- When replacing throws: use `retryable: true` for `luminosity.storage.*` errors, `retryable: false` for `luminosity.config.*` and `luminosity.seo.*` errors

**prism** (11 throws): Use `SystemException`. Create `PrismError extends SystemException`. Codes: `prism.transform_failed`, `prism.render_error`, `prism.plugin_error`, etc.

Replace all bare throws. Create contract tests for all 3 packages (each has significant throw counts).
Verify: `cd packages/<name> && bun test`
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && for pkg in cli luminosity prism; do echo "=== $pkg ===" && grep -r "throw new Error" packages/$pkg/src/ | wc -l; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "throw new Error" packages/cli/src/ | wc -l` returns 0
    - `grep -r "throw new Error" packages/luminosity/src/ | wc -l` returns 0
    - `grep -r "throw new Error" packages/prism/src/ | wc -l` returns 0
    - packages/cli/src/errors/CliError.ts contains `extends SystemException`
    - packages/luminosity/src/errors/LuminosityError.ts contains `extends InfrastructureException`
    - packages/luminosity/src/errors/codes.ts contains `luminosity.storage.` AND `luminosity.config.`
    - packages/prism/src/errors/PrismError.ts contains `extends SystemException`
    - All tests pass for cli, luminosity, prism
  </acceptance_criteria>
  <done>3 tooling packages migrated: 51 bare throws replaced, luminosity has dual ErrorCode groups, contract tests pass</done>
</task>

</tasks>

<verification>
- `for pkg in horizon monitor zenith launchpad cli luminosity prism; do cd packages/$pkg && bun test && cd ../..; done` — all pass
- `grep -r "throw new Error" packages/horizon/src/ packages/monitor/src/ packages/zenith/src/ packages/launchpad/src/ packages/cli/src/ packages/luminosity/src/ packages/prism/src/ | wc -l` = 0
- `bun run typecheck` — no regressions
</verification>

<success_criteria>
- All 7 Batch 4 packages migrated
- 89 total bare throws replaced (17+1+4+16+19+21+11)
- luminosity has separate storage vs config error code namespaces
- All existing tests pass, contract tests added
</success_criteria>

<output>
After completion, create `.planning/phases/19-secondary-orbit-migration/19-07-SUMMARY.md`
</output>
