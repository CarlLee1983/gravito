---
phase: 19-secondary-orbit-migration
plan: "06"
type: execute
wave: 3
depends_on: ["19-01"]
files_modified:
  - packages/echo/src/errors/EchoError.ts
  - packages/echo/src/errors/codes.ts
  - packages/echo/tests/contract/echo-errors.contract.test.ts
  - packages/flare/src/errors/FlareError.ts
  - packages/flare/src/errors/codes.ts
  - packages/flare/tests/contract/flare-errors.contract.test.ts
  - packages/radiance/src/errors/RadianceError.ts
  - packages/radiance/src/errors/codes.ts
  - packages/quark/src/errors/QuarkError.ts
  - packages/quark/src/errors/codes.ts
  - packages/graphql/src/errors/GraphqlError.ts
  - packages/graphql/src/errors/codes.ts
  - packages/graphql/tests/contract/graphql-errors.contract.test.ts
  - packages/impulse/src/errors/ImpulseError.ts
  - packages/impulse/src/errors/codes.ts
  - packages/impulse-bridge/src/errors/codes.ts
  - packages/monolith/src/errors/MonolithError.ts
  - packages/monolith/src/errors/codes.ts
autonomous: true
requirements: [MIGR-01, MIGR-02]

must_haves:
  truths:
    - "All 8 communication packages throw GravitoException subclass with namespaced codes"
    - "No bare throw new Error() remains in echo, flare, radiance, quark, graphql, impulse, impulse-bridge, monolith"
    - "All existing tests pass for all 8 packages"
  artifacts:
    - path: "packages/echo/src/errors/codes.ts"
      provides: "EchoErrorCodes namespace"
      contains: "echo."
    - path: "packages/flare/src/errors/codes.ts"
      provides: "FlareErrorCodes namespace"
      contains: "flare."
    - path: "packages/graphql/src/errors/codes.ts"
      provides: "GraphqlErrorCodes namespace"
      contains: "graphql."
  key_links:
    - from: "packages/echo/src/errors/EchoError.ts"
      to: "@gravito/core InfrastructureException"
      via: "import + extends"
      pattern: "import.*InfrastructureException.*from.*@gravito/core"
---

<objective>
Migrate Batch 3 (Communication domain) — 8 MEDIUM packages to GravitoException hierarchy.

Purpose: Per D-01 Batch 3, these are communication/network packages. All follow the MEDIUM pattern (D-06). impulse-bridge has 0 bare throws — only needs ErrorCodes added.
Output: 8 packages migrated, 66 total bare throws replaced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-secondary-orbit-migration/19-CONTEXT.md
@.planning/phases/19-secondary-orbit-migration/19-RESEARCH.md
@.planning/phases/19-secondary-orbit-migration/19-01-SUMMARY.md

<interfaces>
<!-- InfrastructureException (existing) -->
From packages/core/src/exceptions/InfrastructureException.ts:
```typescript
export abstract class InfrastructureException extends GravitoException {
  public readonly retryable: boolean
  constructor(status: number, code: string, options: InfrastructureExceptionOptions = {})
}
```

<!-- SystemException (existing) -->
From packages/core/src/exceptions/SystemException.ts:
```typescript
export abstract class SystemException extends GravitoException {
  constructor(status: number, code: string, options: ExceptionOptions = {})
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate echo, flare, radiance, quark (network/notification packages)</name>
  <files>
    packages/echo/src/errors/EchoError.ts,
    packages/echo/src/errors/codes.ts,
    packages/echo/tests/contract/echo-errors.contract.test.ts,
    packages/flare/src/errors/FlareError.ts,
    packages/flare/src/errors/codes.ts,
    packages/flare/tests/contract/flare-errors.contract.test.ts,
    packages/radiance/src/errors/RadianceError.ts,
    packages/radiance/src/errors/codes.ts,
    packages/quark/src/errors/QuarkError.ts,
    packages/quark/src/errors/codes.ts
  </files>
  <read_first>
    packages/echo/src/,
    packages/flare/src/,
    packages/radiance/src/,
    packages/quark/src/,
    packages/core/src/exceptions/InfrastructureException.ts,
    packages/plasma/src/errors.ts
  </read_first>
  <action>
For echo (7 throws), flare (21 throws), radiance (4 throws), quark (6 throws), apply the MEDIUM migration pattern per D-06:

**Per-package steps:**

1. Scan `packages/<name>/src/` for all `throw new Error(` occurrences
2. Create `errors/codes.ts` with namespaced ErrorCodes:
   - echo: `echo.connection_failed`, `echo.publish_failed`, `echo.subscribe_failed`, etc. (InfrastructureException)
   - flare: `flare.notification_failed`, `flare.channel_error`, `flare.template_error`, etc. (InfrastructureException)
   - radiance: `radiance.render_failed`, `radiance.connection_error`, etc. (InfrastructureException)
   - quark: `quark.validation_failed`, `quark.transform_error`, etc. (InfrastructureException)

3. Create concrete error class extending InfrastructureException:
   ```typescript
   import { InfrastructureException, type InfrastructureExceptionOptions } from '@gravito/core'
   import type { EchoErrorCode } from './codes'

   export class EchoError extends InfrastructureException {
     constructor(status: number, code: EchoErrorCode, options: InfrastructureExceptionOptions = {}) {
       super(status, code, options)
       this.name = 'EchoError'
       Object.setPrototypeOf(this, new.target.prototype)
     }
   }
   ```

4. Replace all bare `throw new Error(...)` with `throw new <Name>Error(status, code, { message, cause, retryable })`
5. Create contract tests for echo and flare (highest throw counts)
6. Export error classes from package index if applicable
7. Verify: `cd packages/<name> && bun test`
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && for pkg in echo flare radiance quark; do echo "=== $pkg ===" && grep -r "throw new Error" packages/$pkg/src/ | wc -l; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "throw new Error" packages/echo/src/ | wc -l` returns 0
    - `grep -r "throw new Error" packages/flare/src/ | wc -l` returns 0
    - `grep -r "throw new Error" packages/radiance/src/ | wc -l` returns 0
    - `grep -r "throw new Error" packages/quark/src/ | wc -l` returns 0
    - Each error class contains `extends InfrastructureException`
    - Each codes.ts contains package-namespaced error codes
    - All tests pass for echo, flare, radiance, quark
  </acceptance_criteria>
  <done>4 network packages migrated: 38 bare throws replaced, error classes extend InfrastructureException, ErrorCodes created</done>
</task>

<task type="auto">
  <name>Task 2: Migrate graphql, impulse, impulse-bridge, monolith</name>
  <files>
    packages/graphql/src/errors/GraphqlError.ts,
    packages/graphql/src/errors/codes.ts,
    packages/graphql/tests/contract/graphql-errors.contract.test.ts,
    packages/impulse/src/errors/ImpulseError.ts,
    packages/impulse/src/errors/codes.ts,
    packages/impulse-bridge/src/errors/codes.ts,
    packages/monolith/src/errors/MonolithError.ts,
    packages/monolith/src/errors/codes.ts
  </files>
  <read_first>
    packages/graphql/src/,
    packages/impulse/src/,
    packages/impulse-bridge/src/,
    packages/monolith/src/,
    packages/core/src/exceptions/SystemException.ts,
    packages/core/src/exceptions/InfrastructureException.ts
  </read_first>
  <action>
Migrate 4 packages:
- **graphql** (15 throws): Use `SystemException` — graphql errors are system/framework-level, not infrastructure I/O. Create `GraphqlError extends SystemException`. Codes: `graphql.schema_error`, `graphql.resolver_failed`, `graphql.validation_error`, `graphql.execution_error`, etc.
- **impulse** (8 throws): Use `InfrastructureException` — WebSocket/realtime communication. Create `ImpulseError extends InfrastructureException`. Codes: `impulse.connection_failed`, `impulse.broadcast_error`, `impulse.channel_error`, etc.
- **impulse-bridge** (0 throws): Only add ErrorCodes file — no error class needed per D-07 since there are no throws. Create `ImpulseBridgeErrorCodes` const for documentation consistency. No bare throws to replace.
- **monolith** (5 throws): Use `InfrastructureException`. Create `MonolithError extends InfrastructureException`. Codes: `monolith.merge_failed`, `monolith.config_error`, etc.

**Per-package steps:**
1. Scan, create ErrorCodes, create concrete error class (except impulse-bridge)
2. Replace bare throws
3. Create contract test for graphql (highest throw count, 15)
4. Verify: `cd packages/<name> && bun test`
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && for pkg in graphql impulse impulse-bridge monolith; do echo "=== $pkg ===" && grep -r "throw new Error" packages/$pkg/src/ | wc -l; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "throw new Error" packages/graphql/src/ | wc -l` returns 0
    - `grep -r "throw new Error" packages/impulse/src/ | wc -l` returns 0
    - `grep -r "throw new Error" packages/impulse-bridge/src/ | wc -l` returns 0
    - `grep -r "throw new Error" packages/monolith/src/ | wc -l` returns 0
    - packages/graphql/src/errors/GraphqlError.ts contains `extends SystemException`
    - packages/impulse/src/errors/ImpulseError.ts contains `extends InfrastructureException`
    - packages/impulse-bridge/src/errors/codes.ts exists
    - All tests pass for graphql, impulse, impulse-bridge, monolith
  </acceptance_criteria>
  <done>4 packages migrated: 28 bare throws replaced, graphql uses SystemException, impulse/monolith use InfrastructureException, impulse-bridge has ErrorCodes only</done>
</task>

</tasks>

<verification>
- `for pkg in echo flare radiance quark graphql impulse impulse-bridge monolith; do cd packages/$pkg && bun test && cd ../..; done` — all pass
- `grep -r "throw new Error" packages/echo/src/ packages/flare/src/ packages/radiance/src/ packages/quark/src/ packages/graphql/src/ packages/impulse/src/ packages/impulse-bridge/src/ packages/monolith/src/ | wc -l` = 0
- `bun run typecheck` — no regressions
</verification>

<success_criteria>
- All 8 Batch 3 packages migrated
- 66 total bare throws replaced (7+21+4+6+15+8+0+5)
- ErrorCodes with dot-separated namespaces created for each package
- All existing tests pass, contract tests added for high-throw packages
</success_criteria>

<output>
After completion, create `.planning/phases/19-secondary-orbit-migration/19-06-SUMMARY.md`
</output>
