---
phase: 05-satellite-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-satellite-verification/05-03-SUMMARY.md
autonomous: true
requirements: [SAT-COMMERCE, SAT-D02-COMMERCE, SAT-CROSS]

must_haves:
  truths:
    - "Commerce 71 tests all pass (0 fail, no regressions)"
    - "Commerce five-dimension audit completed with per-dimension status"
    - "Commerce cross-satellite dependency on flash-sale assessed and documented"
    - "flash-sale GravitoEngineAdapter issue (TD-06) impact evaluated"
  artifacts:
    - path: ".planning/phases/05-satellite-verification/05-03-SUMMARY.md"
      provides: "Commerce five-dimension audit report + cross-satellite analysis"
  key_links:
    - from: "commerce/package.json"
      to: "@gravito/satellite-flash-sale"
      via: "npm dependency (^0.2.0)"
      pattern: "satellite-flash-sale"
    - from: "commerce/src/"
      to: "@gravito/core GravitoContext"
      via: "type import"
      pattern: "import.*GravitoContext.*from.*@gravito/core"
---

<objective>
Complete Commerce satellite five-dimension audit and cross-satellite dependency verification

Purpose: Commerce is the most complex satellite with a cross-satellite dependency on flash-sale (the only such dependency among the three). This plan validates Commerce's health and specifically investigates the flash-sale coupling (TD-02, TD-06) which is the primary integration risk.

Output: Commerce five-dimension audit report + cross-satellite dependency analysis in SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-satellite-verification/05-CONTEXT.md
@.planning/phases/05-satellite-verification/05-RESEARCH.md

CRITICAL: Satellite source code is NOT in gravito-core. It is at:
/Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/commerce/

flash-sale satellite is at:
/Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/flash-sale/

All test commands must run in the respective satellite directories.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commerce five-dimension audit</name>
  <files>.planning/phases/05-satellite-verification/05-03-SUMMARY.md</files>
  <action>
Run Commerce tests and perform five-dimension audit. Work in: `/Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/commerce/`

**Test Baseline:**
1. `cd /Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/commerce && bun test` — record total/pass/fail/skip
2. `bun tsc -p tsconfig.json --noEmit --skipLibCheck` — record TypeCheck result
Expected: 71 pass, 0 fail, 0 TypeCheck errors.

**Dimension 1: Test Coverage**
- Record test counts
- List test files: `find tests/ -name "*.test.ts" -o -name "*.spec.ts"`
- Note: RESEARCH.md flags TD-03 — Interface/Http Controllers (AdminOrderController, CheckoutController) have NO tests
- Assess: what percentage of domain logic vs infrastructure is tested?

**Dimension 2: Integration Health**
- Check for cross-satellite imports: `grep -r "from '@gravito/satellite-" src/`
- This is the critical check: Commerce depends on `@gravito/satellite-flash-sale: "^0.2.0"` (see package.json)
- Determine HOW Commerce uses flash-sale: is it a direct import in src/ or only in tests?
- List all @gravito/* dependencies: `grep -r "from '@gravito/" src/ | sort -u`
- Cross-reference with package.json
- Status: Expected YELLOW (per RESEARCH.md) due to flash-sale coupling

**Dimension 3: Type Safety**
- TypeCheck result
- Search `as any` usage: `grep -rn "as any" src/` — note TD-05 (AdminOrderController `ctx.json() as any`)
- Search `@ts-ignore` / `@ts-expect-error`: `grep -rn "@ts-ignore\|@ts-expect-error" src/`

**Dimension 4: API Stability**
- Read `src/index.ts` for public exports
- Check version (expected: 0.2.0)
- Note the CommerceServiceProvider entry point

**Dimension 5: Hono Readiness**
- Confirm zero Hono/photon imports: `grep -r "from 'hono\|from \"hono\|from '@gravito/photon" src/`
- Confirm GravitoContext usage: `grep -r "GravitoContext" src/`
- Expected: GREEN
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/commerce && bun test 2>&1 | tail -5 && bun tsc -p tsconfig.json --noEmit --skipLibCheck 2>&1 | tail -3</automated>
  </verify>
  <done>Commerce five-dimension audit complete. Each dimension has status. Integration Health is YELLOW due to flash-sale dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-satellite dependency verification (Commerce - flash-sale)</name>
  <files>.planning/phases/05-satellite-verification/05-03-SUMMARY.md</files>
  <action>
Investigate the Commerce-to-flash-sale dependency — this is the only cross-satellite direct dependency (violating satellite isolation principle per D-01 scope).

**flash-sale Investigation:**
1. Check if flash-sale source exists: `ls /Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/flash-sale/`
2. If exists, run its tests: `cd /Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/flash-sale && bun test`
3. Check for TD-06 (GravitoEngineAdapter missing): `grep -r "GravitoEngineAdapter" /Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/flash-sale/`
4. Read flash-sale's package.json for its dependency tree

**Dependency Coupling Analysis:**
1. In Commerce src/, find all references to flash-sale: `grep -rn "flash-sale\|flash_sale\|FlashSale" /Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/commerce/src/`
2. Determine coupling type:
   - Direct import (tight coupling — satellite isolation violation)
   - Event-based communication (loose coupling — acceptable)
   - Type-only import (moderate coupling — acceptable for shared types)
3. If direct import found, document what is imported and suggest migration to event-based pattern

**Impact Assessment:**
- Does flash-sale's TD-06 (GravitoEngineAdapter) affect Commerce functionality?
- If flash-sale tests fail, does Commerce still pass?
- What is the risk level if flash-sale is not maintained?

**Document in SUMMARY:**
- Cross-satellite dependency map
- Coupling type classification
- Risk assessment for Phase 5B
- Recommendation: refactor now (Phase 5) or defer (Phase 5B)?

Per D-03 (Hono Compatibility Decision): assess but defer implementation. Document findings only; no refactoring in this phase.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/commerce && bun test 2>&1 | tail -5</automated>
  </verify>
  <done>Cross-satellite dependency fully analyzed. Commerce-flash-sale coupling type documented. TD-06 impact assessed. Phase 5B recommendation included in SUMMARY.</done>
</task>

</tasks>

<verification>
- Commerce test suite: 71/71 pass (0 fail)
- TypeCheck: 0 errors
- Five-dimension audit documented
- Cross-satellite dependency (Commerce -> flash-sale) fully analyzed
- TD-02, TD-03, TD-05, TD-06 documented with severity and recommendations
</verification>

<success_criteria>
- Commerce test baseline confirmed (71 pass, 0 fail)
- All five dimensions evaluated with status recorded
- flash-sale dependency coupling type identified (direct import vs event-based)
- TD-06 (GravitoEngineAdapter) impact on Commerce assessed
- Phase 5B readiness recommendation documented
</success_criteria>

<output>
After completion, create `.planning/phases/05-satellite-verification/05-03-SUMMARY.md`
</output>
