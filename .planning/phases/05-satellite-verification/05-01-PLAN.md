---
phase: 05-satellite-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/rbac/tests/Interface/RbacServiceProvider.test.ts
  - .planning/phases/05-satellite-verification/05-01-SUMMARY.md
autonomous: true
requirements: [SAT-RBAC, SAT-D02-RBAC]

must_haves:
  truths:
    - "RBAC 110 tests all pass (0 fail)"
    - "RBAC five-dimension audit completed with GREEN or documented YELLOW status"
    - "RBAC ServiceProvider test fix is a test-only change, no production code modified"
  artifacts:
    - path: "/Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/rbac/tests/Interface/RbacServiceProvider.test.ts"
      provides: "Fixed test file passing container to register()"
      contains: "provider.register(core.container)"
    - path: ".planning/phases/05-satellite-verification/05-01-SUMMARY.md"
      provides: "RBAC five-dimension audit report"
  key_links:
    - from: "RbacServiceProvider.test.ts"
      to: "RbacServiceProvider.register(container)"
      via: "test calling register with container argument"
      pattern: "register\\(.*container"
---

<objective>
Fix RBAC 6 failing tests and complete RBAC five-dimension audit

Purpose: RBAC is the highest-priority satellite because it has 6 failing tests blocking a clean baseline. Fixing these (test-only, 1-line change per call site) and then auditing all five dimensions (per D-02) establishes RBAC's health status.

Output: RBAC at 100% test pass rate + five-dimension audit report in SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-satellite-verification/05-CONTEXT.md
@.planning/phases/05-satellite-verification/05-RESEARCH.md

CRITICAL: Satellite source code is NOT in gravito-core. It is at:
/Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/rbac/

All test commands must run in that directory.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix RBAC ServiceProvider test failures (TD-01)</name>
  <files>/Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/rbac/tests/Interface/RbacServiceProvider.test.ts</files>
  <action>
Fix the 6 failing tests in RbacServiceProvider.test.ts. The root cause (confirmed in RESEARCH.md) is that test calls `provider.register()` without passing the `container` argument, but the production `register(container: any)` method uses `container.singleton()`.

Steps:
1. Read `/Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/rbac/tests/Interface/RbacServiceProvider.test.ts`
2. Find all calls to `provider.register()` that do NOT pass a container argument
3. Change each to `provider.register(core.container)` (where `core` is the mock core object created by `createMockCore()`)
4. If `boot()` tests also fail after this fix, check if boot() needs similar argument passing
5. Do NOT modify any production source code — this is a test-only fix (per D-04: validation approach)

Expected error pattern: `undefined is not an object (evaluating 'container.singleton')`
Expected fix: Pass `core.container` (or the mock container from createMockCore) to `provider.register()`
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/rbac && bun test</automated>
  </verify>
  <done>RBAC test suite: 110 tests, 110 pass, 0 fail. All previously failing RbacServiceProvider tests now pass.</done>
</task>

<task type="auto">
  <name>Task 2: RBAC five-dimension audit</name>
  <files>.planning/phases/05-satellite-verification/05-01-SUMMARY.md</files>
  <action>
Perform a comprehensive five-dimension audit of the RBAC satellite (per D-02). Work in the satellite source directory: `/Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/rbac/`

**Dimension 1: Test Coverage**
- Run `bun test` and record total/pass/fail/skip counts
- List test file distribution across layers (Application, Domain, Infrastructure, Interface)
- Note any untested critical paths (e.g., middleware authorization flows)

**Dimension 2: Integration Health**
- Check for circular dependencies: `grep -r "from '@gravito/satellite-" src/`
- Verify all @gravito/* imports resolve correctly: `grep -r "from '@gravito/" src/ | sort -u`
- Confirm no implicit dependencies (all used packages declared in package.json)

**Dimension 3: Type Safety**
- Run `bun tsc -p tsconfig.json --noEmit --skipLibCheck` and record result
- Search for `as any` usage: `grep -rn "as any" src/`
- Search for `@ts-ignore` or `@ts-expect-error`: `grep -rn "@ts-ignore\|@ts-expect-error" src/`
- Note the `register(container: any)` type weakness (TD-05)

**Dimension 4: API Stability**
- Read `src/index.ts` to catalog public API exports
- Check version in package.json (0.1.0 — note it's pre-0.2.0)
- Verify no deprecated exports or breaking change warnings

**Dimension 5: Hono Readiness**
- Confirm zero Hono imports: `grep -r "from 'hono\|from \"hono" src/`
- Confirm zero photon imports: `grep -r "from '@gravito/photon" src/`
- Confirm GravitoContext usage pattern: `grep -r "GravitoContext" src/`
- Status should be GREEN (confirmed in RESEARCH.md)

Record all findings in the SUMMARY with a status per dimension (GREEN/YELLOW/RED).
Include technical debt items discovered (reference TD-01 through TD-08 catalog from RESEARCH.md).
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-dev-env/gravito-satellites/rbac && bun test 2>&1 | tail -5 && bun tsc -p tsconfig.json --noEmit --skipLibCheck 2>&1 | tail -3</automated>
  </verify>
  <done>RBAC five-dimension audit complete. Each dimension has GREEN/YELLOW/RED status documented. Technical debt items cataloged. SUMMARY file written.</done>
</task>

</tasks>

<verification>
- RBAC test suite: 110/110 pass (0 fail)
- TypeCheck: 0 errors
- No production code modified (test-only fix)
- Five-dimension audit documented in SUMMARY
</verification>

<success_criteria>
- RBAC 6 previously-failing tests now pass
- All five dimensions evaluated with status recorded
- Technical debt items (TD-01, TD-05) documented with severity
- Health baseline maintained (no regressions introduced)
</success_criteria>

<output>
After completion, create `.planning/phases/05-satellite-verification/05-01-SUMMARY.md`
</output>
