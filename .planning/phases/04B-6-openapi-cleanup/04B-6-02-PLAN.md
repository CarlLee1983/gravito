---
phase: 04B-6-openapi-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["04B-6-01"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/04B-hono-migration-pending/MIGRATION_ROADMAP.md
autonomous: true
requirements:
  - REQ-04-HONO-MIGRATION-PHASE-4B
  - REQ-OPENAPI-DEPRECATION
  - REQ-FINAL-HONO-CLEANUP

must_haves:
  truths:
    - "All sub-path imports work: /jwt, /client, /openapi, /adapters/*"
    - "All 21 downstream packages import from photon without issues"
    - "Test pass rate >= 99.7% (Phase 4A baseline)"
    - "TypeScript: 0 errors across 83 packages"
    - "Health baseline >= 93/100"
    - "ROADMAP.md reflects Phase 4B-6 completion"
    - "STATE.md updated with Phase 4B-6 results"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4B-6 completion status"
      contains: "COMPLETE"
    - path: ".planning/STATE.md"
      provides: "Updated project state with Phase 4B-6 results"
      contains: "04B-6"
  key_links:
    - from: "packages/photon/package.json"
      to: "21 downstream packages"
      via: "workspace dependency resolution"
      pattern: "@gravito/photon"
---

<objective>
Full framework verification after Phase 4B-6 source changes. Validate all sub-path imports, run full test suite, confirm TypeScript health across 83 packages, and update project documentation to mark Phase 4B Hono migration as COMPLETE.

Purpose: Prove the Hono migration is fully complete with no regressions.
Output: Verified health baseline, updated ROADMAP.md and STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04B-6-openapi-cleanup/04B-6-CONTEXT.md
@.planning/phases/04B-6-openapi-cleanup/04B-6-01-SUMMARY.md

@packages/photon/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full framework verification — tests, typecheck, sub-path imports</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/phases/04B-6-openapi-cleanup/04B-6-01-SUMMARY.md (what was changed in Plan 01)
    - packages/photon/package.json (verify final state of exports and dependencies)
  </read_first>
  <action>
    **Step 1 — Run full TypeScript typecheck across all 83 packages:**
    ```bash
    bun run typecheck
    ```
    Expected: 0 errors, 83/83 packages pass.

    **Step 2 — Run full test suite:**
    ```bash
    bun test --timeout=10000
    ```
    Expected: >= 11,666 pass (99.7% baseline from Phase 4A). Record exact pass/fail/skip counts.

    **Step 3 — Verify all sub-path imports work:**
    Run a verification script that tests each sub-path import:
    ```bash
    cd packages/photon
    # Verify openapi sub-path (new)
    bun -e "const m = require('./dist/openapi.js'); console.log('openapi:', Object.keys(m))"
    # Verify jwt sub-path
    bun -e "const m = require('./dist/jwt.js'); console.log('jwt:', Object.keys(m))"
    # Verify client sub-path
    bun -e "const m = require('./dist/client.js'); console.log('client:', Object.keys(m))"
    # Verify adapter sub-paths
    bun -e "const m = require('./dist/adapter/cloudflare.js'); console.log('cloudflare:', Object.keys(m))"
    bun -e "const m = require('./dist/adapter/deno.js'); console.log('deno:', Object.keys(m))"
    bun -e "const m = require('./dist/adapter/vercel.js'); console.log('vercel:', Object.keys(m))"
    ```
    Note: If dist/ files don't exist, run `bun run build` first to generate them, then verify.

    **Step 4 — Verify bun sub-path is gone:**
    ```bash
    test ! -f packages/photon/dist/bun.js && echo "bun.js removed from dist" || echo "FAIL: bun.js still exists"
    ```
    Note: If dist/bun.js still exists from a prior build, it's OK as long as package.json exports no longer reference it. The next `bun run build` will not regenerate it since src/bun.ts is gone and build.ts no longer includes it.

    **Step 5 — Verify hono not in dependencies:**
    ```bash
    node -e "const p=require('./packages/photon/package.json'); const hasDep=p.dependencies?.hono; const hasPeer=p.peerDependencies?.hono; console.log('hono in deps:', hasDep, 'hono in peer:', hasPeer); if(!hasDep && !hasPeer) console.log('PASS'); else console.log('FAIL')"
    ```

    **Step 6 — Calculate health score:**
    Using the formula: health = base(70) + test_bonus(max 15) + typecheck_bonus(max 10) + stability_bonus(max 5)
    - test_bonus: 15 if pass_rate >= 99.5%, 10 if >= 98%, 5 if >= 95%
    - typecheck_bonus: 10 if 0 errors
    - stability_bonus: 5 if no circular deps and no implicit deps
    Target: >= 93/100 (maintain Phase 4A baseline)
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && bun run typecheck 2>&1 | tail -3 && echo "---" && cd packages/photon && bun test --timeout=10000 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TypeCheck: 0 errors (83/83 packages pass)
    - Test pass rate >= 99.7% (>= 11,666 pass out of ~11,706 total)
    - All 6 sub-path imports resolve successfully (/jwt, /client, /openapi, /adapters/cloudflare, /adapters/deno, /adapters/vercel)
    - hono not in photon dependencies or peerDependencies
    - Health score >= 93/100
  </acceptance_criteria>
  <done>Full framework verification passed: TypeCheck 0 errors, test pass rate >= 99.7%, all sub-paths work, health >= 93/100</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md, STATE.md, and MIGRATION_ROADMAP.md to mark Phase 4B complete</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/04B-hono-migration-pending/MIGRATION_ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current roadmap — Phase 4B-6 not yet marked complete)
    - .planning/STATE.md (current state — needs Phase 4B-6 entry)
    - .planning/phases/04B-hono-migration-pending/MIGRATION_ROADMAP.md (status field at line 6)
  </read_first>
  <action>
    **Step 1 — Update ROADMAP.md:**
    Add a new Phase 4B-6 section after the Phase 4B-5 entry (after line 533). Include:

    ```markdown
    ### Phase 4B-6: OpenAPI Scoping and Final Cleanup (COMPLETE)

    **Status:** COMPLETE -- {date}

    **Goal:** Scope @hono/zod-openapi to /openapi sub-path, remove bun.ts, remove hono from dependencies

    **Plans:** 2 plans created
    - [x] 04B-6-01-PLAN.md -- OpenAPI scoping + bun.ts removal + dependency cleanup
    - [x] 04B-6-02-PLAN.md -- Full verification and health check

    **Decisions Locked:**
    - **D-01 (Phase 04B-6):** Remove bun.ts entirely from photon (LOCKED, COMPLETE)
    - **D-02 (Phase 04B-6):** Scope @hono/zod-openapi to /openapi sub-path with @deprecated v2.0 (LOCKED, COMPLETE)
    - **D-03 (Phase 04B-6):** Remove hono from dependencies and peerDependencies (LOCKED, COMPLETE)

    **Verification:** TypeCheck 0 errors, test pass rate >= 99.7%, health >= 93/100

    **Baseline:** Health {score}/100 | {pass_rate}% test pass | 0 TypeScript errors

    --- HONO MIGRATION COMPLETE ---
    Phase 4B (4B-1 through 4B-6) successfully completed. All 12 photon compat shims addressed. hono removed from main dependencies.
    ```

    Also update the plans list to show completion checkmarks for Phase 4B-6.

    **Step 2 — Update STATE.md:**
    Add a Phase 4B-6 completion entry in the Current Phase Status section, following the existing format from Phase 4B-5. Include:
    - Completion date
    - Plans completed (2/2)
    - Key changes (bun.ts removed, openapi scoped, hono removed from deps)
    - Verification results (TypeCheck, test count, health score)
    - Commits
    - Summary file path
    - Status: Phase 4B HONO MIGRATION COMPLETE

    Update `Current Focus` to reflect migration completion.

    **Step 3 — Update MIGRATION_ROADMAP.md status:**
    Change line 6 from `**Status:** Planning complete -- ready for Phase 4B-1 execution` to:
    `**Status:** COMPLETE -- All 6 sub-phases (4B-1 through 4B-6) executed successfully`

    Also update the Success Criteria section (line 758+) to check off all items.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && grep -q "4B-6.*COMPLETE" .planning/ROADMAP.md && grep -q "04B-6" .planning/STATE.md && grep -q "COMPLETE" .planning/phases/04B-hono-migration-pending/MIGRATION_ROADMAP.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md contains Phase 4B-6 entry marked COMPLETE
    - ROADMAP.md contains "HONO MIGRATION COMPLETE" marker
    - STATE.md contains Phase 4B-6 completion entry with verification results
    - MIGRATION_ROADMAP.md status updated to COMPLETE
    - All verification numbers (test count, health score) reflect actual Task 1 results
  </acceptance_criteria>
  <done>ROADMAP.md, STATE.md, and MIGRATION_ROADMAP.md updated to reflect Phase 4B-6 and full Hono migration completion</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Full framework health verified (TypeCheck, tests, sub-paths)
2. Health score >= 93/100 confirmed
3. All project documentation updated to reflect Hono migration completion
4. No regressions from Plan 01 changes
</verification>

<success_criteria>
- TypeCheck: 0 errors (83/83 packages)
- Test pass rate >= 99.7%
- Health score >= 93/100
- All sub-path imports verified working
- ROADMAP.md, STATE.md, MIGRATION_ROADMAP.md updated with completion status
- Phase 4B Hono migration marked COMPLETE
</success_criteria>

<output>
After completion, create `.planning/phases/04B-6-openapi-cleanup/04B-6-02-SUMMARY.md`
</output>
