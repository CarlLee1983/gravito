---
phase: 04B-3-external-package-type-cleanup
plan: 01
type: execute
wave: 1
depends_on: ["04B-2-01"]
files_modified: ["packages/mass/src/coercion.ts", "packages/mass/tests/coercion.test.ts"]
autonomous: true
requirements: ["HONO-CLEANUP-03"]

must_haves:
  truths:
    - "HonoContext import removed from mass/coercion.ts"
    - "GravitoContext type used for context handling"
    - "All mass validation coercion tests pass"
    - "Zero TypeScript errors in mass package"
  artifacts:
    - path: "packages/mass/src/coercion.ts"
      provides: "Type-safe coercion without Hono dependency"
      min_lines: 370
    - path: "packages/mass/tests/coercion.test.ts"
      provides: "Validation test coverage for coercion functions"
      min_lines: 80
  key_links:
    - from: "packages/mass/src/coercion.ts"
      to: "@gravito/core"
      via: "GravitoContext type import"
      pattern: "import.*GravitoContext.*from.*@gravito/core"
    - from: "packages/mass/src/coercion.ts"
      to: "validateWithCoercion function"
      via: "Context handling in middleware"
      pattern: "const.*ctx.*as GravitoContext"
---

<objective>
Replace type-only HonoContext import with GravitoContext in @gravito/mass package.

Purpose: Eliminate Hono type dependency from mass/coercion.ts, completing external package type cleanup per Phase 4B-3 scope.

Output: mass/coercion.ts with GravitoContext handling, all tests passing, zero TypeScript errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04B-3-external-package-type-cleanup/04B-3-CONTEXT.md
@.planning/phases/04B-hono-migration-pending/MIGRATION_ROADMAP.md
@.planning/phases/04B-2-jwt-native-implementation/04B-2-01-SUMMARY.md

From phase context (04B-3-CONTEXT.md):
- Decision D-01 LOCKED: Replace HonoContext with GravitoContext in mass/coercion.ts
- Current usage: Line 5 imports `Context as HonoContext` from `hono`; Line 304 casts `(ctx.native || ctx) as HonoContext`
- Target: Use `GravitoContext` type directly, remove Hono import entirely
- Rationale: coercion logic only needs Gravito's context abstraction, not Hono-specific types
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace HonoContext type import with GravitoContext</name>
  <files>packages/mass/src/coercion.ts</files>
  <action>
    1. Open packages/mass/src/coercion.ts
    2. Remove line 5: `import type { Context as HonoContext } from 'hono'`
    3. Verify line 1 imports GravitoContext and GravitoMiddleware from @gravito/core (should already be there)
    4. Locate validateWithCoercion function (around line 297-375)
    5. Find line 304: `const honoCtx = (ctx.native || ctx) as HonoContext`
    6. Replace with: `const nativeCtx = ctx as GravitoContext`
    7. Replace all references to `honoCtx` in the function with `nativeCtx` (should be ~12 usages):
       - Line 310: `honoCtx.req.json()` → `nativeCtx.req.json()`
       - Line 313: `honoCtx.json(...)` → `nativeCtx.json(...)`
       - Line 318: `honoCtx.req.query()` → `nativeCtx.req.query()`
       - Line 322: `honoCtx.req.param()` → `nativeCtx.req.param()`
       - Line 327: `honoCtx.req.parseBody()` → `nativeCtx.req.parseBody()`
       - Line 330: `honoCtx.json(...)` → `nativeCtx.json(...)`
       - Line 350: `honoCtx.json(...)` → `nativeCtx.json(...)`
       - Line 357: `(honoCtx.req as any).valid` → `(nativeCtx.req as any).valid`
    8. Verify no remaining references to HonoContext in the file
    9. Save file
    10. Run: `bun run typecheck -- packages/mass` to verify zero TypeScript errors in mass package
    11. Commit with message: `fix: [mass] Replace HonoContext with GravitoContext type`
  </action>
  <verify>
    <automated>
      bun run typecheck -- packages/mass 2>&1 | grep -E "error|Error" && echo "FAIL: TypeScript errors found" || echo "PASS: Zero TypeScript errors"
    </automated>
    <automated>
      grep -n "HonoContext\|hono" packages/mass/src/coercion.ts && echo "FAIL: Hono import still present" || echo "PASS: Hono import removed"
    </automated>
  </verify>
  <done>
    HonoContext import completely removed from mass/coercion.ts. All references replaced with GravitoContext. TypeScript check passes for mass package (zero errors).
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify mass coercion tests pass with GravitoContext</name>
  <files>packages/mass/tests/coercion.test.ts</files>
  <action>
    1. Run full mass test suite: `bun test packages/mass --timeout=10000`
    2. If tests fail:
       a. Check error messages for GravitoContext compatibility issues
       b. Verify GravitoContext provides .req.json(), .req.query(), .req.param(), .req.parseBody() methods
       c. If method signatures differ, adjust coercion.ts accordingly (rare - GravitoContext is a superset)
    3. If all tests pass (expected):
       a. Note test count and pass rate
       b. Verify no new failures introduced
    4. Run: `bun run typecheck` (full monorepo) to confirm no downstream type errors
    5. Commit with message: `test: [mass] Verify coercion tests pass with GravitoContext`
  </action>
  <verify>
    <automated>
      bun test packages/mass --timeout=10000 2>&1 | tail -20 | grep -E "pass|fail" | head -1
    </automated>
    <automated>
      bun run typecheck 2>&1 | grep -c "error" || echo "0"
    </automated>
  </verify>
  <done>
    All mass coercion tests pass. Full monorepo typecheck passes with zero errors. GravitoContext successfully replaces HonoContext in validateWithCoercion middleware.
  </done>
</task>

</tasks>

<verification>
1. Verify HonoContext import removed:
   - `grep "HonoContext" packages/mass/src/coercion.ts` returns nothing
   - `grep "from 'hono'" packages/mass/src/coercion.ts` returns nothing

2. Verify GravitoContext type used:
   - `grep "as GravitoContext" packages/mass/src/coercion.ts` shows line ~304
   - Context variable named `nativeCtx` or similar

3. Verify all tests pass:
   - `bun test packages/mass` shows 100% pass rate (baseline: 40+ tests)
   - No new failures introduced

4. Verify downstream impact:
   - `bun run typecheck` passes with 0 errors across all 83 packages
   - No downstream packages affected by mass type change
</verification>

<success_criteria>
- [ ] HonoContext type import removed from packages/mass/src/coercion.ts
- [ ] All references to HonoContext replaced with GravitoContext
- [ ] Variable `honoCtx` renamed to `nativeCtx` throughout validateWithCoercion function
- [ ] All mass tests pass (100% success rate)
- [ ] TypeScript typecheck passes (0 errors in mass package, 0 errors monorepo-wide)
- [ ] No new type errors introduced in downstream 21 packages that depend on @gravito/photon
- [ ] Changes committed with clear message referencing D-01 decision

</success_criteria>

<output>
After completion, create `.planning/phases/04B-3-external-package-type-cleanup/04B-3-01-SUMMARY.md`

Summary should include:
- Execution time
- Test count and pass rate
- TypeScript error count pre/post
- Commits created
- Any issues encountered and how resolved
</output>
