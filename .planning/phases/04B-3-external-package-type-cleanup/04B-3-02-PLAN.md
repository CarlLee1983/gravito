---
phase: 04B-3-external-package-type-cleanup
plan: 02
type: execute
wave: 1
depends_on: ["04B-2-01"]
files_modified: ["packages/beam/src/index.ts", "packages/beam/src/helpers.ts"]
autonomous: true
requirements: ["HONO-CLEANUP-03"]

must_haves:
  truths:
    - "beam package uses Hono type for RPC type inference"
    - "hono/client kept as type-only peerDependency"
    - "Deprecation notice documented in JSDoc"
    - "All beam tests pass"
    - "Zero TypeScript errors in beam package"
  artifacts:
    - path: "packages/beam/src/index.ts"
      provides: "createBeam function with Hono type generic"
      contains: "export function createBeam<T extends Hono"
    - path: "packages/beam/src/helpers.ts"
      provides: "RPC helper functions with Hono type constraints"
      contains: "export function createAuthenticatedBeam<T extends Hono"
  key_links:
    - from: "packages/beam/src/index.ts"
      to: "@gravito/photon/client"
      via: "hc import from photon client re-export"
      pattern: "import.*hc.*from.*@gravito/photon/client"
    - from: "packages/beam/src/index.ts"
      to: "hono"
      via: "Type-only generic constraint"
      pattern: "type Hono.*from.*hono"
---

<objective>
Assess and document @gravito/beam's RPC type system based on Hono, implementing strategic decision D-02.

Purpose: Keep hono/client as type-only peerDependency in @gravito/photon per MIGRATION_ROADMAP, document deprecation targeting v3.0 native RPC system.

Output: beam/index.ts and beam/helpers.ts with deprecation JSDoc notices, all tests passing, zero TypeScript errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04B-3-external-package-type-cleanup/04B-3-CONTEXT.md
@.planning/phases/04B-hono-migration-pending/MIGRATION_ROADMAP.md

From phase context (04B-3-CONTEXT.md):
- Decision D-02 LOCKED: Keep hono/client as type-only peerDependency
- Current usage: beam/src/index.ts and beam/src/helpers.ts use `type Hono` for generic RPC client typing
- Target: Document deprecation targeting v3.0 native RPC type system replacement
- Rationale: Beam's `createBeam<T extends Hono<any, any, any>>` is tightly coupled to Hono's App type for type inference. Native RPC system deferred to Phase 4B-5 or v3.0+
- Implementation: Add @deprecated JSDoc to createBeam and createAuthenticatedBeam functions
- Strategy: Keep hono/client optional peerDependency in @gravito/photon/package.json; no code changes needed (type-only)

From MIGRATION_ROADMAP.md (Phase 4B-5):
- RPC Client Strategy: Keep hono/client as type-only peerDependency through v1.x and v2.x
- Deprecation target: v3.0 (deferred native RPC type system)
- Risk: MEDIUM if Hono import removed without replacement; mitigation: keep as peer dep
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review beam RPC type system and document Hono usage</name>
  <files>packages/beam/src/index.ts, packages/beam/src/helpers.ts</files>
  <action>
    1. Open packages/beam/src/index.ts (line 1-75)
    2. Identify Hono imports:
       - Line 2: `import type { Hono } from 'hono'` (type-only, confirms D-02)
       - Line 1: `import { hc as beamClient } from '@gravito/photon/client'` (Hono types come via photon re-export)
    3. Confirm createBeam generic constraint (line 49): `<T extends Hono<any, any, any> = Hono<any, any, any>>`
       - This is the RPC type inference mechanism that requires Hono's App type
    4. Open packages/beam/src/helpers.ts (line 1-56)
    5. Identify Hono imports:
       - Line 1: `import type { Hono } from 'hono'` (type-only)
    6. Confirm createAuthenticatedBeam generic constraint (line 45): `<T extends Hono<any, any, any>>`
    7. Document findings:
       - Beam MUST import `type Hono` for generic constraints in both index.ts and helpers.ts
       - No runtime Hono code used (purely type-level constraints)
       - Hono is imported via @gravito/photon/client which re-exports hc() from hono/client
       - This is expected and correct per D-02 (keep as type-only peerDependency)
    8. No changes needed to beam source code at this stage — types are already type-only
    9. Commit (if any changes): `docs: [beam] Document RPC type system Hono dependency`
  </action>
  <verify>
    <automated>
      grep -n "import type { Hono }" packages/beam/src/index.ts packages/beam/src/helpers.ts && echo "PASS: Hono imported as type-only" || echo "FAIL: Type-only import missing"
    </automated>
    <automated>
      grep -n "extends Hono<" packages/beam/src/index.ts packages/beam/src/helpers.ts && echo "PASS: Hono generic constraints present" || echo "FAIL: Generic constraints missing"
    </automated>
  </verify>
  <done>
    Beam RPC type system assessment complete. Hono type imports confirmed as type-only. Both createBeam and createAuthenticatedBeam use Hono generics for type inference. No source code changes needed (types already correct).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add @deprecated JSDoc notices to beam RPC functions targeting v3.0</name>
  <files>packages/beam/src/index.ts, packages/beam/src/helpers.ts</files>
  <action>
    1. Open packages/beam/src/index.ts
    2. Locate createBeam function documentation (lines 15-48)
    3. Add @deprecated JSDoc after existing JSDoc block (before @example):
       ```
       * @deprecated v3.0 — Beam's RPC type system will migrate to native implementation.
       * Currently uses Hono's type inference. For v3.0, a native RPC type system will replace this dependency.
       * See https://github.com/gravito/gravito-core/issues/rpc-native-system for roadmap.
       ```
    4. Find createGravitoClient alias (line 178):
       - Already has deprecation notice (good)
       - No changes needed
    5. Open packages/beam/src/helpers.ts
    6. Locate createAuthenticatedBeam function (lines 6-44)
    7. Add @deprecated JSDoc after existing JSDoc block (before @example):
       ```
       * @deprecated v3.0 — Part of Beam's RPC type system.
       * Will be updated to use native RPC types when available in v3.0+.
       ```
    8. Run TypeScript check: `bun run typecheck -- packages/beam`
    9. Verify no TypeScript errors introduced by JSDoc additions
    10. Commit with message: `docs: [beam] Add @deprecated notices targeting v3.0 native RPC system`
  </action>
  <verify>
    <automated>
      grep -A2 "@deprecated" packages/beam/src/index.ts | grep -c "v3.0" && echo "PASS: Deprecation notice added to createBeam" || echo "FAIL: Deprecation notice missing"
    </automated>
    <automated>
      grep -A2 "@deprecated" packages/beam/src/helpers.ts | grep -c "v3.0" && echo "PASS: Deprecation notice added to createAuthenticatedBeam" || echo "FAIL: Deprecation notice missing"
    </automated>
    <automated>
      bun run typecheck -- packages/beam 2>&1 | grep -E "error|Error" && echo "FAIL: TypeScript errors" || echo "PASS: Zero TypeScript errors"
    </automated>
  </verify>
  <done>
    @deprecated JSDoc notices added to createBeam and createAuthenticatedBeam targeting v3.0. TypeScript check passes with zero errors in beam package.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify beam tests pass with Hono type-only dependency</name>
  <files>packages/beam/tests</files>
  <action>
    1. Run beam test suite: `bun test packages/beam --timeout=10000`
    2. If tests fail:
       a. Check error messages for Hono type inference issues
       b. Verify generic type constraints are being resolved correctly
       c. If mocks are involved, ensure Hono types are correctly mocked
    3. If all tests pass (expected):
       a. Note test count and pass rate
       b. Verify no new failures introduced by type updates
    4. Run: `bun run typecheck` (full monorepo) to confirm no downstream type errors
    5. Commit with message: `test: [beam] Verify tests pass with Hono type-only RPC system`
  </action>
  <verify>
    <automated>
      bun test packages/beam --timeout=10000 2>&1 | tail -20 | grep -E "pass|fail" | head -1
    </automated>
    <automated>
      bun run typecheck 2>&1 | grep -c "error" || echo "0"
    </automated>
  </verify>
  <done>
    All beam RPC tests pass. Full monorepo typecheck passes with zero errors. Hono type-only dependency functioning correctly for createBeam and createAuthenticatedBeam generic type inference.
  </done>
</task>

</tasks>

<verification>
1. Verify Hono type-only imports:
   - `grep "import type { Hono }" packages/beam/src/*.ts` shows both index.ts and helpers.ts
   - No runtime Hono imports detected

2. Verify deprecation notices:
   - `grep -B1 -A2 "@deprecated" packages/beam/src/index.ts` shows v3.0 notice on createBeam
   - `grep -B1 -A2 "@deprecated" packages/beam/src/helpers.ts` shows v3.0 notice on createAuthenticatedBeam

3. Verify all tests pass:
   - `bun test packages/beam` shows 100% pass rate (baseline: 40+ tests)
   - No new failures introduced

4. Verify downstream impact:
   - `bun run typecheck` passes with 0 errors across all 83 packages
   - Hono types still correctly inferred through @gravito/photon/client re-export
</verification>

<success_criteria>
- [ ] Hono type-only imports confirmed in both beam/src/index.ts and beam/src/helpers.ts
- [ ] @deprecated JSDoc notices added to createBeam and createAuthenticatedBeam
- [ ] Deprecation notices target v3.0 native RPC system replacement
- [ ] All beam tests pass (100% success rate)
- [ ] TypeScript typecheck passes (0 errors in beam package, 0 errors monorepo-wide)
- [ ] No code changes to beam source (only JSDoc additions)
- [ ] Changes committed with clear message referencing D-02 decision

</success_criteria>

<output>
After completion, create `.planning/phases/04B-3-external-package-type-cleanup/04B-3-02-SUMMARY.md`

Summary should include:
- Execution time
- Assessment findings (Hono usage confirmed as type-only)
- JSDoc notices added
- Test count and pass rate
- TypeScript error count pre/post
- Commits created
- Confirmation that D-02 decision is properly documented
</output>
