---
phase: 04B-3-external-package-type-cleanup
plan: 03
type: execute
wave: 1
depends_on: ["04B-2-01"]
files_modified: ["packages/zenith/package.json"]
autonomous: true
requirements: ["HONO-CLEANUP-03"]

must_haves:
  truths:
    - "Zenith source code audited for Hono imports"
    - "Hono dependency verified as unused in src/"
    - "Decision made: remove Hono from zenith/package.json"
    - "All zenith tests pass"
    - "Zero TypeScript errors in zenith package"
  artifacts:
    - path: "packages/zenith/package.json"
      provides: "Removed hono dependency from production"
      contains: "No hono entry in dependencies"
  key_links:
    - from: "packages/zenith/package.json"
      to: "packages/zenith/src/"
      via: "No actual Hono imports in source"
      pattern: "No grep matches for import.*hono"
---

<objective>
Verify and resolve @gravito/zenith's Hono dependency status per Phase 4B-3 scope.

Purpose: Audit zenith source code to determine if Hono is actually used. If unused, remove from production dependencies. If used, document and plan for Phase 4B-5+.

Output: Hono dependency verified/removed from zenith/package.json, all tests passing, zero TypeScript errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04B-3-external-package-type-cleanup/04B-3-CONTEXT.md
@.planning/phases/04B-hono-migration-pending/MIGRATION_ROADMAP.md

From phase context (04B-3-CONTEXT.md):
- Decision D-03 PENDING: Audit zenith source code to determine Hono usage
- Current state: zenith/package.json has `hono@^4.12.2` in production dependencies
- Task: Grep zenith source for actual Hono imports (hono/, @hono/)
- Outcome: If unused, remove dependency; if used, document usage and plan for Phase 4B-5+

From codebase scan (preliminary):
- zenith/src/ contains no grep matches for "import.*hono" (based on earlier scan)
- This suggests Hono is likely an unused leftover from initialization
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit zenith source code for actual Hono imports</name>
  <files>packages/zenith/src</files>
  <action>
    1. Grep zenith source for all Hono imports:
       `grep -r "from ['\"]hono" packages/zenith/src/ --include="*.ts" --include="*.tsx" 2>/dev/null`
    2. Grep for Hono named imports:
       `grep -r "import.*Hono" packages/zenith/src/ --include="*.ts" --include="*.tsx" 2>/dev/null`
    3. Grep for Hono usage patterns (Context, middleware, etc):
       `grep -r "Hono\|HonoRequest\|HonoContext" packages/zenith/src/ --include="*.ts" --include="*.tsx" 2>/dev/null`
    4. Document findings:
       - If matches found: Record file paths and usage context
       - If no matches: Confirm Hono is unused leftover from initialization
    5. Check if photon is imported (zenith imports @gravito/photon):
       `grep -r "@gravito/photon" packages/zenith/package.json packages/zenith/src/index.ts`
    6. Conclusion:
       - Expected outcome: NO Hono imports in zenith/src/ (based on earlier scan results)
       - This means hono ^4.12.2 in package.json is an unused dependency
  </action>
  <verify>
    <automated>
      grep -r "from ['\"]hono\|from ['\"]@hono" packages/zenith/src/ --include="*.ts" --include="*.tsx" 2>/dev/null | wc -l | xargs bash -c 'test "$1" -eq 0 && echo "PASS: No Hono imports found" || (echo "FAIL: Hono imports found:"; grep -r "from ['\"]hono\|from ['\"]@hono" packages/zenith/src/ --include="*.ts" --include="*.tsx" 2>/dev/null)' _
    </automated>
  </verify>
  <done>
    Zenith source code audited. No Hono imports found in packages/zenith/src/. Hono dependency is unused leftover from initialization and can be safely removed per D-03 decision.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove unused Hono dependency from zenith/package.json</name>
  <files>packages/zenith/package.json</files>
  <action>
    1. Open packages/zenith/package.json
    2. Locate dependencies section (line 32-50)
    3. Find and delete line: `"hono": "^4.12.2",`
    4. Verify no other hono-related entries (@hono/* packages)
    5. Verify photon dependency remains (expected: `"@gravito/photon": "^1.1.3"`)
    6. Save file
    7. Run: `bun install` to update lockfile
    8. Verify no installation errors (expected: clean)
    9. Run: `bun run typecheck -- packages/zenith` to verify zero errors
    10. Commit with message: `chore: [zenith] Remove unused hono dependency`
  </action>
  <verify>
    <automated>
      grep -i "hono" packages/zenith/package.json && echo "FAIL: Hono still in package.json" || echo "PASS: Hono removed"
    </automated>
    <automated>
      bun install 2>&1 | grep -i "error\|ERR" && echo "FAIL: Installation error" || echo "PASS: Installation clean"
    </automated>
    <automated>
      bun run typecheck -- packages/zenith 2>&1 | grep -E "error|Error" && echo "FAIL: TypeScript errors" || echo "PASS: Zero TypeScript errors"
    </automated>
  </verify>
  <done>
    Hono dependency removed from packages/zenith/package.json. Installation completed cleanly. TypeScript check passes with zero errors in zenith package.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify zenith tests pass after Hono dependency removal</name>
  <files>packages/zenith/tests</files>
  <action>
    1. Run zenith test suite: `bun test packages/zenith --timeout=10000`
    2. If tests fail:
       a. Check error messages for missing Hono imports (should not occur based on audit)
       b. Verify dependencies resolved correctly after `bun install`
       c. If failures persist, investigate root cause (likely unrelated to Hono removal)
    3. If all tests pass (expected):
       a. Note test count and pass rate
       b. Verify no new failures introduced by dependency removal
    4. Run: `bun run typecheck` (full monorepo) to confirm no downstream type errors
    5. Commit with message: `test: [zenith] Verify tests pass after Hono removal`
  </action>
  <verify>
    <automated>
      bun test packages/zenith --timeout=10000 2>&1 | tail -20 | grep -E "pass|fail" | head -1
    </automated>
    <automated>
      bun run typecheck 2>&1 | grep -c "error" || echo "0"
    </automated>
  </verify>
  <done>
    All zenith tests pass. Full monorepo typecheck passes with zero errors. Hono dependency successfully removed with no impact on zenith functionality.
  </done>
</task>

</tasks>

<verification>
1. Verify Hono audit completed:
   - No Hono imports found in packages/zenith/src/ confirmed
   - Documented that dependency was unused leftover

2. Verify Hono dependency removed:
   - `grep "hono" packages/zenith/package.json` returns nothing
   - `bun.lock` updated (no hono entries for zenith)

3. Verify all tests pass:
   - `bun test packages/zenith` shows 100% pass rate (baseline: varies)
   - No new failures introduced

4. Verify downstream impact:
   - `bun run typecheck` passes with 0 errors across all 83 packages
   - No packages depend on zenith's Hono re-export (none exists)
</verification>

<success_criteria>
- [ ] Zenith source code audited for Hono imports (none found)
- [ ] Hono dependency removed from packages/zenith/package.json
- [ ] bun install completes cleanly with updated lockfile
- [ ] All zenith tests pass (100% success rate)
- [ ] TypeScript typecheck passes (0 errors in zenith package, 0 errors monorepo-wide)
- [ ] No downstream packages affected by Hono removal
- [ ] Changes committed with clear message referencing D-03 decision

</success_criteria>

<output>
After completion, create `.planning/phases/04B-3-external-package-type-cleanup/04B-3-03-SUMMARY.md`

Summary should include:
- Execution time
- Audit findings (Hono confirmed as unused)
- Dependency removal action taken
- Test count and pass rate
- TypeScript error count pre/post
- Commits created
- Confirmation that D-03 decision is complete
</output>
