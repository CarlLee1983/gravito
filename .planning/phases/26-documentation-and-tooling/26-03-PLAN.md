---
phase: 26-documentation-and-tooling
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - turbo.json
  - package.json
autonomous: true
requirements: [DOC-03]

must_haves:
  truths:
    - "Running `turbo run publint` validates all packages with exports maps"
    - "publint fails the pipeline if any package has invalid exports"
    - "publint runs after build (dependsOn build)"
  artifacts:
    - path: "turbo.json"
      provides: "publint pipeline task with dependsOn build"
      contains: "publint"
    - path: "package.json"
      provides: "publint devDependency at root"
      contains: "publint"
  key_links:
    - from: "turbo.json publint task"
      to: "packages/*/package.json publint script"
      via: "turbo discovers per-package publint scripts"
      pattern: "publint"
---

<objective>
Install publint, add it to the Turbo pipeline after build, and add a `"publint"` script to every package that has an `exports` map in its package.json (57/59 packages per D-06).

Purpose: CI gate ensuring package.json exports maps are valid and match dist output (DOC-03).
Output: publint installed, turbo.json updated, per-package scripts added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-documentation-and-tooling/26-CONTEXT.md
@.planning/phases/26-documentation-and-tooling/26-RESEARCH.md

@turbo.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install publint and add Turbo pipeline task</name>
  <files>turbo.json, package.json</files>
  <read_first>
    - turbo.json (full file — existing tasks structure, especially "build" task at line 7)
    - package.json (root — devDependencies section)
  </read_first>
  <action>
**Step 1: Install publint (per D-06):**
```bash
bun add -D publint
```

**Step 2: Add publint task to turbo.json (per D-06):**
Add a new `"publint"` entry in the `"tasks"` object of turbo.json:
```json
"publint": {
  "dependsOn": ["build"],
  "inputs": ["dist/**", "package.json"],
  "outputs": [],
  "cache": false
}
```

Place it after the existing `"build:dts"` task (around line 23) to keep pipeline tasks grouped logically. `cache: false` because publint reads dist/ which is already cached by build. `dependsOn: ["build"]` ensures publint only runs after build completes.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && grep '"publint"' turbo.json && grep 'publint' package.json | head -1</automated>
  </verify>
  <acceptance_criteria>
    - turbo.json contains a `"publint"` task with `"dependsOn": ["build"]`
    - turbo.json publint task has `"cache": false`
    - package.json devDependencies contains `"publint"`
    - `bun run --filter @gravito/core publint` exits without "command not found" (publint binary available)
  </acceptance_criteria>
  <done>publint installed as devDependency; turbo.json has publint pipeline task depending on build</done>
</task>

<task type="auto">
  <name>Task 2: Add publint script to all packages with exports maps</name>
  <files>packages/*/package.json (57 packages with exports maps)</files>
  <read_first>
    - packages/core/package.json (example package with exports map — check structure)
    - packages/atlas/package.json (second example to confirm pattern)
  </read_first>
  <action>
**Per D-07:** Each package with an `"exports"` field in its package.json gets a `"publint": "publint"` script entry.

**Approach:**
1. Find all package.json files under packages/ that have an `"exports"` field:
   ```bash
   grep -rl '"exports"' packages/*/package.json
   ```
2. For each matching package.json, add `"publint": "publint"` to the `"scripts"` object.
3. Skip packages without `"exports"` (there should be 2 per D-06 — identify and skip them).

**Script placement:** Add `"publint": "publint"` as the last entry in each `"scripts"` block, or alphabetically if the scripts are already sorted.

**Verification after adding all scripts:**
```bash
turbo run publint --dry-run 2>&1 | grep -c "publint"
```
This should show ~57 packages in the dry run.

Then run actual publint on a few packages to verify it works:
```bash
bun run --filter @gravito/core publint
bun run --filter @gravito/atlas publint
```

If publint reports errors about missing dist files, that's expected if build hasn't been run. The key is that the script exists and publint CLI resolves. Run `bun run build` first if needed, then verify publint passes on at least 3 packages.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && grep -rl '"publint": "publint"' packages/*/package.json | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - At least 55 package.json files under packages/ contain `"publint": "publint"` in scripts
    - Packages WITHOUT exports map do NOT have the publint script
    - `turbo run publint --dry-run` lists the expected number of packages
    - `bun run --filter @gravito/core publint` exits 0 (after build)
  </acceptance_criteria>
  <done>57 packages have publint scripts; turbo run publint validates all package exports maps</done>
</task>

</tasks>

<verification>
1. `grep '"publint"' turbo.json` shows the pipeline task
2. `grep -rl '"publint": "publint"' packages/*/package.json | wc -l` returns ~57
3. `bun run --filter @gravito/core publint` exits 0
4. `turbo run publint --dry-run` shows all expected packages
</verification>

<success_criteria>
- publint installed as devDependency
- turbo.json has publint task with dependsOn build
- 57 packages have publint script
- publint passes on built packages
</success_criteria>

<output>
After completion, create `.planning/phases/26-documentation-and-tooling/26-03-SUMMARY.md`
</output>
