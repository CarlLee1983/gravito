---
phase: 26-documentation-and-tooling
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/events/MessageQueueBridge.ts
  - packages/core/src/events/PriorityEscalationManager.ts
  - packages/core/src/events/RetryScheduler.ts
  - packages/core/src/events/WorkerPool.ts
  - packages/core/src/events/queue-core.ts
  - packages/core/src/events/observability/EventMetrics.ts
  - packages/core/src/events/observability/ObservableHookManager.ts
  - packages/core/src/events/observability/OTelEventMetrics.ts
autonomous: true
requirements: [DOC-01]
gap_closure: true

must_haves:
  truths:
    - "biome lint packages/core/src/events/ --diagnostic-level=error reports 0 noExplicitAny violations"
    - "bun run typecheck passes with zero new errors after all replacements"
  artifacts:
    - path: "packages/core/src/events/MessageQueueBridge.ts"
      provides: "Zero any annotations"
      contains: "options?: unknown"
    - path: "packages/core/src/events/PriorityEscalationManager.ts"
      provides: "Zero any annotations"
      contains: "as string"
    - path: "packages/core/src/events/RetryScheduler.ts"
      provides: "Zero any annotations"
      contains: "as Record<string, unknown>"
    - path: "packages/core/src/events/WorkerPool.ts"
      provides: "Zero any annotations"
      contains: "unknown"
    - path: "packages/core/src/events/queue-core.ts"
      provides: "Zero any annotations"
      contains: "as string"
    - path: "packages/core/src/events/observability/OTelEventMetrics.ts"
      provides: "Zero any annotations"
      contains: "meter.createCounter"
    - path: "packages/core/src/events/observability/ObservableHookManager.ts"
      provides: "Zero any annotations"
      contains: "unknown"
    - path: "packages/core/src/events/observability/EventMetrics.ts"
      provides: "Zero any annotations"
      contains: "registry: unknown"
  key_links:
    - from: "biome.json noExplicitAny override"
      to: "packages/core/src/events/**/*.ts"
      via: "Biome override glob"
      pattern: "packages/core/src/\\*\\*/\\*.ts"
---

<objective>
Fix all 37 noExplicitAny violations in packages/core/src/events/ (8 files) to close the DOC-01 gap.

Purpose: The biome.json override already enforces noExplicitAny as error for packages/core/src/**. These 37 violations in the events/ subtree were missed by Plan 01. Fixing them is required for CI to pass cleanly on packages/core/src/events/.

Output: 8 modified files with zero `any` annotations remaining; biome lint clean on events/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-documentation-and-tooling/26-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix noExplicitAny in events/ core files (5 files, 13 violations)</name>
  <files>
    packages/core/src/events/MessageQueueBridge.ts
    packages/core/src/events/PriorityEscalationManager.ts
    packages/core/src/events/RetryScheduler.ts
    packages/core/src/events/WorkerPool.ts
    packages/core/src/events/queue-core.ts
  </files>
  <read_first>
    packages/core/src/events/MessageQueueBridge.ts
    packages/core/src/events/PriorityEscalationManager.ts
    packages/core/src/events/RetryScheduler.ts
    packages/core/src/events/WorkerPool.ts
    packages/core/src/events/queue-core.ts
  </read_first>
  <action>
Replace `any` with `unknown` or concrete types in these 5 files. Exact fixes per file:

**MessageQueueBridge.ts (2 violations):**
- Line 121: `options?: any` parameter in `dispatchWithQueue` -- change to `options?: Record<string, unknown>` (it's a generic options bag passed through to queue)
- Line 131: `(this.config.hookManager as any).getCircuitBreaker` -- change to `(this.config.hookManager as unknown as { getCircuitBreaker?: (name: string) => { getState?: () => string } }).getCircuitBreaker` or simpler: extract the cast to a local variable with `unknown` intermediate: `const hm = this.config.hookManager as unknown as Record<string, unknown>` then `const breaker = (typeof hm.getCircuitBreaker === 'function') ? (hm.getCircuitBreaker as (name: string) => { getState?: () => string })(eventName) : undefined`. Prefer the simplest approach that satisfies Biome and TypeScript.

**PriorityEscalationManager.ts (4 violations):**
- Line 29: `(task.options.priority as any) || 'normal'` -- change to `(task.options.priority as string) || 'normal'` (priority is a string union)
- Line 48: `(task.options.priority as any) || 'normal'` -- same fix: `(task.options.priority as string) || 'normal'`
- Lines 73-74: `(task1.options.priority as any) || 'normal'` and `(task2.options.priority as any) || 'normal'` -- same fix: cast as `string`

**RetryScheduler.ts (3 violations):**
- Line 172: `(queue as any).add` -- change to `(queue as unknown as Record<string, unknown>).add` then check `typeof addMethod === 'function'` (already done). Alternatively: `const queueRecord = queue as unknown as { add?: (...args: unknown[]) => Promise<unknown> }` and use `queueRecord.add?.call(...)`.
- Line 208: `queue as any` in getStats -- change to `const queueObj = queue as unknown as Record<string, Record<string, number>>` or `queue as unknown as { count?: Record<string, number> }`
- Line 233: `queue as any` in shutdown -- change to `const queueObj = queue as unknown as { close?: () => Promise<void> }`

**WorkerPool.ts (3 violations):**
- Line 49: `private healthCheckTimer?: any` -- change to `private healthCheckTimer?: ReturnType<typeof setInterval>`
- Line 50: `private metricsTimer?: any` -- change to `private metricsTimer?: ReturnType<typeof setInterval>`
- Line 58: `(process as any).browser` -- change to `(process as unknown as Record<string, unknown>).browser`

**queue-core.ts (1 violation):**
- Line 113: `priority as any` in `this.priorityStats?.recordEvent(priority as any)` -- change to `priority as string` (recordEvent likely accepts string). If recordEvent expects a specific union type, use that type. Check the EventMetrics interface for the recordEvent parameter type and match it. If it accepts `string`, use `as string`.

After all edits, run `biome lint packages/core/src/events/MessageQueueBridge.ts packages/core/src/events/PriorityEscalationManager.ts packages/core/src/events/RetryScheduler.ts packages/core/src/events/WorkerPool.ts packages/core/src/events/queue-core.ts --diagnostic-level=error` and confirm 0 noExplicitAny errors. Then run `bun run typecheck` to confirm no type regression.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && biome lint packages/core/src/events/MessageQueueBridge.ts packages/core/src/events/PriorityEscalationManager.ts packages/core/src/events/RetryScheduler.ts packages/core/src/events/WorkerPool.ts packages/core/src/events/queue-core.ts --diagnostic-level=error 2>&1 | grep -c "noExplicitAny" | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `biome lint packages/core/src/events/MessageQueueBridge.ts --diagnostic-level=error 2>&1 | grep noExplicitAny` returns empty
    - `biome lint packages/core/src/events/PriorityEscalationManager.ts --diagnostic-level=error 2>&1 | grep noExplicitAny` returns empty
    - `biome lint packages/core/src/events/RetryScheduler.ts --diagnostic-level=error 2>&1 | grep noExplicitAny` returns empty
    - `biome lint packages/core/src/events/WorkerPool.ts --diagnostic-level=error 2>&1 | grep noExplicitAny` returns empty
    - `biome lint packages/core/src/events/queue-core.ts --diagnostic-level=error 2>&1 | grep noExplicitAny` returns empty
    - `grep -n ': any' packages/core/src/events/MessageQueueBridge.ts` returns no lines (excluding biome-ignore comments)
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>All 13 noExplicitAny violations in the 5 core event files are replaced with unknown or concrete types. Biome lint clean. TypeScript typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix noExplicitAny in events/observability/ files (3 files, 24 violations)</name>
  <files>
    packages/core/src/events/observability/OTelEventMetrics.ts
    packages/core/src/events/observability/ObservableHookManager.ts
    packages/core/src/events/observability/EventMetrics.ts
  </files>
  <read_first>
    packages/core/src/events/observability/OTelEventMetrics.ts
    packages/core/src/events/observability/ObservableHookManager.ts
    packages/core/src/events/observability/EventMetrics.ts
  </read_first>
  <action>
Replace `any` with `unknown` or concrete types in the 3 observability files. Exact fixes:

**OTelEventMetrics.ts (20 violations):**

The `meter` parameter is already typed as `Meter` from `@opentelemetry/api`. The code pattern is:
```typescript
this.someCounter = (meter as any).createCounter
  ? (meter as any).createCounter('name', { ... })
  : ({ add: () => {} } as any)
```

The `Meter` interface already defines `createCounter`. The `as any` casts are unnecessary. Fix all 20 occurrences by:

1. Remove the `(meter as any)` defensive check pattern entirely. Since `meter` is typed as `Meter`, just call `meter.createCounter(...)` directly. The Meter type guarantees the method exists.

2. For each fallback stub `({ add: () => {} } as any)`, replace with a properly typed no-op: `({ add: () => {} } as unknown as Counter)`.

The 20 violations are at lines: 211, 212, 216, 219, 220, 224, 227, 228, 232, 247, 248, 252, 269, 270, 274, 277, 278, 282, 295, 296.

Each follows the same pattern. The fix transforms:
```typescript
this.cbFailuresCounter = (meter as any).createCounter
  ? (meter as any).createCounter('name', { ... })
  : ({ add: () => {} } as any)
```
Into:
```typescript
this.cbFailuresCounter = meter.createCounter('name', { ... })
```

If the defensive runtime check is intentional (e.g., meter might be a partial mock in tests), keep the check but use unknown:
```typescript
const meterRecord = meter as unknown as Record<string, unknown>
this.cbFailuresCounter = typeof meterRecord.createCounter === 'function'
  ? meter.createCounter('name', { ... })
  : ({ add: () => {} } as unknown as Counter)
```

Prefer the simpler approach (just `meter.createCounter(...)`) since `Meter` already types the method. Only use the defensive pattern if removing it causes test failures.

**ObservableHookManager.ts (3 violations):**
- Line 28: `metrics?: any` in config interface -- change to `metrics?: unknown` (external MetricsRegistry injection point with no shared type in core)
- Line 110: `let span: any` -- change to `let span: unknown`. Then downstream usage like `span.setAttributes(...)` needs to be guarded: `if (span && typeof (span as Record<string, unknown>).setAttributes === 'function') { (span as Record<string, unknown>).setAttributes(...) }`. Alternatively, define a minimal span interface inline: `let span: { end?: () => void; setAttributes?: (attrs: Record<string, unknown>) => void; recordException?: (err: unknown) => void; setStatus?: (status: Record<string, unknown>) => void } | undefined`. Read the file to see which span methods are used and create a minimal interface.
- Line 188: `let span: any` -- same fix as line 110. Use the same minimal span interface.

**EventMetrics.ts (1 violation):**
- Line 44: `registry: any` constructor parameter -- change to `registry: unknown`. Then all `registry.histogram(...)`, `registry.counter(...)` calls need the registry cast to a typed interface. Define a minimal `MetricsRegistry` interface inline (above the class or in the constructor):
```typescript
interface MetricsRegistryLike {
  histogram(config: Record<string, unknown>): { observe: (value: number, labels?: Record<string, string>) => void }
  counter(config: Record<string, unknown>): { inc: (labels?: Record<string, string>) => void }
}
```
Then cast: `const reg = registry as MetricsRegistryLike`. Read the file first to confirm which registry methods are called and their usage patterns.

After all edits, run `biome lint packages/core/src/events/observability/ --diagnostic-level=error` and confirm 0 noExplicitAny errors. Then run `bun run typecheck` to confirm no type regression.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && biome lint packages/core/src/events/observability/ --diagnostic-level=error 2>&1 | grep -c "noExplicitAny" | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `biome lint packages/core/src/events/observability/OTelEventMetrics.ts --diagnostic-level=error 2>&1 | grep noExplicitAny` returns empty
    - `biome lint packages/core/src/events/observability/ObservableHookManager.ts --diagnostic-level=error 2>&1 | grep noExplicitAny` returns empty
    - `biome lint packages/core/src/events/observability/EventMetrics.ts --diagnostic-level=error 2>&1 | grep noExplicitAny` returns empty
    - `grep -n 'as any' packages/core/src/events/observability/OTelEventMetrics.ts` returns no lines
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>All 24 noExplicitAny violations in events/observability/ are replaced. Biome lint clean on the 3 observability files. TypeScript typecheck passes.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Full events/ directory lint check
biome lint packages/core/src/events/ --diagnostic-level=error 2>&1 | grep noExplicitAny
# Expected: empty (0 matches)

# Typecheck
bun run typecheck
# Expected: exit 0

# Core package tests
cd packages/core && bun test --timeout=10000
# Expected: all pass
```
</verification>

<success_criteria>
- biome lint packages/core/src/events/ --diagnostic-level=error reports 0 noExplicitAny violations
- bun run typecheck passes with zero new errors
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-documentation-and-tooling/26-05-SUMMARY.md`
</output>
