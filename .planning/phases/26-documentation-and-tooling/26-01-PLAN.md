---
phase: 26-documentation-and-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - biome.json
  - packages/core/src/types.ts
  - packages/core/src/HookManager.ts
  - packages/core/src/GravitoServer.ts
  - packages/core/src/ConfigManager.ts
  - packages/core/src/Container/RequestScopeManager.ts
autonomous: true
requirements: [DOC-01, DOC-02]

must_haves:
  truths:
    - "biome lint packages/core/src/ reports noExplicitAny as error, not warn"
    - "biome lint packages/core/src/ reports noConsole as error for non-CLI/non-Logger files"
    - "biome lint packages/core/src/cli/ does NOT report noConsole errors"
    - "biome lint packages/core/src/Logger.ts does NOT report noConsole errors"
    - "All 16 existing noExplicitAny violations in core/src are fixed (no any remaining)"
  artifacts:
    - path: "biome.json"
      provides: "Two new overrides: core noExplicitAny+noConsole error, cli/Logger noConsole off"
      contains: "noConsole"
    - path: "packages/core/src/types.ts"
      provides: "GravitoContext with unknown instead of any"
    - path: "packages/core/src/HookManager.ts"
      provides: "messageQueueBridge typed as unknown instead of any"
    - path: "packages/core/src/GravitoServer.ts"
      provides: "ModuleResolver returns Promise<unknown>, baseOrbits typed"
  key_links:
    - from: "biome.json"
      to: "packages/core/src/**"
      via: "overrides[].includes glob"
      pattern: "packages/core/src/\\*\\*/\\*.ts"
---

<objective>
Add Biome noExplicitAny (error) and noConsole (error) overrides scoped to packages/core/src/, with CLI/Logger exclusion. Fix all 16 existing `any` violations so the new rule passes clean.

Purpose: Establish lint enforcement for the improved API surface (DOC-01 config + violation fixes, DOC-02 config only).
Output: Updated biome.json with 2 new overrides; 6 source files with `any` replaced by concrete types or `unknown`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-documentation-and-tooling/26-CONTEXT.md
@.planning/phases/26-documentation-and-tooling/26-RESEARCH.md

@biome.json
@packages/core/src/types.ts
@packages/core/src/HookManager.ts
@packages/core/src/GravitoServer.ts
@packages/core/src/ConfigManager.ts
@packages/core/src/Container/RequestScopeManager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Biome overrides for noExplicitAny error and noConsole error</name>
  <files>biome.json</files>
  <read_first>
    - biome.json (current overrides array — 6 entries starting at line 9)
  </read_first>
  <action>
Add two new override entries to the `overrides` array in biome.json. They must be appended AFTER the existing 6 overrides (the graphql override, the html override, the ion/bin/create override, the css override, the vue/svelte override, and the k6 override). Order matters — the exclusion override must come after the enabling override.

**Override 1 — Enable noExplicitAny error + noConsole error for core/src (per D-01, D-03):**
```json
{
  "includes": ["packages/core/src/**/*.ts"],
  "linter": {
    "rules": {
      "suspicious": {
        "noExplicitAny": "error",
        "noConsole": "error"
      }
    }
  }
}
```

**Override 2 — Disable noConsole for CLI and Logger (per D-04):**
```json
{
  "includes": [
    "packages/core/src/cli/**/*.ts",
    "packages/core/src/Logger.ts"
  ],
  "linter": {
    "rules": {
      "suspicious": {
        "noConsole": "off"
      }
    }
  }
}
```

Do NOT change the global `noExplicitAny: "warn"` at line 146 — it stays as-is. Only the override scopes core/src to error level.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && grep -c 'noConsole' biome.json</automated>
  </verify>
  <acceptance_criteria>
    - biome.json contains `"noConsole": "error"` inside an override with includes `["packages/core/src/**/*.ts"]`
    - biome.json contains `"noConsole": "off"` inside an override with includes containing `"packages/core/src/cli/**/*.ts"` and `"packages/core/src/Logger.ts"`
    - biome.json contains `"noExplicitAny": "error"` inside the core/src override
    - The global `"noExplicitAny": "warn"` at line 146 is unchanged
    - The cli/Logger exclusion override appears AFTER the core/src enabling override in the overrides array
    - `biome lint packages/core/src/Logger.ts` does not report noConsole errors
  </acceptance_criteria>
  <done>biome.json has 2 new overrides: core/src noExplicitAny+noConsole error, and cli/Logger noConsole off</done>
</task>

<task type="auto">
  <name>Task 2: Fix all 16 noExplicitAny violations in core/src (per D-02)</name>
  <files>
    packages/core/src/types.ts,
    packages/core/src/HookManager.ts,
    packages/core/src/GravitoServer.ts,
    packages/core/src/ConfigManager.ts,
    packages/core/src/Container/RequestScopeManager.ts
  </files>
  <read_first>
    - packages/core/src/types.ts (3 violations: GravitoContext req, res, json data param)
    - packages/core/src/HookManager.ts (4 violations: messageQueueBridge and others around lines 57, 695, 733)
    - packages/core/src/GravitoServer.ts (2 violations: ModuleResolver return type line 19, baseOrbits param line 34)
    - packages/core/src/ConfigManager.ts (1 violation: line 55 `let current: any`)
    - packages/core/src/Container/RequestScopeManager.ts (1 violation: around line 120)
    - packages/core/src/PlanetCore.ts (to check GravitoOrbit type for baseOrbits fix)
  </read_first>
  <action>
Fix all 16 `any` annotations across source files and test files. Use `unknown` for untyped data flowing from external boundaries; use concrete types where the type is known.

**packages/core/src/types.ts (3 fixes):**
- `req: any` -> `req: unknown`
- `res: any` -> `res: unknown`
- `json(data: any, ...)` -> `json(data: unknown, ...)`

**packages/core/src/HookManager.ts (4 fixes):**
- `private messageQueueBridge?: any` -> `private messageQueueBridge?: unknown`
- Line ~695: inspect the exact `any` usage and replace with `unknown` or the appropriate type
- Line ~733: inspect the exact `any` usage and replace with `unknown` or the appropriate type
- Any remaining `any` in the file: replace with `unknown`

**packages/core/src/GravitoServer.ts (2 fixes):**
- `export type ModuleResolver = () => Promise<any>` -> `export type ModuleResolver = () => Promise<unknown>`
- `baseOrbits: any[] = []` -> import GravitoOrbit type and use `baseOrbits: GravitoOrbit[] = []`. If GravitoOrbit is not importable without circular dependency, use `unknown[]`.

**packages/core/src/ConfigManager.ts (1 fix):**
- `let current: any = this.config.get(rootKey)` -> `let current: unknown = this.config.get(rootKey)`
- Verify downstream usage has type guards (it should — the function walks nested keys).

**packages/core/src/Container/RequestScopeManager.ts (1 fix):**
- Inspect line ~120 for the `any` annotation and replace with `unknown` or the concrete type.

**Test files under packages/core/src/__tests__/ (5 fixes):**
- Run `biome lint packages/core/src/__tests__/ --diagnostic-level=error 2>&1 | grep noExplicitAny` to find the 5 test file violations.
- Replace each `any` with `unknown` or the concrete type. Common pattern in tests: `as any` for mock objects — replace with proper typed mocks or `as unknown as ConcreteType`.

After all fixes, run `bun run typecheck` to verify no type errors introduced.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && biome lint packages/core/src/ --diagnostic-level=error 2>&1 | grep -c noExplicitAny || echo "0 violations"</automated>
  </verify>
  <acceptance_criteria>
    - `biome lint packages/core/src/` reports 0 noExplicitAny errors
    - packages/core/src/types.ts contains `req: unknown` and `res: unknown` and `json(data: unknown`
    - packages/core/src/HookManager.ts contains `messageQueueBridge?: unknown` (no `any`)
    - packages/core/src/GravitoServer.ts contains `Promise<unknown>` for ModuleResolver
    - packages/core/src/ConfigManager.ts contains `let current: unknown`
    - `bun run typecheck` passes with zero new errors
    - No `@ts-ignore` added anywhere
  </acceptance_criteria>
  <done>All 16 noExplicitAny violations fixed; biome lint clean for noExplicitAny; typecheck passes</done>
</task>

</tasks>

<verification>
1. `biome lint packages/core/src/ --diagnostic-level=error 2>&1 | grep noExplicitAny` returns no matches
2. `biome lint packages/core/src/Logger.ts 2>&1 | grep noConsole` returns no matches (excluded)
3. `biome lint packages/core/src/cli/ 2>&1 | grep noConsole` returns no matches (excluded)
4. `bun run typecheck` passes
5. `grep -rn 'noConsole' biome.json` shows both "error" and "off" entries
</verification>

<success_criteria>
- biome.json has 2 new overrides scoped to packages/core/src/
- All 16 existing `any` violations replaced with `unknown` or concrete types
- `biome lint packages/core/src/` clean for noExplicitAny
- `bun run typecheck` passes at workspace root
</success_criteria>

<output>
After completion, create `.planning/phases/26-documentation-and-tooling/26-01-SUMMARY.md`
</output>
