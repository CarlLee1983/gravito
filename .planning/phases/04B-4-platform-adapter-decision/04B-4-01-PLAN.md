---
phase: 04B-4-platform-adapter-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/photon/src/adapter/cloudflare.ts
  - packages/photon/src/adapter/deno.ts
  - packages/photon/src/adapter/vercel.ts
autonomous: true
requirements:
  - REQ-04-HONO-MIGRATION-PHASE-4B
  - REQ-PLATFORM-ADAPTER-DEPRECATION
must_haves:
  truths:
    - "Three adapter modules can be imported via sub-paths (cloudflare, deno, vercel)"
    - "@deprecated JSDoc explicitly states v2.0 deprecation with v3.0 removal target"
    - "Each adapter still exports Hono compatibility layer functions for backwards compatibility"
    - "All adapters pass TypeScript strict mode without suppressions"
    - "Health baseline maintained (93/100 before implementation, ≥93/100 after)"
  artifacts:
    - path: packages/photon/src/adapter/cloudflare.ts
      provides: "Cloudflare Workers/Pages adapter with deprecation notice"
      contains: "@deprecated v2.0"
    - path: packages/photon/src/adapter/deno.ts
      provides: "Deno Deploy adapter with deprecation notice"
      contains: "@deprecated v2.0"
    - path: packages/photon/src/adapter/vercel.ts
      provides: "Vercel Edge Functions adapter with deprecation notice"
      contains: "@deprecated v2.0"
  key_links:
    - from: packages/photon/package.json
      to: packages/photon/src/adapter/cloudflare.ts
      via: "exports ./adapters/cloudflare"
      pattern: "./adapter/cloudflare"
    - from: packages/photon/src/adapter/cloudflare.ts
      to: "hono/cloudflare-workers, hono/cloudflare-pages"
      via: "re-export statements"
      pattern: "export.*from.*hono"
---

<objective>
Refine @deprecated JSDoc notices in three Hono platform adapter modules (Cloudflare Workers, Deno Deploy, Vercel Edge Functions) to match the locked deprecation strategy. Each adapter will declare v2.0 deprecation with v3.0 removal target, maintaining full backwards compatibility while signaling migration timeline to end users.

Purpose: Communicate long-term roadmap to users importing these optional adapters. Per decision D-04, we keep adapters as @deprecated sub-paths (not removing yet, not replacing natively) with clear removal timeline.

Output: Three updated adapter files with refined JSDoc, ready for build and test verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04B-4-platform-adapter-decision/04B-4-CONTEXT.md
@.planning/phases/04B-hono-migration-pending/MIGRATION_ROADMAP.md

# Code context (existing patterns)
@packages/photon/src/adapter/cloudflare.ts
@packages/photon/src/adapter/deno.ts
@packages/photon/src/adapter/vercel.ts
@packages/photon/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine Cloudflare adapter @deprecated JSDoc</name>
  <files>packages/photon/src/adapter/cloudflare.ts</files>
  <action>
Update the JSDoc header in cloudflare.ts to match the locked deprecation strategy from CONTEXT.md decision D-04:

1. **Current state analysis:** File has partial @deprecated notice (v2.0 removal timeline v2.0 2026 Q3) but missing clarity on v3.0 removal target and migration rationale

2. **Update JSDoc block to include:**
   - @deprecated v2.0 — clearly states version when deprecation takes effect
   - Removal target: v3.0 — explicit statement of when feature will be removed
   - Rationale: "Hono Cloudflare Workers adapter (optional path). In v3.0+, this will be replaced with native Gravito platform adapter system."
   - Usage note: "import { ... } from '@gravito/photon/adapters/cloudflare'"
   - Backwards compatibility assurance: "v2.0 and v2.x users can continue using this adapter"

3. **Preserve existing code:**
   - Keep all export statements unchanged (handle, handleMiddleware from hono/cloudflare-pages; getConnInfo, serveStatic, upgradeWebSocket from hono/cloudflare-workers)
   - Keep example code snippets (show both Pages and Workers patterns)
   - Keep mixed-language documentation (English deprecation + Chinese examples)

4. **Follow code style from CLAUDE.md:**
   - 100 character line width (apply to comment wrapping)
   - Immutability: No mutations to existing exports
   - No @ts-ignore or type suppressions
   - JSDoc must be valid TypeScript

5. **Expected output:**
```typescript
/**
 * @deprecated v2.0 — Hono Cloudflare Workers adapter (optional path)
 *
 * Removal target: v3.0
 *
 * In v3.0+, this module will be replaced with a native Gravito platform
 * adapter system. v2.0 and v2.x users can continue using this adapter.
 *
 * Use: import { ... } from '@gravito/photon/adapters/cloudflare'
 *
 * [Keep existing examples and Chinese docs]
 */
```
  </action>
  <verify>
    <automated>
      # Verify file exists and contains @deprecated with both v2.0 and v3.0
      grep -q "@deprecated v2.0" packages/photon/src/adapter/cloudflare.ts && \
      grep -q "Removal target: v3.0" packages/photon/src/adapter/cloudflare.ts && \
      grep -q "native Gravito platform" packages/photon/src/adapter/cloudflare.ts && \
      # Verify exports are unchanged
      grep -q "hono/cloudflare-workers" packages/photon/src/adapter/cloudflare.ts && \
      grep -q "hono/cloudflare-pages" packages/photon/src/adapter/cloudflare.ts && \
      echo "✓ cloudflare.ts JSDoc refinement verified"
    </automated>
  </verify>
  <done>
    - cloudflare.ts has refined @deprecated JSDoc with v2.0 deprecation and v3.0 removal target
    - All export statements preserved (backwards compatible)
    - File passes syntax validation (valid TypeScript)
    - No type suppressions or @ts-ignore present
  </done>
</task>

<task type="auto">
  <name>Task 2: Refine Deno adapter @deprecated JSDoc</name>
  <files>packages/photon/src/adapter/deno.ts</files>
  <action>
Update the JSDoc header in deno.ts to match the locked deprecation strategy from CONTEXT.md decision D-04 (same pattern as Task 1):

1. **Current state analysis:** File has partial @deprecated notice but needs clarity on v3.0 removal target and rationale

2. **Update JSDoc block to include:**
   - @deprecated v2.0 — explicitly states version when deprecation takes effect
   - Removal target: v3.0 — explicit statement of when feature will be removed
   - Rationale: "Hono Deno adapter (optional path). In v3.0+, this will be replaced with native Gravito platform adapter system."
   - Usage note: "import { ... } from '@gravito/photon/adapters/deno'"
   - Backwards compatibility assurance: "v2.0 and v2.x users can continue using this adapter"

3. **Preserve existing code:**
   - Keep all export statements unchanged (getConnInfo, serveStatic, toSSG, upgradeWebSocket from hono/deno)
   - Keep example code snippets (static file serving, Deno.serve pattern)
   - Keep mixed-language documentation

4. **Follow code style from CLAUDE.md:**
   - 100 character line width
   - No mutations to existing exports
   - No @ts-ignore or type suppressions
   - Valid TypeScript JSDoc

5. **Expected output:**
```typescript
/**
 * @deprecated v2.0 — Hono Deno adapter (optional path)
 *
 * Removal target: v3.0
 *
 * In v3.0+, this module will be replaced with a native Gravito platform
 * adapter system. v2.0 and v2.x users can continue using this adapter.
 *
 * Use: import { ... } from '@gravito/photon/adapters/deno'
 *
 * [Keep existing examples and Chinese docs]
 */
```
  </action>
  <verify>
    <automated>
      # Verify file exists and contains @deprecated with both v2.0 and v3.0
      grep -q "@deprecated v2.0" packages/photon/src/adapter/deno.ts && \
      grep -q "Removal target: v3.0" packages/photon/src/adapter/deno.ts && \
      grep -q "native Gravito platform" packages/photon/src/adapter/deno.ts && \
      # Verify exports are unchanged
      grep -q "hono/deno" packages/photon/src/adapter/deno.ts && \
      echo "✓ deno.ts JSDoc refinement verified"
    </automated>
  </verify>
  <done>
    - deno.ts has refined @deprecated JSDoc with v2.0 deprecation and v3.0 removal target
    - All export statements preserved (backwards compatible)
    - File passes syntax validation (valid TypeScript)
    - No type suppressions or @ts-ignore present
  </done>
</task>

<task type="auto">
  <name>Task 3: Refine Vercel adapter @deprecated JSDoc</name>
  <files>packages/photon/src/adapter/vercel.ts</files>
  <action>
Update the JSDoc header in vercel.ts to match the locked deprecation strategy from CONTEXT.md decision D-04 (same pattern as Tasks 1-2):

1. **Current state analysis:** File has partial @deprecated notice but needs clarity on v3.0 removal target and rationale

2. **Update JSDoc block to include:**
   - @deprecated v2.0 — explicitly states version when deprecation takes effect
   - Removal target: v3.0 — explicit statement of when feature will be removed
   - Rationale: "Hono Vercel adapter (optional path). In v3.0+, this will be replaced with native Gravito platform adapter system."
   - Usage note: "import { ... } from '@gravito/photon/adapters/vercel'"
   - Backwards compatibility assurance: "v2.0 and v2.x users can continue using this adapter"

3. **Preserve existing code:**
   - Keep all export statements unchanged (getConnInfo, handle from hono/vercel)
   - Keep example code snippets (Edge Functions pattern)
   - Keep mixed-language documentation

4. **Follow code style from CLAUDE.md:**
   - 100 character line width
   - No mutations to existing exports
   - No @ts-ignore or type suppressions
   - Valid TypeScript JSDoc

5. **Expected output:**
```typescript
/**
 * @deprecated v2.0 — Hono Vercel adapter (optional path)
 *
 * Removal target: v3.0
 *
 * In v3.0+, this module will be replaced with a native Gravito platform
 * adapter system. v2.0 and v2.x users can continue using this adapter.
 *
 * Use: import { ... } from '@gravito/photon/adapters/vercel'
 *
 * [Keep existing examples and Chinese docs]
 */
```
  </action>
  <verify>
    <automated>
      # Verify file exists and contains @deprecated with both v2.0 and v3.0
      grep -q "@deprecated v2.0" packages/photon/src/adapter/vercel.ts && \
      grep -q "Removal target: v3.0" packages/photon/src/adapter/vercel.ts && \
      grep -q "native Gravito platform" packages/photon/src/adapter/vercel.ts && \
      # Verify exports are unchanged
      grep -q "hono/vercel" packages/photon/src/adapter/vercel.ts && \
      echo "✓ vercel.ts JSDoc refinement verified"
    </automated>
  </verify>
  <done>
    - vercel.ts has refined @deprecated JSDoc with v2.0 deprecation and v3.0 removal target
    - All export statements preserved (backwards compatible)
    - File passes syntax validation (valid TypeScript)
    - No type suppressions or @ts-ignore present
  </done>
</task>

</tasks>

<verification>
After all three adapter files are updated:

1. **Syntax check:** All three files parse as valid TypeScript
```bash
cd packages/photon && bun tsc --noEmit --skipLibCheck 2>&1 | grep -E "adapter/(cloudflare|deno|vercel)" || echo "✓ No type errors in adapter files"
```

2. **Build check:** Photon package builds successfully with new adapter files
```bash
cd packages/photon && bun run build 2>&1 | tail -5
```

3. **Export paths work:** All three sub-paths are accessible
```bash
cd packages/photon && node -e "const cloudflare = require('./dist/adapter/cloudflare.js'); console.log(Object.keys(cloudflare).length > 0)" && echo "✓ cloudflare exports exist"
```

4. **No health regression:** Overall framework health maintained at ≥93/100
</verification>

<success_criteria>
1. ✓ Three adapter files (@deprecated JSDoc refined with v2.0 deprecation + v3.0 removal target)
2. ✓ All exports preserved and working (backwards compatible)
3. ✓ TypeScript strict mode compliance (0 errors, 0 suppressions in adapter files)
4. ✓ Build successful with no warnings
5. ✓ Health baseline maintained (93/100 as of Phase 4B-2)
6. ✓ All tests passing (photon tests: 284+ pass, 0 fail)
7. ✓ No new type errors introduced across framework (83/83 packages pass)
</success_criteria>

<output>
After completion, create `.planning/phases/04B-4-platform-adapter-decision/04B-4-01-SUMMARY.md` containing:
- Execution summary (3 adapters updated, time taken)
- Changes made to each file (JSDoc refinement summary)
- Verification results (typecheck, build, exports validation)
- Health score confirmation (93/100)
- Git commits created
</output>
