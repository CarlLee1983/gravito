---
phase: 02-1-day
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [
  packages/core/tests/orbit-middleware-isolation.test.ts,
  packages/luminosity/tests/scanner/remix-scanner.test.ts,
  packages/luminosity/tests/scanner/astro-scanner.test.ts,
  packages/luminosity/tests/scanner/svelte-kit-scanner.test.ts,
  packages/luminosity/tests/storage/jsonl-logger.test.ts,
  packages/luminosity/tests/storage/compactor.test.ts,
  packages/luminosity/tests/storage/log-rotator.test.ts,
  packages/scaffold/tests/generators.test.ts,
  packages/flare/tests/csrf-helpers.test.ts,
  packages/jwt/tests/jwt-module.test.ts,
  packages/galaxy/tests/showcase-integration.test.ts,
  packages/banking/tests/cqrs-e2e.test.ts,
]
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, DEPS-03]
user_setup: []

must_haves:
  truths:
    - "Framework health score improves from 88/100 to ≥90/100"
    - "All Medium-priority test failures investigated and fixed"
    - "Low-priority items fixed or documented for future work"
    - "Test suite maintains ≥96% pass rate (environment-only failures acceptable)"
    - "TypeScript typecheck remains at 0 errors across all packages"
    - "Full test suite executable without new regressions"
  artifacts:
    - path: packages/luminosity/tests/scanner/
      provides: "Fixed SEO route scanner tests (improved test isolation)"
      min_lines: 50
    - path: packages/luminosity/tests/storage/
      provides: "Fixed logging infrastructure tests (improved file handling)"
      min_lines: 50
    - path: packages/scaffold/tests/generators.test.ts
      provides: "Fixed code generator tests (ESM/CJS path resolution)"
      min_lines: 30
    - path: .planning/phases/02-1-day/02-03-SUMMARY.md
      provides: "Phase 2C execution summary and health score final assessment"
      min_lines: 100
  key_links:
    - from: "Test infrastructure"
      to: "Framework stability"
      via: "Improved test isolation, file system mocking"
      pattern: "beforeEach/afterEach cleanup, temp directory management"
    - from: "Test failures"
      to: "Health score"
      via: "Root cause analysis and systematic fixes"
      pattern: "Categorize: environmental vs code issues"
---

<objective>
Phase 2C focuses on fixing deferred Medium/Low-priority issues to improve framework health score from 88/100 (Phase 2B baseline) to ≥90/100, completing the health verification cycle before Phase 4 (Hono migration).

**Purpose:**
- Fix Medium-priority issues that are straightforward to address
- Document Low-priority issues with clear roadmaps for future work
- Establish clean baseline for Hono migration phase
- Improve test suite quality through infrastructure improvements

**Output:**
- 02-03-SUMMARY.md with final health assessment
- Fixed test suites with improved isolation and reliability
- Clear documentation of remaining technical debt (Low-priority backlog)
- Health score ≥90/100 verified
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-1-day/02-CONTEXT.md
@.planning/phases/02-1-day/02-RESEARCH.md
@.planning/phases/02-1-day/02-02-SUMMARY.md
@.planning/phases/02-1-day/02-02-INVESTIGATIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Luminosity SEO Route Scanner Tests (35+ failures)</name>
  <files>packages/luminosity/tests/scanner/remix-scanner.test.ts, packages/luminosity/tests/scanner/astro-scanner.test.ts, packages/luminosity/tests/scanner/svelte-kit-scanner.test.ts</files>
  <action>
Based on 02-02-INVESTIGATIONS.md findings: Root cause is test isolation issues (concurrent execution, temp directory cleanup races).

**Fix approach:**
1. Identify temp directory pattern in each scanner test file (likely .tmp-remix/, .tmp-astro/, .tmp-svelte-kit/)
2. Add unique test-run identifier to temp directory paths (e.g., `.tmp-remix-${Date.now()}-${Math.random()}`)
3. Ensure beforeEach creates fresh temp directory, afterEach properly cleans up with error handling
4. Add serial execution hint comment if tests modify shared state
5. Verify file permissions for temp directory creation (use 0o755 or system default)
6. Run tests with `bun test --filter=luminosity/tests/scanner` to verify all pass

**Expected behavior:**
- Remix scanner tests: Detect framework routes correctly without file system conflicts
- Astro scanner tests: Parse route files without race conditions
- SvelteKit scanner tests: Extract routes with proper file cleanup
- All tests pass serially and can handle parallel execution

**Key insight:** Tests are well-written; problem is infrastructure timing, not logic.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && bun test --filter=luminosity/tests/scanner 2>&1 | grep -E "(PASS|FAIL|✓|✗|pass|fail)" | tail -5</automated>
  </verify>
  <done>
All Luminosity SEO scanner tests (remix, astro, svelte-kit) pass. Temp directory management improved. Test isolation verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Luminosity Logging Infrastructure Tests (21+ failures)</name>
  <files>packages/luminosity/tests/storage/jsonl-logger.test.ts, packages/luminosity/tests/storage/compactor.test.ts, packages/luminosity/tests/storage/log-rotator.test.ts</files>
  <action>
Based on 02-02-INVESTIGATIONS.md findings: Root cause is file system operations and state management (file handles not properly closed, temp files persisting).

**Fix approach:**
1. Examine JsonlLogger tests: Add proper file handle cleanup in afterEach hooks
2. Examine Compactor tests: Ensure compression temp files are deleted after test
3. Examine LogRotator tests: Verify file locking cleanup (especially macOS-specific behavior)
4. Add explicit file cleanup with error handling (use try/finally or explicit cleanup in afterEach)
5. Check for file descriptor leaks (use `lsof` if tests hang)
6. Run tests with `bun test --filter=luminosity/tests/storage` to verify all pass

**Expected behavior:**
- JsonlLogger tests: Correctly serialize/deserialize log lines without file conflicts
- Compactor tests: Compress log files and clean up temp artifacts
- LogRotator tests: Rotate files on size/time without leaving orphaned handles
- All tests clean up properly, leaving no temp files

**Key insight:** Implement proper teardown in afterEach, not just afterAll.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && bun test --filter=luminosity/tests/storage 2>&1 | grep -E "(PASS|FAIL|✓|✗|pass|fail)" | tail -5</automated>
  </verify>
  <done>
All Luminosity logging infrastructure tests (jsonl-logger, compactor, log-rotator) pass. File handle management improved. No orphaned temp files after test run.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix Scaffold Code Generator Tests (15+ failures)</name>
  <files>packages/scaffold/tests/generators.test.ts</files>
  <action>
Based on 02-02-INVESTIGATIONS.md findings: Root cause is ESM/CommonJS path resolution in test environment (issue with require vs import, __dirname vs import.meta.url).

**Fix approach:**
1. Examine scaffold/tests/generators.test.ts to identify path resolution issues
2. Check if tests use __dirname (not available in ESM context) — replace with import.meta.url based path resolution
3. Verify template file paths are correctly resolved relative to test file location, not process.cwd()
4. Ensure scaffold/src/generators.ts uses proper ESM-compatible path resolution
5. Test with both CJS and ESM contexts if applicable
6. Run tests with `bun test --filter=scaffold` to verify all pass

**Expected behavior:**
- Code generators correctly load template files
- Path resolution works in monorepo context (not assuming specific cwd)
- Tests pass whether run from project root or package directory
- Generated code matches template expectations

**Key insight:** ESM vs CJS path resolution is subtle in Node.js/Bun; use import.meta.url + path.dirname(fileURLToPath()) pattern.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && bun test --filter=scaffold 2>&1 | grep -E "(PASS|FAIL|✓|✗|pass|fail)" | tail -5</automated>
  </verify>
  <done>
All scaffold code generator tests pass. Path resolution works correctly. Templates load properly in test environment.
  </done>
</task>

<task type="auto">
  <name>Task 4: Fix CSRF Helpers Tests (2 failures) - Low Priority</name>
  <files>packages/flare/tests/csrf-helpers.test.ts</files>
  <action>
Optional/Low-priority task: Fix if straightforward edge case, defer if requires complex refactoring.

**Investigation approach:**
1. Run test to identify specific failure: `bun test --filter=flare.*csrf`
2. Analyze error message to determine if edge case or design issue
3. If edge case (malformed token, timing issue, etc.): Fix with 5-10 min patch
4. If design issue (flawed CSRF logic): Document for Phase 2C+ backlog

**Expected outcome:**
- Two edge cases identified and fixed, OR
- Root cause documented for future consideration
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && bun test --filter=flare.*csrf 2>&1 | tail -10</automated>
  </verify>
  <done>
CSRF helper tests fixed (if straightforward) or documented for backlog (if complex). Error message indicates which path taken.
  </done>
</task>

<task type="auto">
  <name>Task 5: Investigate JWT Module Tests (5 failures) - Low Priority</name>
  <files>packages/jwt/tests/jwt-module.test.ts</files>
  <action>
Optional/Low-priority task: Investigate root cause; fix if simple, defer if complex.

**Investigation approach:**
1. Run test: `bun test --filter=jwt`
2. Analyze failure pattern (all same error? different errors? timeout?)
3. Check if related to external dependency (jose library version? algorithm support?)
4. Determine if environmental (missing secrets?) or code issue (token expiration logic? signature verification?)

**Decision criteria:**
- If 3 of 5 failures are same root cause AND fix is <30 min: Apply fix
- Otherwise: Document root cause for Phase 2C backlog

**Expected outcome:**
- JWT tests fixed OR documented with clear root cause
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && bun test --filter=jwt 2>&1 | tail -15</automated>
  </verify>
  <done>
JWT module tests fixed (if quick fix available) or documented for Phase 2C backlog with root cause analysis.
  </done>
</task>

<task type="auto">
  <name>Task 6: Investigate Galaxy Showcase Integration (6 failures) - Low Priority</name>
  <files>packages/galaxy/tests/showcase-integration.test.ts</files>
  <action>
Optional/Low-priority task: Investigate if example application can be salvaged or should be documented as outdated.

**Investigation approach:**
1. Check if galaxy package is example/showcase or core framework
2. Run test: `bun test --filter=galaxy`
3. Determine if failures are outdated example code or real integration issues
4. If outdated: Mark tests as skip with deprecation notice
5. If real issues AND <1 hour to fix: Apply fixes
6. Otherwise: Document as known limitation

**Decision criteria:**
- Galaxy is lower priority than core packages (photon, atlas, signal, core)
- If failures don't affect core framework, can be safely deferred

**Expected outcome:**
- Galaxy tests either fixed OR marked as deprecated/skipped with explanation
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && bun test --filter=galaxy 2>&1 | tail -10</automated>
  </verify>
  <done>
Galaxy showcase status determined: fixed, skipped with deprecation, or documented as known limitation.
  </done>
</task>

<task type="auto">
  <name>Task 7: Investigate Banking CQRS E2E Timeouts (6 test timeouts) - Low Priority</name>
  <files>packages/banking/tests/cqrs-e2e.test.ts</files>
  <action>
Optional/Low-priority task: Investigate timeout root cause; fix if environmental, defer if architectural.

**Investigation approach:**
1. Run test with increased timeout: `bun test --filter=banking --timeout=60000` (60s timeout)
2. Check if test completes with longer timeout (environmental slowness?) or still hangs (deadlock?)
3. Examine test: Is it starting a server that never becomes ready? Event sourcing backlog processing issue?
4. Determine if:
   - Server startup slowness: Increase timeout or optimize startup
   - Event processing: Check if event bus is properly initialized
   - Database: Verify test database connection

**Decision criteria:**
- If timeout extends successfully: Just increase default timeout and move on
- If still times out: Document architectural issue for Phase 3+

**Expected outcome:**
- E2E tests pass with appropriate timeout, OR
- Root cause documented for Phase 3 architectural work
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && timeout 90 bun test --filter=banking 2>&1 | tail -15</automated>
  </verify>
  <done>
Banking E2E timeouts resolved (timeout adjusted or architectural issue documented) for Phase 2C backlog.
  </done>
</task>

<task type="auto">
  <name>Task 8: Full Test Suite Verification and Health Score Assessment</name>
  <files>.planning/phases/02-1-day/02-03-SUMMARY.md</files>
  <action>
Execute comprehensive verification and prepare final health score assessment.

**Verification steps:**
1. Run full test suite: `cd /Users/carl/Dev/Carl/gravito-core && bun test 2>&1 | tee /tmp/phase2c-test-results.txt`
2. Extract metrics:
   - Total tests, passed, failed, skipped
   - Test pass rate percentage
   - Any new errors vs Phase 2B baseline
3. Run typecheck: `bun run typecheck 2>&1 | tee /tmp/phase2c-typecheck.txt`
   - Verify 0 errors (should match Phase 2B)
4. Check for regressions:
   - Compare test failure count to Phase 2B baseline (~163 failures)
   - Ensure no type errors introduced
   - Verify dist bundles still import correctly

**Health score calculation (based on CONTEXT.md D-06):**
- **Tests (40 pts):** Calculate as (pass_rate * 40)
  - Phase 2B baseline: 96.9% pass rate = 39/40
  - Target: ≥96.5% = ≥38/40
- **TypeScript (25 pts):** 0 errors = 25/25 (maintain)
- **Dependencies (15 pts):** 0 implicit, 0 circular = 15/15 (maintain)
- **Core Modules (14 pts):** photon/signal verified = 14/15 (Orbit routing known limitation = -1)
- **E2E (5 pts):** HTTP/event bus functional = 5/5 (maintain)
- **Total:** Sum of above

**Documentation:**
1. Create 02-03-SUMMARY.md with:
   - Health score breakdown (test/type/deps/core/E2E)
   - Comparison to Phase 1 (78/100) and Phase 2B (88/100)
   - Test failure triage summary (how many fixed? How many deferred?)
   - Recommendations for Phase 2C backlog / Phase 4 readiness
   - Commits made in Phase 2C

2. Record final metrics:
   - Test pass rate
   - Number of failures fixed in 2C
   - Health score achieved
   - Remaining backlog (Medium/Low issues)

**Success criteria:**
- Health score ≥90/100 OR ≥88/100 with clear plan to reach 90/100 in Phase 2C+ backlog
- Test pass rate ≥96% (environment-only failures acceptable)
- TypeScript 0 errors maintained
- No regressions from Phase 2B
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && bun test 2>&1 | tail -30</automated>
  </verify>
  <done>
02-03-SUMMARY.md created with final health score assessment, test results, and clear roadmap for remaining backlog. Health score ≥90/100 achieved or documented with contingency.
  </done>
</task>

</tasks>

<verification>
**Phase 2C Verification Gates:**

1. **Task 1-3 (Medium Priority Fixes):** All pass
   - Luminosity SEO scanner tests: ✅ All pass
   - Luminosity logging tests: ✅ All pass
   - Scaffold generator tests: ✅ All pass

2. **Task 4-7 (Low Priority Investigation):** Documented outcomes
   - CSRF helpers: Fixed OR documented
   - JWT module: Fixed OR documented
   - Galaxy showcase: Fixed, skipped, OR documented
   - Banking E2E: Resolved OR documented

3. **Task 8 (Final Verification):**
   - Full test suite: ≥96% pass rate
   - TypeScript: 0 errors (no regressions)
   - Health score: ≥90/100
   - 02-03-SUMMARY.md: Complete with metrics and roadmap

4. **Phase 2C Completion Criteria (from D-06):**
   - ✅ All Medium-priority items fixed or clearly documented
   - ✅ Low-priority items: Fixed if straightforward, deferred if complex
   - ✅ Health score: 88/100 → ≥90/100
   - ✅ Test failures: Reduced from 162 → <20 (environment-only acceptable)
   - ✅ TypeCheck: 0 errors maintained
   - ✅ No regressions introduced
</verification>

<success_criteria>
**Phase 2C Completion Checklist:**

- [ ] Task 1: Luminosity SEO scanner tests fixed (all test files pass)
- [ ] Task 2: Luminosity logging infrastructure tests fixed (all test files pass)
- [ ] Task 3: Scaffold code generator tests fixed (all test files pass)
- [ ] Task 4: CSRF helpers investigated (fixed or backlog documented)
- [ ] Task 5: JWT module investigated (fixed or backlog documented)
- [ ] Task 6: Galaxy showcase investigated (status determined)
- [ ] Task 7: Banking CQRS E2E investigated (timeouts resolved or documented)
- [ ] Task 8: Full test suite verification complete, health score ≥90/100
- [ ] 02-03-SUMMARY.md created with metrics and health score breakdown
- [ ] Phase 2C status committed to git
- [ ] No regressions from Phase 2B
- [ ] TypeScript 0 errors maintained
- [ ] Test pass rate ≥96%

**Success Definition:**
Phase 2C is complete when:
1. All Medium-priority fixes are complete
2. All Low-priority investigations are documented
3. Health score ≥90/100 verified
4. Test suite stability confirmed
5. 02-03-SUMMARY.md documentation complete
6. Ready to proceed to Phase 4 (Hono migration)
</success_criteria>

<output>
After completion, create `.planning/phases/02-1-day/02-03-SUMMARY.md` with:

1. **Executive Summary**
   - Health score progression: 78/100 (Phase 1) → 85/100 (Phase 2A) → 88/100 (Phase 2B) → ≥90/100 (Phase 2C)
   - Test results and metrics
   - Commits made during Phase 2C

2. **Phase 2C Results**
   - Task 1-3: Fixed items summary
   - Task 4-7: Investigation outcomes and backlog items
   - Task 8: Final health score assessment

3. **Phase 2C Backlog**
   - Low-priority items deferred (with clear roadmaps)
   - 207 skipped tests (environmental)
   - 22 @ts-expect-error suppressions (type safety debt)

4. **Readiness Assessment**
   - Framework ready for Phase 4 Hono migration
   - Test infrastructure improvements completed
   - Known limitations documented
   - Contingency plans for backlog items

5. **Next Steps**
   - Phase 3 Decision: Already SKIP (from Phase 2B)
   - Phase 4: Hono migration can proceed
   - Phase 2C backlog: Future improvement work

6. **Quick Stats**
   - Total tests: 11,925+ (final count)
   - Pass rate: ≥96%
   - Health score: ≥90/100
   - Commits: [list all Phase 2C commits]
</output>
