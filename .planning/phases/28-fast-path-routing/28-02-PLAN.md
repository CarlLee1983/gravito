---
phase: 28-fast-path-routing
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - packages/core/src/adapters/bun/RadixRouter.ts
  - packages/core/src/adapters/bun/RadixNode.ts
  - packages/core/src/adapters/bun/BunNativeAdapter.ts
  - packages/core/src/adapters/bun/types.ts
  - packages/photon/src/photon.ts
autonomous: true
requirements: [PERF-02]
must_haves:
  truths:
    - "Global middleware can be skipped for specific routes via metadata"
    - "Fast-path routes correctly return 401 when unauthorized"
    - "System warns if middleware is added after serveConfig() snapshot"
  artifacts:
    - path: "packages/core/src/adapters/bun/RadixRouter.ts"
      provides: "Metadata storage for route opt-outs"
  key_links:
    - from: "BunNativeAdapter.getCompiledMiddlewareChain"
      to: "RadixNode.metadata"
      via: "Filter middleware by exclusion rules"
---

<objective>
Implement per-route middleware exclusion (opt-out) and verify security contracts for fast-paths to prevent auth bypass vulnerabilities (CVE-2025-29927).

Purpose: Balance performance and security by allowing controlled middleware bypass and enforcing explicit security checks where needed.
Output: Middleware opt-out mechanism, security integration tests, and drift detection warning.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/adapters/bun/RadixRouter.ts
@packages/core/src/adapters/bun/BunNativeAdapter.ts
@.planning/phases/28-fast-path-routing/28-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-route middleware opt-out implementation</name>
  <files>packages/core/src/adapters/bun/RadixRouter.ts, packages/core/src/adapters/bun/RadixNode.ts, packages/core/src/adapters/bun/BunNativeAdapter.ts, packages/core/src/adapters/bun/types.ts, packages/photon/src/photon.ts</files>
  <action>
    1. Update `packages/core/src/adapters/bun/types.ts`:
       - Add `RouteOptions` interface with `excludeMiddleware?: string[]`.
       - Update `RouteMatch` to include `options?: RouteOptions`.
    2. Update `packages/core/src/adapters/bun/RadixNode.ts` and `RadixRouter.ts`:
       - Add storage for `RouteOptions` per method in `RadixNode`.
       - Update `add()` and `match()` to handle metadata.
    3. Update `packages/core/src/adapters/bun/BunNativeAdapter.ts`:
       - Update `route()` to accept `RouteOptions`.
       - Update `getCompiledMiddlewareChain(path, options)` to filter middleware handlers.
       - Note: Middleware might need "tags" or "ids" to be targetable for exclusion. Implement a simple tag system or use middleware function names.
    4. Update `packages/photon/src/photon.ts`:
       - Allow passing `RouteOptions` in `get()`, `post()`, etc.
  </action>
  <verify>
    <automated>bun test packages/photon/tests/middleware-opt-out.test.ts</automated>
  </verify>
  <done>Routes can explicitly opt-out of specific global middleware.</done>
</task>

<task type="auto">
  <name>Task 2: Security Mitigation Test (CVE-2025-29927)</name>
  <files>packages/core/tests/security-fast-path.test.ts</files>
  <action>
    1. Create `packages/core/tests/security-fast-path.test.ts`:
       - Setup a `photon.fast()` route that is intended to be protected.
       - Implement the protection manually within the fast-path handler (since it bypasses global middleware).
       - Verify that calling the route without valid credentials returns 401.
       - Add a "Safeguard" test: If a developer tries to register a fast-path under a prefix that is usually protected, they must be aware that middleware is bypassed. (This is a documentation and "contract" verification task).
  </action>
  <verify>
    <automated>bun test packages/core/tests/security-fast-path.test.ts</automated>
  </verify>
  <done>Security contract for fast-path is verified and documented via tests.</done>
</task>

<task type="auto">
  <name>Task 3: serveConfig() Snapshot Semantics & Drift Detection</name>
  <files>packages/core/src/adapters/bun/BunNativeAdapter.ts, packages/core/src/exceptions/SystemException.ts</files>
  <action>
    1. Update `packages/core/src/adapters/bun/BunNativeAdapter.ts`:
       - Add a flag `isSnapshotLocked = false`.
       - Set `isSnapshotLocked = true` inside `serveConfig()`.
       - In `use()`, `useGlobal()`, and `route()`, check if `isSnapshotLocked` is true.
       - If true AND `process.env.NODE_ENV !== 'production'`, emit a warning or throw `SystemException('FAST_PATH_MIDDLEWARE_DRIFT')`.
    2. Add `FAST_PATH_MIDDLEWARE_DRIFT` to exception types if needed.
    3. Document this behavior in the code and update the framework's internal docs.
  </action>
  <verify>
    <automated>bun test packages/core/src/adapters/bun/__tests__/serveConfig-drift.test.ts</automated>
  </verify>
  <done>System detects and warns about middleware drift after serveConfig() snapshot.</done>
</task>

</tasks>

<verification>
1. Run `bun test` on the new security and middleware opt-out tests.
2. Verify that `FAST_PATH_MIDDLEWARE_DRIFT` warning appears when adding middleware after `serveConfig()`.
</verification>

<success_criteria>
- [ ] Global middleware can be skipped for specific routes via metadata.
- [ ] Protected fast-path returns 401 correctly in integration tests.
- [ ] Developer receives a clear warning if they attempt to modify the application after generating the server configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/28-fast-path-routing/28-02-SUMMARY.md`
</output>
