---
phase: 28-fast-path-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/adapters/bun/FastPathRegistry.ts
  - packages/core/src/adapters/bun/BunNativeAdapter.ts
  - packages/core/src/adapters/types.ts
  - packages/photon/src/photon.ts
autonomous: true
requirements: [PERF-01]
---

<objective>
Implement the `FastPathRegistry` and `photon.fast()` API to allow ultra-low latency routes that bypass the Gravito engine lifecycle, DI context construction, and standard middleware chains.

Purpose: Provide a mechanism for extreme performance (e.g. health checks, static data) by direct integration with Bun's native routing.
Output: Working `photon.fast()` API and `BunNativeAdapter.serveConfig()` method.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/adapters/bun/BunNativeAdapter.ts
@packages/photon/src/photon.ts

# Key interfaces for executor:
<interfaces>
From packages/core/src/adapters/types.ts:
```typescript
export interface HttpAdapter<V extends GravitoVariables = GravitoVariables> {
  // ... existing methods
  route(method: HttpMethod, path: string, ...handlers: (GravitoHandler<V> | GravitoMiddleware<V>)[]): void;
  fetch(request: Request, server?: unknown): Response | Promise<Response>;
}
```

From packages/photon/src/photon.ts:
```typescript
export class Photon {
  private adapter = new BunNativeAdapter();
  get(path: string, ...handlers: (GravitoHandler | GravitoMiddleware)[]): this;
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement FastPathRegistry and registerFastPath() in core</name>
  <files>packages/core/src/adapters/bun/FastPathRegistry.ts, packages/core/src/adapters/bun/BunNativeAdapter.ts, packages/core/src/adapters/types.ts</files>
  <action>
    1. Create `packages/core/src/adapters/bun/FastPathRegistry.ts`:
       - Implement `FastPathRegistry` class with a Map `Map<string, Map<string, (req: Request) => Response | Promise<Response>>>`.
       - Methods: `register(method, path, handler)`, `match(method, path)`, `getAll()`.
       - Use exact string matching for path (no radix/regex).
    2. Update `packages/core/src/adapters/types.ts`:
       - Add optional `registerFastPath?(method: string, path: string, handler: (req: Request) => Response | Promise<Response>): void` to `HttpAdapter`.
       - Add optional `serveConfig?(baseConfig?: Record<string, unknown>): Record<string, unknown>` to `HttpAdapter`.
    3. Update `packages/core/src/adapters/bun/BunNativeAdapter.ts`:
       - Add private `fastPathRegistry = new FastPathRegistry()`.
       - Implement `registerFastPath(method, path, handler)`.
       - In `fetch()`, check `this.fastPathRegistry.match(request.method, url.pathname)` before context acquisition. If matched, return its response immediately.
  </action>
  <verify>
    <automated>bun test packages/core/src/adapters/bun/__tests__/BunNativeAdapter.test.ts</automated>
  </verify>
  <done>Fast-path routes are registered in core and correctly bypassed during fetch.</done>
</task>

<task type="auto">
  <name>Task 2: Implement photon.fast() and shorthand methods</name>
  <files>packages/photon/src/photon.ts</files>
  <action>
    1. Update `packages/photon/src/photon.ts`:
       - Add `fast(method: string, path: string, handler: (req: Request) => Response | Promise<Response>): this`.
       - Delegate to `this.adapter.registerFastPath()`.
       - Add convenient access: `photon.fast.get(path, handler)`, etc., using a proxy or manual definitions.
       - Requirement: handler MUST receive raw `Request` and return `Response` (no GravitoContext).
  </action>
  <verify>
    <automated>bun test packages/photon/tests/photon.test.ts</automated>
  </verify>
  <done>Developers can use photon.fast() to register ultra-fast routes.</done>
</task>

<task type="auto">
  <name>Task 3: Implement BunNativeAdapter.serveConfig() and performance benchmark</name>
  <files>packages/core/src/adapters/bun/BunNativeAdapter.ts, packages/photon/src/photon.ts</files>
  <action>
    1. Update `packages/core/src/adapters/bun/BunNativeAdapter.ts`:
       - Implement `serveConfig(baseConfig = {})`.
       - Return an object with `routes` populated from `FastPathRegistry` and any static routes from `RadixRouter` (if applicable, but focus on fast-path for now).
       - Include `fetch: this.fetch.bind(this)` as fallback.
    2. Update `packages/photon/src/photon.ts`:
       - Expose `serveConfig(baseConfig?: Record<string, unknown>): Record<string, unknown>`.
    3. Add a benchmark test `packages/photon/tests/performance-fast-path.bench.ts`:
       - Compare standard `photon.get()` vs `photon.fast()`.
       - Verify `photon.fast()` bypasses DI context pool (measure memory/allocation if possible, or just latency).
  </action>
  <verify>
    <automated>bun test packages/photon/tests/performance-fast-path.bench.ts</automated>
  </verify>
  <done>serveConfig() correctly offloads fast-path routes to Bun.serve and performance gains are verified.</done>
</task>

</tasks>

<verification>
1. Check that `photon.fast('GET', '/health', () => new Response('OK'))` works.
2. Check that `app.serveConfig()` contains the registered fast route in the `routes` key.
3. Verify that `fetch` fallback still works for regular routes.
</verification>

<success_criteria>
- [ ] `photon.fast()` routes bypass context pool and DI.
- [ ] `serveConfig()` produces valid `Bun.serve` options with native `routes`.
- [ ] Fast-path routes are significantly faster (at least 20-30% lower latency) than regular routes in benchmarks.
</success_criteria>

<output>
After completion, create `.planning/phases/28-fast-path-routing/28-01-SUMMARY.md`
</output>
