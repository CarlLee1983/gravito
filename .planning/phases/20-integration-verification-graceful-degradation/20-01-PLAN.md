---
phase: 20-integration-verification-graceful-degradation
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/resilience/src/degradation/DegradedResult.ts
  - packages/resilience/src/degradation/OrbitDegradationManager.ts
  - packages/resilience/src/index.ts
  - packages/resilience/tests/degradation/OrbitDegradationManager.test.ts
autonomous: true
requirements: [INTG-05]

must_haves:
  truths:
    - "OrbitDegradationManager.execute() returns DegradedResult with degraded=false and source='live' on success"
    - "OrbitDegradationManager.execute() returns DegradedResult with degraded=true and source='fallback' when CircuitOpenException is thrown and fallback is registered"
    - "OrbitDegradationManager.execute() re-throws CircuitOpenException when no fallback is registered"
    - "OrbitDegradationManager.execute() throws CircuitOpenException directly in NODE_ENV=test (D-05)"
    - "Fallback TTL cache returns cached value within TTL window"
  artifacts:
    - path: "packages/resilience/src/degradation/DegradedResult.ts"
      provides: "DegradedResult<T> interface"
      contains: "interface DegradedResult"
    - path: "packages/resilience/src/degradation/OrbitDegradationManager.ts"
      provides: "OrbitDegradationManager class"
      exports: ["OrbitDegradationManager"]
    - path: "packages/resilience/src/index.ts"
      provides: "Barrel exports for DegradedResult and OrbitDegradationManager"
      contains: "OrbitDegradationManager"
    - path: "packages/resilience/tests/degradation/OrbitDegradationManager.test.ts"
      provides: "TDD tests for all DegradationManager behaviors"
  key_links:
    - from: "packages/resilience/src/degradation/OrbitDegradationManager.ts"
      to: "packages/resilience/src/exceptions/CircuitOpenException.ts"
      via: "import and instanceof check in catch block"
      pattern: "instanceof CircuitOpenException"
    - from: "packages/resilience/src/index.ts"
      to: "packages/resilience/src/degradation/OrbitDegradationManager.ts"
      via: "barrel re-export"
      pattern: "export.*OrbitDegradationManager"
---

<objective>
Implement OrbitDegradationManager with TDD — the typed fallback layer that catches CircuitOpenException and returns DegradedResult<T> instead of throwing.

Purpose: INTG-05 requires that when an Orbit circuit breaker is open, callers receive a typed DegradedResult rather than an uncaught exception. This enables Satellites to distinguish live from degraded data without try/catch boilerplate.
Output: DegradedResult interface, OrbitDegradationManager class, full test suite, barrel exports updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-integration-verification-graceful-degradation/20-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/resilience/src/exceptions/CircuitOpenException.ts:
```typescript
export class CircuitOpenException extends InfrastructureException {
  constructor(options: { cause?: unknown; breakerName?: string } = {})
}
```

From packages/resilience/src/index.ts:
```typescript
// Current barrel exports — DegradationManager exports must be appended
export { CircuitOpenException } from './exceptions/CircuitOpenException'
export { RetryExhaustedException } from './exceptions/RetryExhaustedException'
export { withRetry } from './retry/withRetry'
export { withResilience } from './resilience/withResilience'
export type { ResiliencePolicy, InlineCBOptions } from './resilience/ResiliencePolicy'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — DegradedResult interface + OrbitDegradationManager with full test coverage</name>
  <files>
    packages/resilience/src/degradation/DegradedResult.ts,
    packages/resilience/src/degradation/OrbitDegradationManager.ts,
    packages/resilience/tests/degradation/OrbitDegradationManager.test.ts
  </files>
  <read_first>
    packages/resilience/src/exceptions/CircuitOpenException.ts,
    packages/resilience/src/index.ts,
    packages/resilience/src/resilience/withResilience.ts,
    packages/resilience/src/resilience/ResiliencePolicy.ts
  </read_first>
  <behavior>
    - Test 1: execute() returns { value, degraded: false, source: 'live' } when fn succeeds
    - Test 2: execute() returns { value, degraded: true, source: 'fallback' } when fn throws CircuitOpenException and fallback is registered
    - Test 3: execute() re-throws CircuitOpenException when fn throws it but no fallback is registered for that orbitName
    - Test 4: execute() propagates non-CircuitOpenException errors unchanged (e.g., TypeError)
    - Test 5: registerFallback stores a fallback with TTL; execute() uses cached value within TTL window
    - Test 6: execute() re-invokes fallback fn when TTL has expired (stale cache)
    - Test 7: NODE_ENV=test gate — execute() does NOT catch CircuitOpenException; the exception propagates (per D-05)
    - Test 8: registerFallback with ttl=0 never caches; always invokes fallback fn
  </behavior>
  <action>
    RED phase: Create test file at `packages/resilience/tests/degradation/OrbitDegradationManager.test.ts`.
    Import from bun:test. Import CircuitOpenException from `../../src/exceptions/CircuitOpenException`.
    Write 8 test cases per the behavior spec above.

    For Test 7 (NODE_ENV gate): Save original `process.env.NODE_ENV`, set it to `'test'`, run execute with a fn that throws CircuitOpenException, assert the exception propagates via `expect(promise).rejects.toBeInstanceOf(CircuitOpenException)`, then restore the original env var in afterEach.

    For TTL tests: Use `Date.now()` mocking or small TTL values (e.g., 50ms) with actual time passage.

    Run tests — all 8 MUST FAIL (classes don't exist yet).

    GREEN phase:
    1. Create `packages/resilience/src/degradation/DegradedResult.ts`:
       ```typescript
       export interface DegradedResult<T> {
         value: T
         degraded: boolean
         source: 'live' | 'fallback'
       }
       ```

    2. Create `packages/resilience/src/degradation/OrbitDegradationManager.ts`:
       - Import CircuitOpenException from `../exceptions/CircuitOpenException`
       - Import type DegradedResult from `./DegradedResult`
       - Define internal `FallbackEntry<T>` with fields: `fn: () => Promise<T> | T`, `ttl: number`, `cachedValue?: T`, `cachedAt?: number`
       - Class `OrbitDegradationManager`:
         - Private field: `fallbacks = new Map<string, FallbackEntry<any>>()`
         - Method `registerFallback<T>(orbitName: string, options: { fn: () => Promise<T> | T; ttl: number }): void`
           — stores `{ ...options }` in the map
         - Method `async execute<T>(orbitName: string, fn: () => Promise<T>): Promise<DegradedResult<T>>`
           — D-05 gate: if `process.env.NODE_ENV === 'test'`, call `const value = await fn()` and return `{ value, degraded: false, source: 'live' }` (any exception propagates naturally)
           — Normal path: try `fn()`, return live result; catch: if `err instanceof CircuitOpenException`, look up fallback by orbitName; if no entry, re-throw; check TTL cache (if ttl > 0, cachedValue defined, cachedAt defined, and `Date.now() - cachedAt < ttl`, return cached); else invoke `entry.fn()`, cache if ttl > 0, return fallback result; for non-CircuitOpenException errors, re-throw unchanged.

    Run tests — all 8 MUST PASS.

    REFACTOR: Review for clarity. No mutation of input objects. Ensure `FallbackEntry` uses `readonly` where possible.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && bun test packages/resilience/tests/degradation/OrbitDegradationManager.test.ts --timeout=10000</automated>
  </verify>
  <acceptance_criteria>
    - packages/resilience/src/degradation/DegradedResult.ts contains `export interface DegradedResult<T>`
    - packages/resilience/src/degradation/DegradedResult.ts contains `degraded: boolean`
    - packages/resilience/src/degradation/DegradedResult.ts contains `source: 'live' | 'fallback'`
    - packages/resilience/src/degradation/OrbitDegradationManager.ts contains `export class OrbitDegradationManager`
    - packages/resilience/src/degradation/OrbitDegradationManager.ts contains `instanceof CircuitOpenException`
    - packages/resilience/src/degradation/OrbitDegradationManager.ts contains `process.env.NODE_ENV === 'test'`
    - packages/resilience/src/degradation/OrbitDegradationManager.ts contains `registerFallback`
    - packages/resilience/tests/degradation/OrbitDegradationManager.test.ts exits 0 with 8 passing tests
  </acceptance_criteria>
  <done>All 8 TDD tests pass. DegradedResult interface and OrbitDegradationManager class exist with correct NODE_ENV gate, TTL caching, and CircuitOpenException interception.</done>
</task>

<task type="auto">
  <name>Task 2: Update resilience barrel exports</name>
  <files>packages/resilience/src/index.ts</files>
  <read_first>
    packages/resilience/src/index.ts,
    packages/resilience/src/degradation/DegradedResult.ts,
    packages/resilience/src/degradation/OrbitDegradationManager.ts
  </read_first>
  <action>
    Add these two export lines to the bottom of `packages/resilience/src/index.ts`, in a new `// === Degradation ===` section:

    ```typescript
    // === Degradation ===
    export { OrbitDegradationManager } from './degradation/OrbitDegradationManager'
    export type { DegradedResult } from './degradation/DegradedResult'
    ```

    Do NOT modify any existing exports. Append only.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && bun test packages/resilience/tests/ --timeout=10000</automated>
  </verify>
  <acceptance_criteria>
    - packages/resilience/src/index.ts contains `export { OrbitDegradationManager } from './degradation/OrbitDegradationManager'`
    - packages/resilience/src/index.ts contains `export type { DegradedResult } from './degradation/DegradedResult'`
    - All existing resilience tests still pass (0 failures)
  </acceptance_criteria>
  <done>OrbitDegradationManager and DegradedResult are importable via `@gravito/resilience` barrel.</done>
</task>

</tasks>

<verification>
- `bun test packages/resilience/tests/ --timeout=10000` — all tests pass (existing + new)
- `bun run typecheck` — no new TypeScript errors
- `import { OrbitDegradationManager, DegradedResult } from '@gravito/resilience'` resolves correctly
</verification>

<success_criteria>
- OrbitDegradationManager.execute() returns DegradedResult on success and on circuit-open with fallback
- OrbitDegradationManager.execute() throws CircuitOpenException when no fallback registered
- NODE_ENV=test gate prevents silent degradation in test environment (D-05)
- TTL caching works: fresh fallback invoked after TTL expires
- 8 TDD tests pass, all existing resilience tests unbroken
</success_criteria>

<output>
After completion, create `.planning/phases/20-integration-verification-graceful-degradation/20-01-SUMMARY.md`
</output>
