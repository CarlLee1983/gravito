---
phase: 20-integration-verification-graceful-degradation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/resilience/tests/satellite-contracts/orbit-api-signatures.contract.test.ts
  - packages/resilience/tests/satellite-contracts/error-instanceof.contract.test.ts
autonomous: true
requirements: [INTG-05]

must_haves:
  truths:
    - "CircuitOpenException is instanceof InfrastructureException (Satellite catch blocks work)"
    - "Atlas DB.transaction and DB.transactionWithRetry method signatures still exist"
    - "Plasma BunRedisClient exported API shape is unchanged"
    - "Error types thrown by Orbits are backward-compatible with Satellite catch blocks"
  artifacts:
    - path: "packages/resilience/tests/satellite-contracts/orbit-api-signatures.contract.test.ts"
      provides: "Contract tests verifying Orbit API surfaces consumed by Satellites"
    - path: "packages/resilience/tests/satellite-contracts/error-instanceof.contract.test.ts"
      provides: "Contract tests verifying error hierarchy instanceof chains"
  key_links:
    - from: "packages/resilience/tests/satellite-contracts/error-instanceof.contract.test.ts"
      to: "packages/core/tests/contract/helpers.ts"
      via: "import assertGravitoException"
      pattern: "assertGravitoException"
---

<objective>
Create Satellite compatibility contract tests that verify Orbit API signatures and error hierarchy contracts are preserved after Phase 16-19 migration.

Purpose: Per D-10/D-11, these in-repo contract tests provide release confidence that Satellites (RBAC, Catalog, Commerce) will not break from the Orbit error model migration. They verify API shapes and instanceof chains — not actual Satellite execution.
Output: Two contract test files covering API signature preservation and error hierarchy compatibility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-integration-verification-graceful-degradation/20-CONTEXT.md

<interfaces>
<!-- Key types the executor needs -->

From packages/core/tests/contract/helpers.ts:
```typescript
export interface ContractAssertOptions {
  expectedCode: string
  expectedStatus: number
  expectedInstanceOf?: Function[]
  expectRetryable?: boolean
  expectCause?: boolean
}
export function assertGravitoException(err: unknown, opts: ContractAssertOptions): void
```

From packages/resilience/src/exceptions/CircuitOpenException.ts:
```typescript
export class CircuitOpenException extends InfrastructureException {
  constructor(options: { cause?: unknown; breakerName?: string } = {})
}
```

From packages/core/src/exceptions/InfrastructureException.ts:
```typescript
export abstract class InfrastructureException extends GravitoException {
  readonly retryable: boolean
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Satellite error instanceof contract tests</name>
  <files>packages/resilience/tests/satellite-contracts/error-instanceof.contract.test.ts</files>
  <read_first>
    packages/core/tests/contract/helpers.ts,
    packages/resilience/src/exceptions/CircuitOpenException.ts,
    packages/resilience/src/exceptions/RetryExhaustedException.ts,
    packages/core/src/exceptions/InfrastructureException.ts,
    packages/core/src/exceptions/GravitoException.ts,
    packages/atlas/src/exceptions/DatabaseException.ts,
    packages/plasma/src/exceptions/CacheException.ts
  </read_first>
  <action>
    Create directory `packages/resilience/tests/satellite-contracts/` and file `error-instanceof.contract.test.ts`.

    Import from `bun:test` (describe, it, expect).
    Import `assertGravitoException` from `../../../../core/tests/contract/helpers` (relative path per Pitfall 4 in RESEARCH.md).
    Import `CircuitOpenException` from `../../src/exceptions/CircuitOpenException`.
    Import `RetryExhaustedException` from `../../src/exceptions/RetryExhaustedException`.
    Import `InfrastructureException` from `@gravito/core`.
    Import `GravitoException` from `@gravito/core`.

    Write these contract tests:

    1. `describe('Satellite compatibility: CircuitOpenException hierarchy')`:
       - `it('is instanceof InfrastructureException')`: Create `new CircuitOpenException({ breakerName: 'atlas-db' })`, call `assertGravitoException(err, { expectedCode: 'resilience.circuit_open', expectedStatus: 503, expectedInstanceOf: [InfrastructureException], expectRetryable: false })`
       - `it('is instanceof GravitoException')`: Same error, `expect(err).toBeInstanceOf(GravitoException)`

    2. `describe('Satellite compatibility: RetryExhaustedException hierarchy')`:
       - `it('is instanceof InfrastructureException')`: Create `new RetryExhaustedException({ message: 'test', cause: new Error('boom') })`, call `assertGravitoException(err, { expectedCode: 'resilience.retry_exhausted', expectedStatus: 503, expectedInstanceOf: [InfrastructureException], expectCause: true })`

    3. `describe('Satellite compatibility: cross-boundary instanceof')`:
       - `it('catch (e) { if (e instanceof GravitoException) } works for resilience errors')`: Create CircuitOpenException, verify `e instanceof GravitoException === true` (this is the exact pattern Satellites use in their catch blocks)

    4. `describe('Satellite compatibility: error .code field is stable')`:
       - `it('CircuitOpenException.code is resilience.circuit_open')`: Verify the exact string code
       - `it('RetryExhaustedException.code is resilience.retry_exhausted')`: Verify exact string code
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && bun test packages/resilience/tests/satellite-contracts/error-instanceof.contract.test.ts --timeout=10000</automated>
  </verify>
  <acceptance_criteria>
    - packages/resilience/tests/satellite-contracts/error-instanceof.contract.test.ts exists
    - File contains `assertGravitoException` import from `../../../../core/tests/contract/helpers`
    - File contains `instanceof InfrastructureException`
    - File contains `instanceof GravitoException`
    - File contains `resilience.circuit_open`
    - File contains `resilience.retry_exhausted`
    - Test exits 0 with all tests passing
  </acceptance_criteria>
  <done>Error hierarchy contract tests pass — confirming Satellite catch blocks will still work with CircuitOpenException and RetryExhaustedException.</done>
</task>

<task type="auto">
  <name>Task 2: Satellite API signature contract tests</name>
  <files>packages/resilience/tests/satellite-contracts/orbit-api-signatures.contract.test.ts</files>
  <read_first>
    packages/atlas/src/DB.ts,
    packages/plasma/src/BunRedisClient.ts,
    packages/signal/src/index.ts,
    packages/resilience/src/index.ts
  </read_first>
  <action>
    Create `packages/resilience/tests/satellite-contracts/orbit-api-signatures.contract.test.ts`.

    Import from `bun:test` (describe, it, expect).
    Import `readFileSync` from `node:fs` and `join` from `node:path`.

    These are source-scanning contract tests. They do NOT instantiate classes — they verify that exported method signatures Satellites depend on are still present in the source files (per D-10).

    Write these tests:

    1. `describe('Satellite API compat: @gravito/atlas')`:
       - `it('exports DB.transaction static method')`: Read `packages/atlas/src/DB.ts` source, `expect(src).toContain('static async transaction(')` or `expect(src).toContain('transaction(')` (verify the exact signature from read_first)
       - `it('exports DB.transactionWithRetry static method')`: `expect(src).toContain('transactionWithRetry(')`

    2. `describe('Satellite API compat: @gravito/plasma')`:
       - Read the plasma client source file. Verify key methods: `get(`, `set(`, `del(` exist (these are the Redis operations Satellites call).

    3. `describe('Satellite API compat: @gravito/signal')`:
       - Read signal barrel (index.ts). Verify it exports `MailService` or equivalent mail transport class.

    4. `describe('Satellite API compat: @gravito/resilience')`:
       - Read resilience barrel (index.ts). Verify it exports `withResilience`, `CircuitOpenException`, `RetryExhaustedException`, `withRetry` (key APIs Satellites may consume).

    Use `join(import.meta.dir, '../../../../<package>/src/<file>')` for all file paths (relative from the test file location).

    NOTE: After reading the source files in `read_first`, adjust method name assertions to match the EXACT signatures found. Do not guess — use the actual code.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && bun test packages/resilience/tests/satellite-contracts/orbit-api-signatures.contract.test.ts --timeout=10000</automated>
  </verify>
  <acceptance_criteria>
    - packages/resilience/tests/satellite-contracts/orbit-api-signatures.contract.test.ts exists
    - File contains `readFileSync` import
    - File contains test for atlas DB transaction method
    - File contains test for plasma client methods
    - File contains test for resilience barrel exports (withResilience, CircuitOpenException)
    - Test exits 0 with all tests passing
  </acceptance_criteria>
  <done>API signature contract tests pass — confirming Orbit public APIs consumed by Satellites have not regressed.</done>
</task>

</tasks>

<verification>
- `bun test packages/resilience/tests/satellite-contracts/ --timeout=10000` — all contract tests pass
- Tests verify instanceof chains, error codes, and API surface — not implementation details
</verification>

<success_criteria>
- Error hierarchy contract tests confirm CircuitOpenException and RetryExhaustedException are instanceof GravitoException/InfrastructureException
- API signature tests confirm atlas, plasma, signal, and resilience still export the methods Satellites depend on
- All tests pass with exit code 0
</success_criteria>

<output>
After completion, create `.planning/phases/20-integration-verification-graceful-degradation/20-02-SUMMARY.md`
</output>
