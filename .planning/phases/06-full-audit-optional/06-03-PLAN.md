---
phase: 06-full-audit-optional
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-full-audit-optional/SECURITY_AUDIT.md
  - .planning/phases/06-full-audit-optional/audit/security/npm-audit.json
  - .planning/phases/06-full-audit-optional/audit/security/unsafe-patterns.txt
  - .planning/phases/06-full-audit-optional/audit/security/secrets-scan.txt
autonomous: true
requirements: []
must_haves:
  truths:
    - "Dependency vulnerabilities are identified via npm audit (0 critical expected)"
    - "Codebase is scanned for unsafe patterns (eval, Function())"
    - "Git history is scanned for hardcoded secrets"
    - "All findings are documented with remediation status"
  artifacts:
    - path: .planning/phases/06-full-audit-optional/SECURITY_AUDIT.md
      provides: "Security vulnerability and pattern analysis report"
    - path: .planning/phases/06-full-audit-optional/audit/security/npm-audit.json
      provides: "npm audit output with vulnerability details"
    - path: .planning/phases/06-full-audit-optional/audit/security/unsafe-patterns.txt
      provides: "Grep results for eval and Function calls"
    - path: .planning/phases/06-full-audit-optional/audit/security/secrets-scan.txt
      provides: "Secret scanning results (if any found)"
  key_links:
    - from: "package.json"
      to: "npm audit"
      via: "npm audit --json"
      pattern: "dependencies and devDependencies vulnerability scan"
    - from: "packages/*/src"
      to: "unsafe patterns"
      via: "grep -r eval/Function"
      pattern: "\\beval\\(|\\bFunction\\("
    - from: ".git history"
      to: "secrets scan"
      via: "TruffleHog or Gitleaks"
      pattern: "AWS keys, GitHub tokens, API keys"
---

<objective>
Security audit of gravito-core framework to identify vulnerabilities and unsafe patterns.

Purpose: Scan dependencies for known vulnerabilities, detect hardcoded secrets, and identify unsafe code patterns (eval, Function constructor) to ensure framework security baseline.

Output: SECURITY_AUDIT.md report with vulnerability counts, remediation status, and security recommendations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-full-audit-optional/06-RESEARCH.md
@.planning/PROJECT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependency vulnerability scanning with npm audit</name>
  <files>
    .planning/phases/06-full-audit-optional/audit/security/npm-audit.json
  </files>
  <read_first>
    - .planning/phases/06-full-audit-optional/06-RESEARCH.md (npm audit pattern, production vs all)
    - package.json (verify workspace structure)
    - bun.lock (verify dependencies are locked)
  </read_first>
  <action>
    1. Create audit directory: `mkdir -p .planning/phases/06-full-audit-optional/audit/security`

    2. Run npm audit on production dependencies only (strict):
       - Run: `npm audit --production --json > .planning/phases/06-full-audit-optional/audit/security/npm-audit-prod.json 2>&1`
       - This identifies vulnerabilities that could reach end users

    3. Run npm audit on all dependencies (including dev):
       - Run: `npm audit --json > .planning/phases/06-full-audit-optional/audit/security/npm-audit-all.json 2>&1`

    4. Parse npm audit output to summarize vulnerabilities:
       - Create summary using jq or bun script:
       ```bash
       bun -e "
       const fs = require('fs');
       const prodAudit = JSON.parse(fs.readFileSync('.planning/phases/06-full-audit-optional/audit/security/npm-audit-prod.json', 'utf8'));
       const allAudit = JSON.parse(fs.readFileSync('.planning/phases/06-full-audit-optional/audit/security/npm-audit-all.json', 'utf8'));

       console.log('Production Vulnerabilities:');
       console.log('- Critical:', prodAudit.metadata.vulnerabilities.critical || 0);
       console.log('- High:', prodAudit.metadata.vulnerabilities.high || 0);
       console.log('- Medium:', prodAudit.metadata.vulnerabilities.medium || 0);
       console.log('- Low:', prodAudit.metadata.vulnerabilities.low || 0);
       console.log('');
       console.log('All Vulnerabilities (including dev):');
       console.log('- Critical:', allAudit.metadata.vulnerabilities.critical || 0);
       console.log('- High:', allAudit.metadata.vulnerabilities.high || 0);
       console.log('- Medium:', allAudit.metadata.vulnerabilities.medium || 0);
       console.log('- Low:', allAudit.metadata.vulnerabilities.low || 0);
       " 2>&1 | tee .planning/phases/06-full-audit-optional/audit/security/npm-audit-summary.txt
       ```

    5. Check for critical production vulnerabilities:
       - If npm audit exits with code >0 and there are critical/high prod vulns, document them
       - Expected: 0 critical production vulnerabilities (baseline from Phase 4B)
       - If found, prioritize remediations

    6. Identify any unfixable vulnerabilities (in low-level dependencies):
       - These are noted but may be acceptable if no direct upgrade path exists
       - Examples: TypeScript compiler vulnerabilities in devDeps

    7. Create final npm-audit.json summary:
       ```json
       {
         "production": {
           "critical": 0,
           "high": 0,
           "medium": 0,
           "low": 0
         },
         "all_dependencies": {
           "critical": 0,
           "high": 0,
           "medium": 0,
           "low": 0
         },
         "remediation_status": "All production vulnerabilities fixed|Some high dev dependencies noted|Critical vulns require updates",
         "timestamp": "2026-03-26T..."
       }
       ```

    8. Verify output: `bun -e "const j=JSON.parse(require('fs').readFileSync('.planning/phases/06-full-audit-optional/audit/security/npm-audit.json', 'utf8')); console.log('Prod critical:', j.production.critical)"`
  </action>
  <verify>
    <automated>
      # Verify npm-audit.json exists and contains vulnerability counts
      test -f .planning/phases/06-full-audit-optional/audit/security/npm-audit.json && \
      bun -e "const j=JSON.parse(require('fs').readFileSync('.planning/phases/06-full-audit-optional/audit/security/npm-audit.json', 'utf8')); console.log('Production critical:', j.production.critical)" && \
      echo "npm audit complete"
    </automated>
  </verify>
  <done>
    - npm-audit.json created with vulnerability counts (critical, high, medium, low)
    - Production and all-dependencies counts separated
    - Remediation status documented
    - Expected: 0 critical production vulnerabilities
  </done>
</task>

<task type="auto">
  <name>Task 2: Unsafe pattern scanning (eval, Function constructor)</name>
  <files>
    .planning/phases/06-full-audit-optional/audit/security/unsafe-patterns.txt
  </files>
  <read_first>
    - .planning/phases/06-full-audit-optional/06-RESEARCH.md (grep pattern for eval and Function)
    - packages/*/src (quick scan to understand code patterns)
  </read_first>
  <action>
    1. Scan for eval() usage:
       - Run: `grep -rn "\\beval\\s*(" packages --include="*.ts" --include="*.js" 2>/dev/null > /tmp/eval-results.txt || true`
       - Run: `wc -l /tmp/eval-results.txt` (should be 0 or very low)

    2. Scan for Function() constructor usage:
       - Run: `grep -rn "\\bFunction\\s*(" packages --include="*.ts" --include="*.js" 2>/dev/null > /tmp/function-results.txt || true`
       - Run: `wc -l /tmp/function-results.txt` (should be 0 or very low)

    3. Scan for eval in strings (common in Dynamic imports):
       - Run: `grep -rn "eval\|Function(" packages --include="*.ts" 2>/dev/null | grep -v "test\|example\|comment" > /tmp/unsafe-all.txt || true`

    4. Manual review of any findings:
       - If eval or Function found, verify they are:
         * Not in production code (only tests or build scripts)
         * Protected with security context checks
         * Documented with security justification

    5. Create unsafe-patterns.txt report:
       ```
       Unsafe Pattern Scan - [DATE]

       eval() usage:
       - Found: N occurrences
       - Severity: NONE|LOW|MEDIUM|HIGH
       - Details:
         * [file:line] context (if found)

       Function() constructor usage:
       - Found: N occurrences
       - Severity: NONE|LOW|MEDIUM|HIGH
       - Details:
         * [file:line] context (if found)

       Other unsafe patterns:
       - Dynamic code execution: None found
       - Serialized object evaluation: None found

       Overall Assessment: PASS (no unsafe patterns)|WARN (non-production only)|FAIL (production code at risk)

       Recommendations:
       - Avoid eval and Function() in production code
       - Use safer alternatives: JSON.parse, function factories, module imports
       ```

    6. Expected result:
       - 0 eval() calls in packages/*/src
       - 0 Function() constructor calls in packages/*/src
       - Any usage in tests/examples/build is acceptable but documented

    7. Verify: `grep -q "PASS\|WARN\|FAIL" .planning/phases/06-full-audit-optional/audit/security/unsafe-patterns.txt`
  </action>
  <verify>
    <automated>
      # Verify unsafe-patterns.txt exists and has assessment
      test -f .planning/phases/06-full-audit-optional/audit/security/unsafe-patterns.txt && \
      grep -q "Unsafe Pattern Scan\|Overall Assessment" .planning/phases/06-full-audit-optional/audit/security/unsafe-patterns.txt && \
      wc -l .planning/phases/06-full-audit-optional/audit/security/unsafe-patterns.txt
    </automated>
  </verify>
  <done>
    - unsafe-patterns.txt created with eval and Function() scan results
    - Count of unsafe patterns documented (expected: 0 in production)
    - Overall assessment determined (PASS if none found, WARN if test-only, FAIL if production)
    - File contains specific file locations if any issues found
  </done>
</task>

<task type="auto">
  <name>Task 3: Secret scanning in git history</name>
  <files>
    .planning/phases/06-full-audit-optional/audit/security/secrets-scan.txt
  </files>
  <read_first>
    - .planning/phases/06-full-audit-optional/06-RESEARCH.md (TruffleHog pattern, secret scanning approach)
    - .env (check if exists and verify it's in .gitignore)
    - .gitignore (verify secrets are excluded)
  </read_first>
  <action>
    1. Verify .env is in .gitignore:
       - Run: `grep -q "^\\.env" .gitignore && echo ".env is in .gitignore" || echo "WARNING: .env may not be in .gitignore"`

    2. Check for obvious secret patterns in recent commits:
       - Look for: AWS_KEY, GITHUB_TOKEN, API_KEY patterns in committed files
       - Run: `git log --all -S "AWS\|GITHUB_TOKEN\|API_KEY" --oneline | head -10 || echo "No obvious secrets in git log"`

    3. Run TruffleHog scan on git history (if available):
       - Try: `trufflehog git file://. --json --fail 2>/dev/null | wc -l` (check if TruffleHog exists)
       - If TruffleHog not available, use git pattern search as fallback

    4. Create secrets-scan.txt report:
       ```
       Secret Scanning - [DATE]

       Secrets Scanner: TruffleHog|Gitleaks|Git Pattern Search [method used]

       Scan Result: PASS|WARN|FAIL

       Details:
       - Hardcoded AWS keys: 0
       - Hardcoded GitHub tokens: 0
       - Hardcoded API keys: 0
       - Hardcoded passwords: 0
       - Hardcoded database credentials: 0

       Recent commits checked: last 100 commits
       Verified secrets (if any): None found

       .env file status:
       - Exists: [yes|no]
       - In .gitignore: [yes|no]

       Recommendations:
       - Ensure all secrets use environment variables
       - Keep .env in .gitignore
       - Use .env.example with placeholder values
       - Rotate any exposed keys (if found)

       Overall Assessment: PASS (no secrets found in history)
       ```

    5. Expected result:
       - PASS: No secrets found
       - WARN: .env exists but .gitignore may have changed (review)
       - FAIL: If secrets are found, immediate rotation required

    6. Verify: `grep -q "Scan Result: PASS\|Scan Result: WARN\|Scan Result: FAIL" .planning/phases/06-full-audit-optional/audit/security/secrets-scan.txt`
  </action>
  <verify>
    <automated>
      # Verify secrets-scan.txt exists and has scan result
      test -f .planning/phases/06-full-audit-optional/audit/security/secrets-scan.txt && \
      grep -q "Secret Scanning\|Scan Result:" .planning/phases/06-full-audit-optional/audit/security/secrets-scan.txt && \
      wc -l .planning/phases/06-full-audit-optional/audit/security/secrets-scan.txt
    </automated>
  </verify>
  <done>
    - secrets-scan.txt created with secret scanning results
    - Scan method documented (TruffleHog, Gitleaks, or git pattern search)
    - Overall assessment determined (PASS if no secrets found)
    - .env file status and .gitignore verification documented
  </done>
</task>

<task type="auto">
  <name>Task 4: Generate SECURITY_AUDIT.md report</name>
  <files>
    .planning/phases/06-full-audit-optional/SECURITY_AUDIT.md
  </files>
  <read_first>
    - .planning/phases/06-full-audit-optional/audit/security/npm-audit.json
    - .planning/phases/06-full-audit-optional/audit/security/unsafe-patterns.txt
    - .planning/phases/06-full-audit-optional/audit/security/secrets-scan.txt
    - .planning/ROADMAP.md (baseline context)
  </read_first>
  <action>
    1. Read all three security files and extract key findings

    2. Create SECURITY_AUDIT.md with sections:
       - **Executive Summary**: Overall security health (expected: "PASS — no critical vulnerabilities")
       - **Dependency Vulnerabilities**: npm audit results (critical, high, medium, low counts)
       - **Unsafe Code Patterns**: eval and Function() scan results
       - **Secret Scanning**: Git history scan results
       - **Remediation Status**: For any identified issues
       - **End-of-Life Packages**: Optional scan for deprecated/unmaintained deps
       - **Recommendations**: Priority list for security improvements
       - **Methodology**: How audits were conducted
       - **Timestamp**: Date and time of audit

    3. Format as Markdown with:
       - Clear status indicators (✅ PASS, ⚠️ WARN, ❌ FAIL)
       - Tables for vulnerability summaries
       - Lists for findings and recommendations

    4. Vulnerability summary table:
       ```markdown
       | Severity | Production | All Dependencies | Remediation Status |
       |----------|------------|------------------|--------------------|
       | Critical | 0 | 0 | PASS ✅ |
       | High | 0 | X | [status] |
       | Medium | 0 | Y | [status] |
       | Low | 0 | Z | [status] |
       ```

    5. Include context from Phase 4B baseline:
       - Health Score: 93/100
       - Test Pass Rate: 99.7%
       - TypeScript: 0 errors
       - Status: Post-Hono migration stable

    6. Security recommendations structure:
       - Priority 1 (Critical): Production vulnerabilities (if any)
       - Priority 2 (High): High-severity devDeps (blockers for releases)
       - Priority 3 (Medium): Medium-severity devDeps (track for updates)
       - Priority 4 (Low): Low-severity items (monitor only)
       - Priority 5 (Optional): EOL package evaluations

    7. Expected findings:
       - 0 critical production vulnerabilities ✅
       - Unsafe patterns scan: PASS (no eval/Function in prod)
       - Secret scan: PASS (no secrets in git history)
       - Overall: PASS or WARN (if high devDeps exist)

    8. Verify Markdown: `grep -c "^##" .planning/phases/06-full-audit-optional/SECURITY_AUDIT.md` (should be >4 sections)
  </action>
  <verify>
    <automated>
      # Verify SECURITY_AUDIT.md exists and has required sections
      test -f .planning/phases/06-full-audit-optional/SECURITY_AUDIT.md && \
      grep -q "Dependency Vulnerabilities\|Unsafe Code Patterns\|Secret Scanning\|Recommendations" .planning/phases/06-full-audit-optional/SECURITY_AUDIT.md && \
      wc -l .planning/phases/06-full-audit-optional/SECURITY_AUDIT.md
    </automated>
  </verify>
  <done>
    - SECURITY_AUDIT.md created with comprehensive security assessment
    - Dependency vulnerability counts documented (critical, high, medium, low)
    - Unsafe pattern scan results included (expected: PASS)
    - Secret scanning results included (expected: PASS)
    - Remediation status documented for any identified issues
    - Recommendations prioritized by security impact
    - Markdown formatting is correct and file contains >150 lines
  </done>
</task>

</tasks>

<verification>
Phase 3 (Security Audit) verification:
1. All three files created in audit/security/ directory with valid format
2. SECURITY_AUDIT.md created with sections for vulnerabilities, patterns, secrets
3. Report includes specific numeric values (vulnerability counts)
4. Expected results: 0 critical prod vulns, 0 eval/Function in prod, 0 secrets in history
5. Remediation status documented for any issues found
6. Files ready for phase consolidation and reporting
</verification>

<success_criteria>
- [ ] npm-audit.json contains vulnerability counts (critical, high, medium, low)
- [ ] unsafe-patterns.txt contains eval and Function() scan results
- [ ] secrets-scan.txt contains secret scanning assessment
- [ ] SECURITY_AUDIT.md created with all sections and recommendations
- [ ] All security files have valid format and required fields
- [ ] Report includes context from Phase 4B baseline (93/100 health)
- [ ] Expected results: 0 critical prod vulns, no unsafe patterns in prod, no secrets in history
- [ ] No errors or failures in security scanning
</success_criteria>

<output>
After completion, create `.planning/phases/06-full-audit-optional/06-03-SUMMARY.md`
</output>
