---
phase: 22-exception-hierarchy-clarification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/exceptions/AuthException.ts
  - packages/core/src/exceptions/AuthenticationException.ts
  - packages/core/src/exceptions/AuthenticationException.d.ts
autonomous: true
requirements: [EXC-01]

must_haves:
  truths:
    - "AuthException JSDoc states it is the abstract base class for auth-related errors, not to be thrown directly"
    - "AuthenticationException JSDoc states it is the concrete 401 Unauthorized implementation to throw and catch"
    - "JSDoc accurately reflects that AuthenticationException extends DomainException (sibling, not child of AuthException)"
    - "A developer reading VS Code hover tooltip can distinguish which class to extend vs which to throw"
  artifacts:
    - path: "packages/core/src/exceptions/AuthException.ts"
      provides: "AuthException with role-clarifying JSDoc"
      contains: "@abstract"
    - path: "packages/core/src/exceptions/AuthenticationException.ts"
      provides: "AuthenticationException with role-clarifying JSDoc"
      contains: "401 Unauthorized"
    - path: "packages/core/src/exceptions/AuthenticationException.d.ts"
      provides: "Declaration file with synced JSDoc"
      contains: "401 Unauthorized"
  key_links:
    - from: "packages/core/src/exceptions/AuthException.ts"
      to: "packages/fortify/src/errors/FortifyError.ts"
      via: "extends AuthException"
      pattern: "extends AuthException"
    - from: "packages/core/src/exceptions/AuthException.ts"
      to: "packages/sentinel/src/errors/SentinelError.ts"
      via: "extends AuthException"
      pattern: "extends AuthException"
---

<objective>
Add clear JSDoc role-separation documentation to AuthException and AuthenticationException so developers can distinguish their purposes from VS Code hover tooltips alone.

Purpose: EXC-01 requires that the confusingly-named AuthException (abstract base) and AuthenticationException (concrete 401) are clearly documented so developers know which to extend and which to throw. No structural changes — purely documentation.
Output: Three files updated with enhanced JSDoc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-exception-hierarchy-clarification/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add role-clarifying JSDoc to AuthException, AuthenticationException, and declaration file</name>
  <files>
    packages/core/src/exceptions/AuthException.ts,
    packages/core/src/exceptions/AuthenticationException.ts,
    packages/core/src/exceptions/AuthenticationException.d.ts
  </files>
  <read_first>
    packages/core/src/exceptions/AuthException.ts,
    packages/core/src/exceptions/AuthenticationException.ts,
    packages/core/src/exceptions/AuthenticationException.d.ts,
    packages/core/src/exceptions/DomainException.ts,
    packages/fortify/src/errors/FortifyError.ts,
    packages/sentinel/src/errors/SentinelError.ts,
    packages/core/tests/contract/intermediate-exceptions.contract.test.ts,
    packages/core/tests/contract/core-exceptions.contract.test.ts
  </read_first>
  <action>
Update JSDoc on three files per D-01 (English), D-02 (role description only, no @example), D-03 (accurate inheritance — siblings not parent-child).

**File 1: `packages/core/src/exceptions/AuthException.ts`**

Replace the existing JSDoc block with:

```
/**
 * Abstract base class for authentication and authorization errors in the Gravito ecosystem.
 *
 * Do not throw this class directly — it serves as the common supertype that
 * `fortify` ({@link FortifyError}) and `sentinel` ({@link SentinelError}) extend
 * to participate in shared `instanceof` checks.
 *
 * To signal a 401 Unauthorized response, throw {@link AuthenticationException} instead
 * (which is a sibling class extending DomainException, not a subclass of AuthException).
 *
 * @abstract
 * @public
 * @see AuthenticationException
 */
```

**File 2: `packages/core/src/exceptions/AuthenticationException.ts`**

Replace the existing JSDoc block with:

```
/**
 * Concrete 401 Unauthorized exception — throw this when a request lacks valid credentials.
 *
 * This class extends {@link DomainException} directly and is **not** a subclass
 * of {@link AuthException}. To create a custom auth error for use with fortify
 * or sentinel, extend AuthException instead.
 *
 * @public
 * @see AuthException
 */
```

**File 3: `packages/core/src/exceptions/AuthenticationException.d.ts`**

Replace the existing JSDoc block with the same JSDoc as File 2 above (keeping the declaration file in sync).

**Critical constraints:**
- Do NOT modify any class signatures, imports, constructors, or runtime code
- Do NOT rename or delete either class
- Do NOT add @example blocks (per D-02)
- All JSDoc in English (per D-01)
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && bun run typecheck 2>&1 | tail -5 && cd packages/core && bun test tests/contract/intermediate-exceptions.contract.test.ts tests/contract/core-exceptions.contract.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - AuthException.ts contains `@abstract` JSDoc tag
    - AuthException.ts contains `Do not throw this class directly`
    - AuthException.ts contains `@see AuthenticationException`
    - AuthenticationException.ts contains `Concrete 401 Unauthorized exception`
    - AuthenticationException.ts contains `not a subclass of {@link AuthException}`
    - AuthenticationException.ts contains `@see AuthException`
    - AuthenticationException.d.ts contains `Concrete 401 Unauthorized exception`
    - AuthenticationException.d.ts contains `@see AuthException`
    - No class signatures, imports, or constructors are changed (runtime code untouched)
    - `bun run typecheck` passes with zero new errors
    - Contract tests for both classes pass
  </acceptance_criteria>
  <done>All three files have role-clarifying JSDoc. AuthException clearly marked as abstract base for fortify/sentinel. AuthenticationException clearly marked as concrete 401 to throw. Sibling relationship accurately documented. Contract tests pass. Typecheck passes.</done>
</task>

</tasks>

<verification>
1. `bun run typecheck` — zero new errors (JSDoc-only changes should not affect types)
2. `cd packages/core && bun test tests/contract/` — all contract tests pass (instanceof chains unaffected)
3. Grep verification:
   - `grep -n "@abstract" packages/core/src/exceptions/AuthException.ts` returns a match
   - `grep -n "Concrete 401" packages/core/src/exceptions/AuthenticationException.ts` returns a match
   - `grep -n "Concrete 401" packages/core/src/exceptions/AuthenticationException.d.ts` returns a match
</verification>

<success_criteria>
- EXC-01 satisfied: JSDoc on AuthException and AuthenticationException clearly explains role separation
- VS Code hover tooltips show distinct role descriptions for each class
- No runtime behavior changes — purely documentation
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-exception-hierarchy-clarification/22-01-SUMMARY.md`
</output>
