---
phase: 24-config-type-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/PlanetCore.ts
  - packages/core/src/Application.ts
autonomous: true
requirements: [TYPE-01, FIX-03]

must_haves:
  truths:
    - "ApplicationConfig logger and config fields are inherited from GravitoConfig via Pick, not duplicated"
    - "Passing observabilityProvider to PlanetCore.boot() makes it available in the booted instance"
    - "All downstream packages typecheck cleanly after the change"
  artifacts:
    - path: "packages/core/src/PlanetCore.ts"
      provides: "GravitoConfig type with JSDoc on logger and config fields"
      contains: "logger?: Logger"
    - path: "packages/core/src/Application.ts"
      provides: "ApplicationConfig extending Pick<GravitoConfig>"
      contains: "extends Pick<GravitoConfig"
  key_links:
    - from: "packages/core/src/Application.ts"
      to: "packages/core/src/PlanetCore.ts"
      via: "import type { GravitoConfig } from './PlanetCore'"
      pattern: "import type.*GravitoConfig.*from.*PlanetCore"
---

<objective>
Unify ApplicationConfig and GravitoConfig so that the `logger` and `config` fields are defined in exactly one place (GravitoConfig), and ApplicationConfig inherits them via `extends Pick<GravitoConfig, 'logger' | 'config'>`. Add JSDoc to the source-of-truth fields. Verify FIX-03 (observabilityProvider forwarding) remains passing.

Purpose: Eliminate duplicate field definitions that can drift out of sync between two config types used across the framework.
Output: Modified PlanetCore.ts and Application.ts with single-source-of-truth config fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/PlanetCore.ts
@packages/core/src/Application.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSDoc to GravitoConfig logger/config fields and refactor ApplicationConfig to extend Pick</name>
  <files>packages/core/src/PlanetCore.ts, packages/core/src/Application.ts</files>

  <read_first>
    packages/core/src/PlanetCore.ts (lines 80-157 for GravitoConfig type definition)
    packages/core/src/Application.ts (lines 1-111 for imports and ApplicationConfig interface)
  </read_first>

  <action>
**Step 1 — PlanetCore.ts: Add JSDoc to GravitoConfig fields (per D-03)**

In `packages/core/src/PlanetCore.ts`, locate the `GravitoConfig` type (around line 85). The `logger` and `config` fields currently have no JSDoc. Add JSDoc to make them the single documentation source:

```typescript
export type GravitoConfig = {
  /**
   * Logger instance for the application.
   * Used by both PlanetCore and Application. Defaults to ConsoleLogger if not provided.
   * @since 2.0.0
   */
  logger?: Logger

  /**
   * Initial configuration values, loaded into ConfigManager on boot.
   * Accessible via `core.config` or `app.config` after booting.
   * @since 2.0.0
   */
  config?: Record<string, unknown>

  // ... remaining fields (orbits, adapter, container, observability, observabilityProvider) unchanged
}
```

**Step 2 — Application.ts: Add GravitoConfig type import**

Add a type-only import for `GravitoConfig` from `./PlanetCore`. The file already imports `PlanetCore` as a value (line 26). Add a separate type import:

```typescript
import type { GravitoConfig } from './PlanetCore'
```

Place it adjacent to the existing `import { PlanetCore } from './PlanetCore'` line (line 26).

**Step 3 — Application.ts: Refactor ApplicationConfig to extend Pick (per D-01, D-02)**

Change the interface declaration from:
```typescript
export interface ApplicationConfig {
```
to:
```typescript
export interface ApplicationConfig extends Pick<GravitoConfig, 'logger' | 'config'> {
```

Then **remove** the `logger` and `config` field declarations from the interface body. Specifically, remove these lines (currently around lines 92-99):
```typescript
  /**
   * Logger instance
   */
  logger?: Logger

  /**
   * Initial configuration values
   */
  config?: Record<string, unknown>
```

The remaining fields (`basePath`, `configPath`, `providersPath`, `env`, `providers`, `autoDiscoverProviders`) stay unchanged.

Do NOT expand Pick to include any other GravitoConfig fields (adapter, container, observabilityProvider, etc.) per D-01.
  </action>

  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core/packages/core && bun run typecheck</automated>
  </verify>

  <acceptance_criteria>
    - PlanetCore.ts GravitoConfig `logger` field has JSDoc containing "Logger instance for the application"
    - PlanetCore.ts GravitoConfig `config` field has JSDoc containing "Initial configuration values, loaded into ConfigManager"
    - Application.ts contains `import type { GravitoConfig } from './PlanetCore'`
    - Application.ts contains `export interface ApplicationConfig extends Pick<GravitoConfig, 'logger' | 'config'>`
    - Application.ts does NOT contain a `logger?: Logger` field declaration inside the ApplicationConfig body
    - Application.ts does NOT contain a `config?: Record<string, unknown>` field declaration inside the ApplicationConfig body
    - `cd packages/core && bun run typecheck` exits 0
  </acceptance_criteria>

  <done>ApplicationConfig inherits logger and config from GravitoConfig via Pick; field definitions exist in exactly one place (GravitoConfig in PlanetCore.ts)</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and workspace typecheck to verify FIX-03 and zero regressions</name>
  <files>packages/core/src/Application.ts, packages/core/src/PlanetCore.ts</files>

  <read_first>
    packages/core/tests/ioc.test.ts (around line 67 — the existing FIX-03 observabilityProvider forwarding test)
    packages/core/tests/application.test.ts (existing Application tests to confirm they still pass)
  </read_first>

  <action>
**Step 1 — Run core package tests to verify FIX-03**

Execute `cd packages/core && bun run test` to confirm:
- The existing `ioc.test.ts` test at line 67 (observabilityProvider forwarding) passes — this satisfies FIX-03 verification
- All `application.test.ts` tests pass — confirms ApplicationConfig change is runtime-compatible
- No other test regressions

**Step 2 — Run workspace-wide typecheck**

Execute `bun run typecheck` at the workspace root (`/Users/carl/Dev/Carl/gravito-core/`). This checks all 50+ downstream packages that import from `@gravito/core`.

If typecheck fails:
- Check for packages that explicitly reference `ApplicationConfig.logger` or `ApplicationConfig.config` in a way that breaks with the Pick inheritance. This is unlikely since the types are identical, but verify.

**Step 3 — If all green, no further action needed**

FIX-03 was already completed in Phase 21. The existing test in `ioc.test.ts:67` confirms the behavior. No new test is needed (per Research pitfall 4).
  </action>

  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && bun run typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `cd packages/core && bun run test` exits 0 with 0 failures
    - `cd packages/core && bun test tests/ioc.test.ts` exits 0 (FIX-03 observabilityProvider test passes)
    - `bun run typecheck` at workspace root exits 0 with zero new errors
  </acceptance_criteria>

  <done>Full test suite passes, workspace typecheck green, FIX-03 observabilityProvider forwarding confirmed by existing test</done>
</task>

</tasks>

<verification>
1. `grep -n "extends Pick<GravitoConfig" packages/core/src/Application.ts` returns a match
2. `grep -n "logger?: Logger" packages/core/src/Application.ts` returns NO match (field removed from ApplicationConfig body)
3. `grep -n "config?: Record<string, unknown>" packages/core/src/Application.ts` returns NO match (field removed from ApplicationConfig body)
4. `grep -n "import type.*GravitoConfig.*PlanetCore" packages/core/src/Application.ts` returns a match
5. `cd packages/core && bun run test` — 0 failures
6. `bun run typecheck` — 0 errors
</verification>

<success_criteria>
- ApplicationConfig is defined as `extends Pick<GravitoConfig, 'logger' | 'config'>` — verified by grep
- logger and config field definitions exist in exactly one place (GravitoConfig in PlanetCore.ts) — verified by absence in Application.ts
- GravitoConfig logger and config fields have JSDoc — verified by grep
- Existing FIX-03 test (ioc.test.ts) passes — verified by test run
- Workspace typecheck passes with zero errors — verified by `bun run typecheck`
</success_criteria>

<output>
After completion, create `.planning/phases/24-config-type-unification/24-01-SUMMARY.md`
</output>
