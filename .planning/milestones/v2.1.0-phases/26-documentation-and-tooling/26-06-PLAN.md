---
phase: 26-documentation-and-tooling
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/runtime/archive.ts
  - packages/core/src/runtime/detection.ts
  - packages/core/src/runtime/adapter-bun.ts
  - packages/core/src/runtime/adapter-deno.ts
  - packages/core/src/runtime/adapter-node.ts
  - packages/core/src/hooks/types.ts
autonomous: true
requirements: [DOC-01]
gap_closure: true

must_haves:
  truths:
    - "biome lint packages/core/src/runtime/ --diagnostic-level=error reports 0 noExplicitAny violations"
    - "biome lint packages/core/src/hooks/types.ts --diagnostic-level=error reports 0 noExplicitAny violations"
    - "bun run typecheck passes with zero new errors after all replacements"
  artifacts:
    - path: "packages/core/src/runtime/archive.ts"
      provides: "Zero any annotations"
      contains: "globalThis as unknown"
    - path: "packages/core/src/runtime/detection.ts"
      provides: "Zero any annotations"
      contains: "globalThis as unknown"
    - path: "packages/core/src/runtime/adapter-deno.ts"
      provides: "Zero any annotations"
      contains: "globalThis as unknown"
    - path: "packages/core/src/runtime/adapter-bun.ts"
      provides: "Zero any annotations"
      contains: "unknown"
    - path: "packages/core/src/runtime/adapter-node.ts"
      provides: "Zero any annotations"
      contains: "unknown"
    - path: "packages/core/src/hooks/types.ts"
      provides: "Zero any annotations"
      contains: "unknown"
  key_links:
    - from: "biome.json noExplicitAny override"
      to: "packages/core/src/runtime/**/*.ts and packages/core/src/hooks/types.ts"
      via: "Biome override glob"
      pattern: "packages/core/src/\\*\\*/\\*.ts"
---

<objective>
Fix all 24 noExplicitAny violations in packages/core/src/runtime/ (5 files, 21 violations) and packages/core/src/hooks/types.ts (3 violations) to close the DOC-01 gap.

Purpose: These runtime adapter files and hooks type definition contain `any` for globalThis runtime detection (Deno/Bun) and external dependency type stubs. Replacing with `unknown` and typed interfaces completes the noExplicitAny enforcement for packages/core/src/.

Output: 6 modified files with zero `any` annotations remaining; biome lint clean on runtime/ and hooks/types.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-documentation-and-tooling/26-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix noExplicitAny in runtime/ adapter and detection files (5 files, 21 violations)</name>
  <files>
    packages/core/src/runtime/archive.ts
    packages/core/src/runtime/detection.ts
    packages/core/src/runtime/adapter-bun.ts
    packages/core/src/runtime/adapter-deno.ts
    packages/core/src/runtime/adapter-node.ts
  </files>
  <read_first>
    packages/core/src/runtime/archive.ts
    packages/core/src/runtime/detection.ts
    packages/core/src/runtime/adapter-bun.ts
    packages/core/src/runtime/adapter-deno.ts
    packages/core/src/runtime/adapter-node.ts
  </read_first>
  <action>
Replace `any` with `unknown` or concrete types. The common pattern in these files is `(globalThis as any).Deno` or `(globalThis as any).Bun` for runtime detection. The standard fix is `(globalThis as unknown as Record<string, unknown>).Deno` or define a helper type.

**Recommended: Define a shared type alias at the top of each file (or a shared runtime-types file):**
```typescript
type GlobalThisRecord = Record<string, unknown>
```
Then use `(globalThis as unknown as GlobalThisRecord).Deno` etc.

**detection.ts (2 violations):**
- Line 18: `(globalThis as any).Deno` -- change to `(globalThis as unknown as Record<string, unknown>).Deno`
- Line 38: `(globalThis as any).Deno` -- same fix

**archive.ts (5 violations):**
- Lines 25, 54, 62, 70: `(globalThis as any).Bun` in createBunArchiveAdapter -- change all 4 to `(globalThis as unknown as Record<string, unknown>).Bun`. Since the result is used as `B.Tar`, `B.Glob`, `B.file()`, you may need to cast `B` to an interface after the null check:
```typescript
const B = (globalThis as unknown as Record<string, unknown>).Bun as
  | { Tar?: new () => { append: (path: string, data: Uint8Array) => void; out: ReadableStream }; Glob?: new (pattern: string) => { scan: (path: string) => AsyncIterable<string> }; file?: (path: string) => { arrayBuffer: () => Promise<ArrayBuffer> } }
  | undefined
```
Or simpler: just cast to `unknown` and use the existing null check (`if (!B)` already guards).
```typescript
const B = (globalThis as unknown as Record<string, unknown>).Bun as Record<string, unknown> | undefined
```
The code already does null checks and method existence checks after getting B, so `Record<string, unknown>` is safe.

- Line 220: `(globalThis as any).Bun` in archiveFromDirectory -- same fix as above.

**adapter-deno.ts (8 violations):**
All 8 are the pattern `(globalThis as any).Deno`. Lines: 40, 101, 187, 214, 222, 233, 245, 253.

Fix: Add a helper at top of file:
```typescript
function getDenoGlobal(): Record<string, unknown> | undefined {
  return (globalThis as unknown as Record<string, unknown>).Deno as Record<string, unknown> | undefined
}
```
Or simpler, just replace each inline: `(globalThis as unknown as Record<string, unknown>).Deno`.

Since each usage does `const deno = ...; if (!deno?.Command)` etc., the `Record<string, unknown>` type works. The code uses optional chaining for all method accesses.

**adapter-bun.ts (2 violations):**
- Line 264: `config.websocket as any` in `Bun.serve({ websocket: config.websocket as any })` -- change to `config.websocket as unknown` (Bun.serve websocket param type mismatch). If TypeScript complains about unknown not being assignable to the Bun websocket type, use the double-cast pattern: `config.websocket as unknown as undefined` would lose the value. Instead: check the actual Bun.serve websocket parameter type. If it expects a specific interface and config.websocket is a different type, use: `websocket: config.websocket as unknown as Parameters<typeof Bun.serve>[0]['websocket']`. The simplest fix that satisfies both Biome and TypeScript is likely `config.websocket as unknown` since Bun.serve accepts various shapes.
- Line 274: same pattern `config.websocket as any` -- same fix.

**adapter-node.ts (4 violations):**
- Line 59: `(maybeWeb as any).getReader` -- change to `(maybeWeb as unknown as Record<string, unknown>).getReader` (checking if a stream has getReader method)
- Line 62: `stream.Readable.toWeb(streamReadable as any)` -- change to `stream.Readable.toWeb(streamReadable as unknown as import('stream').Readable)`. Since `streamReadable` might already be typed, check the actual type. The `as any` is likely to bypass a type mismatch between Node stream types. Use: `stream.Readable.toWeb(streamReadable as never)` or `as unknown as Parameters<typeof stream.Readable.toWeb>[0]`.
- Line 106: `(child as any).resourceUsage?.()` -- change to `(child as unknown as Record<string, unknown>).resourceUsage` then check typeof.
- Line 203: `(result.error as any)?.code` -- change to `(result.error as unknown as Record<string, string> | undefined)?.code` or `(result.error as unknown as { code?: string })?.code`.

After all edits, run biome lint on each file individually to confirm 0 noExplicitAny. Then run `bun run typecheck`.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && biome lint packages/core/src/runtime/ --diagnostic-level=error 2>&1 | grep -c "noExplicitAny" | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `biome lint packages/core/src/runtime/archive.ts --diagnostic-level=error 2>&1 | grep noExplicitAny` returns empty
    - `biome lint packages/core/src/runtime/detection.ts --diagnostic-level=error 2>&1 | grep noExplicitAny` returns empty
    - `biome lint packages/core/src/runtime/adapter-bun.ts --diagnostic-level=error 2>&1 | grep noExplicitAny` returns empty
    - `biome lint packages/core/src/runtime/adapter-deno.ts --diagnostic-level=error 2>&1 | grep noExplicitAny` returns empty
    - `biome lint packages/core/src/runtime/adapter-node.ts --diagnostic-level=error 2>&1 | grep noExplicitAny` returns empty
    - `grep -n 'as any' packages/core/src/runtime/detection.ts` returns no lines
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>All 21 noExplicitAny violations in runtime/ are replaced with unknown or concrete types. Biome lint clean. TypeScript typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix noExplicitAny in hooks/types.ts and run full verification (1 file, 3 violations)</name>
  <files>
    packages/core/src/hooks/types.ts
  </files>
  <read_first>
    packages/core/src/hooks/types.ts
  </read_first>
  <action>
Replace the 3 `any` annotations in hooks/types.ts with `unknown`:

**hooks/types.ts (3 violations):**
- Line 106: `db?: any` -- change to `db?: unknown` (external database connection injection, no shared type in core). This is in a config/options interface for DLQ persistence.
- Line 118: `messageQueueBridge?: any` -- change to `messageQueueBridge?: unknown` (external BullMQ bridge injection point).
- Line 124: `aggregation?: any` -- change to `aggregation?: unknown` (event aggregation config, external type).

These are all in the `EventOptions` interface (or similar config interface). The `unknown` type is architecturally correct since these are injection points for external dependencies that core does not type-check.

After editing, verify:
1. `biome lint packages/core/src/hooks/types.ts --diagnostic-level=error` reports 0 noExplicitAny
2. Run full verification across ALL three subdirectories to confirm the entire gap is closed:
   ```bash
   biome lint packages/core/src/events/ packages/core/src/runtime/ packages/core/src/hooks/ --diagnostic-level=error 2>&1 | grep noExplicitAny
   ```
   Expected: empty (0 matches)
3. `bun run typecheck` exits 0
4. `cd packages/core && bun test --timeout=10000` -- all tests pass
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && biome lint packages/core/src/hooks/types.ts --diagnostic-level=error 2>&1 | grep -c "noExplicitAny" | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `biome lint packages/core/src/hooks/types.ts --diagnostic-level=error 2>&1 | grep noExplicitAny` returns empty
    - `grep -n ': any' packages/core/src/hooks/types.ts` returns no lines
    - hooks/types.ts contains `db?: unknown` at line 106
    - hooks/types.ts contains `messageQueueBridge?: unknown` at line 118
    - hooks/types.ts contains `aggregation?: unknown` at line 124
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>All 3 noExplicitAny violations in hooks/types.ts are replaced with unknown. Combined with Plan 05 (events/) and Task 1 (runtime/), the full noExplicitAny gap is closed: 0 violations remain in packages/core/src/.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Full runtime/ + hooks/ lint check
biome lint packages/core/src/runtime/ packages/core/src/hooks/ --diagnostic-level=error 2>&1 | grep noExplicitAny
# Expected: empty (0 matches)

# Typecheck
bun run typecheck
# Expected: exit 0

# Core package tests
cd packages/core && bun test --timeout=10000
# Expected: all pass
```
</verification>

<success_criteria>
- biome lint packages/core/src/runtime/ --diagnostic-level=error reports 0 noExplicitAny violations
- biome lint packages/core/src/hooks/types.ts --diagnostic-level=error reports 0 noExplicitAny violations
- bun run typecheck passes with zero new errors
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-documentation-and-tooling/26-06-SUMMARY.md`
</output>
