---
phase: 26-documentation-and-tooling
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - packages/core/src/GravitoServer.ts
  - packages/core/src/router/RequestValidator.ts
  - packages/core/src/Container.ts
  - packages/core/src/error-handling/RequestScopeErrorContext.ts
  - packages/core/src/reliability/DeadLetterQueueManager.ts
  - packages/core/src/GlobalErrorHandlers.ts
  - packages/core/src/ffi/NativeAccelerator.ts
  - packages/core/src/hooks/dlq-operations.ts
  - packages/core/src/hooks/ActionManager.ts
  - packages/core/src/hooks/MigrationWarner.ts
  - packages/core/src/hooks/FilterManager.ts
  - packages/core/src/Container/RequestScopeManager.ts
  - packages/core/src/adapters/bun/BunNativeAdapter.ts
  - packages/core/src/adapters/bun/AdaptiveAdapter.ts
  - packages/core/src/events/queue-core.ts
  - packages/core/src/engine/Gravito.ts
  - packages/core/src/events/EventPriorityQueue.ts
  - packages/core/src/events/task-executor.ts
  - packages/core/src/events/RetryScheduler.ts
  - packages/core/src/events/DeadLetterQueue.ts
  - packages/core/src/events/MessageQueueBridge.ts
  - packages/core/src/events/aggregation/EventBatcher.ts
  - packages/core/src/HookManager.ts
autonomous: true
requirements: [DOC-02]

must_haves:
  truths:
    - "biome lint packages/core/src/ reports 0 noConsole errors (all violations either migrated to Logger or suppressed with biome-ignore + reason)"
    - "Logger.ts and cli/ files are exempt from noConsole"
    - "Every biome-ignore comment has a reason explaining why direct console is needed"
  artifacts:
    - path: "packages/core/src/hooks/ActionManager.ts"
      provides: "Console calls migrated to Logger or biome-ignore suppressed"
    - path: "packages/core/src/events/task-executor.ts"
      provides: "Console calls migrated to Logger or biome-ignore suppressed"
    - path: "packages/core/src/events/MessageQueueBridge.ts"
      provides: "Console calls migrated to Logger or biome-ignore suppressed"
  key_links:
    - from: "biome.json override (plan 01)"
      to: "packages/core/src/**"
      via: "noConsole: error enforced after console migration"
      pattern: "biome-ignore lint/suspicious/noConsole"
---

<objective>
Migrate or suppress all ~60 console.log/warn/error calls in packages/core/src/ (excluding cli/ and Logger.ts) to satisfy the noConsole Biome rule added in Plan 01.

Purpose: Enforce the noConsole lint rule clean (DOC-02 violation fixes). Files deep in the event system that lack Logger access get `biome-ignore` with reason; files with Logger access get migrated.
Output: All ~23 source files updated; `biome lint` clean for noConsole in core/src.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-documentation-and-tooling/26-CONTEXT.md
@.planning/phases/26-documentation-and-tooling/26-RESEARCH.md
@.planning/phases/26-documentation-and-tooling/26-01-SUMMARY.md

@packages/core/src/Logger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate or suppress console calls in core/src files (batch 1 — hooks, containers, adapters, server)</name>
  <files>
    packages/core/src/GravitoServer.ts,
    packages/core/src/router/RequestValidator.ts,
    packages/core/src/Container.ts,
    packages/core/src/error-handling/RequestScopeErrorContext.ts,
    packages/core/src/reliability/DeadLetterQueueManager.ts,
    packages/core/src/GlobalErrorHandlers.ts,
    packages/core/src/ffi/NativeAccelerator.ts,
    packages/core/src/hooks/dlq-operations.ts,
    packages/core/src/hooks/ActionManager.ts,
    packages/core/src/hooks/MigrationWarner.ts,
    packages/core/src/hooks/FilterManager.ts,
    packages/core/src/Container/RequestScopeManager.ts,
    packages/core/src/adapters/bun/BunNativeAdapter.ts,
    packages/core/src/adapters/bun/AdaptiveAdapter.ts
  </files>
  <read_first>
    - packages/core/src/Logger.ts (Logger interface: debug, info, warn, error methods)
    - packages/core/src/GravitoServer.ts (lines 43, 65, 68 — console.log, console.log, console.error)
    - packages/core/src/router/RequestValidator.ts (lines 74, 76 — console.warn x2)
    - packages/core/src/Container.ts (line 210 — console.warn)
    - packages/core/src/error-handling/RequestScopeErrorContext.ts (line 174 — console.error)
    - packages/core/src/reliability/DeadLetterQueueManager.ts (lines 316, 543, 574)
    - packages/core/src/GlobalErrorHandlers.ts (line 182 — console.error)
    - packages/core/src/ffi/NativeAccelerator.ts (line 72 — console.log)
    - packages/core/src/hooks/dlq-operations.ts (lines 100, 132, 153)
    - packages/core/src/hooks/ActionManager.ts (lines 179, 222, 248, 253)
    - packages/core/src/hooks/MigrationWarner.ts (lines 32-35 — console.warn x4)
    - packages/core/src/hooks/FilterManager.ts (line 67)
    - packages/core/src/Container/RequestScopeManager.ts (line 120)
    - packages/core/src/adapters/bun/BunNativeAdapter.ts (lines 295, 298)
    - packages/core/src/adapters/bun/AdaptiveAdapter.ts (lines 194, 221)
  </read_first>
  <action>
For each file, determine whether a Logger instance is available in scope. If yes, migrate `console.*` to the equivalent Logger method. If no Logger is available (common in hooks/ and error-handling/), add a `biome-ignore` comment on the line before the console call.

**Migration pattern (when Logger available):**
```typescript
// Before
console.error('[Module] Something failed:', error)
// After
this.logger.error('[Module] Something failed:', error)
```

**Suppression pattern (when Logger unavailable, per D-05):**
```typescript
// biome-ignore lint/suspicious/noConsole: No Logger instance available — process-level error handler
console.error('[Module] Something failed:', error)
```

**Per-file decisions (per D-04, D-05):**

Files that should use `biome-ignore` (no Logger in scope, or error boundary where Logger may be uninitialized):
- `error-handling/RequestScopeErrorContext.ts` — error handler boundary
- `GlobalErrorHandlers.ts` — process error handler fallback (Logger may not exist)
- `hooks/MigrationWarner.ts` — migration warning system that runs before Logger is bootstrapped

All other files: inspect the constructor/class for Logger availability. If Logger is injected or accessible via `this.app` or `this.core`, migrate. If not, use `biome-ignore`.

The biome-ignore comment format MUST be:
`// biome-ignore lint/suspicious/noConsole: <reason>`

After completing all files, run `bun run typecheck` to verify no type errors from any Logger migration.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && biome lint packages/core/src/GravitoServer.ts packages/core/src/hooks/ActionManager.ts packages/core/src/adapters/bun/AdaptiveAdapter.ts --diagnostic-level=error 2>&1 | grep -c noConsole || echo "0 violations"</automated>
  </verify>
  <acceptance_criteria>
    - Every console.* call in the listed files is either migrated to Logger or preceded by `// biome-ignore lint/suspicious/noConsole: <reason>`
    - No biome-ignore comment lacks a reason after the colon
    - `bun run typecheck` passes (no new type errors from Logger imports)
    - `grep -rn 'console\.' packages/core/src/GlobalErrorHandlers.ts` lines are all preceded by biome-ignore
    - `grep -rn 'console\.' packages/core/src/hooks/MigrationWarner.ts` lines are all preceded by biome-ignore
  </acceptance_criteria>
  <done>All console violations in batch 1 files (14 files) resolved via Logger migration or biome-ignore suppression</done>
</task>

<task type="auto">
  <name>Task 2: Migrate or suppress console calls in core/src event system files (batch 2)</name>
  <files>
    packages/core/src/events/queue-core.ts,
    packages/core/src/engine/Gravito.ts,
    packages/core/src/events/EventPriorityQueue.ts,
    packages/core/src/events/task-executor.ts,
    packages/core/src/events/RetryScheduler.ts,
    packages/core/src/events/DeadLetterQueue.ts,
    packages/core/src/events/MessageQueueBridge.ts,
    packages/core/src/events/aggregation/EventBatcher.ts,
    packages/core/src/HookManager.ts
  </files>
  <read_first>
    - packages/core/src/events/queue-core.ts (lines 276, 293-314 — console.warn x5)
    - packages/core/src/engine/Gravito.ts (lines 382, 536 — console.error x2)
    - packages/core/src/events/EventPriorityQueue.ts (line 265 — console.error)
    - packages/core/src/events/task-executor.ts (11 console calls across lines 165-318)
    - packages/core/src/events/RetryScheduler.ts (lines 95, 183)
    - packages/core/src/events/DeadLetterQueue.ts (lines 179, 324)
    - packages/core/src/events/MessageQueueBridge.ts (7 console calls across lines 155-251)
    - packages/core/src/events/aggregation/EventBatcher.ts (lines 213, 236)
    - packages/core/src/HookManager.ts (line 785 — console.error)
  </read_first>
  <action>
The event system files (`events/`, `engine/Gravito.ts`, `HookManager.ts`) are deeply embedded infrastructure that typically do NOT hold a Logger reference. For each file:

1. Check the constructor for a `logger` parameter or `this.logger` / `this.app` access.
2. If Logger is available: migrate `console.*` to Logger equivalent.
3. If Logger is NOT available: add `biome-ignore lint/suspicious/noConsole:` with a specific reason.

**Expected: most event system files will need biome-ignore** because they are low-level infrastructure created before the Logger/PlanetCore is available.

**Specific attention to task-executor.ts** which has 11 console calls — verify each one individually. Use a consistent reason comment like:
```
// biome-ignore lint/suspicious/noConsole: Event infrastructure — no Logger dependency injected
```

For `engine/Gravito.ts` (the HTTP engine): check if it has access to the PlanetCore logger. If yes, migrate. If not, biome-ignore.

After all fixes, run the full biome lint check to verify zero noConsole errors remain:
```bash
biome lint packages/core/src/ --diagnostic-level=error 2>&1 | grep noConsole
```

Then run `bun run typecheck` to verify.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/Carl/gravito-core && biome lint packages/core/src/ --diagnostic-level=error 2>&1 | grep -c noConsole || echo "0 violations"</automated>
  </verify>
  <acceptance_criteria>
    - `biome lint packages/core/src/ --diagnostic-level=error 2>&1 | grep noConsole` returns 0 matches
    - Every biome-ignore comment includes a reason after the colon
    - `bun run typecheck` passes at workspace root
    - `biome lint packages/core/src/Logger.ts` does NOT report noConsole (excluded by override)
    - `biome lint packages/core/src/cli/` does NOT report noConsole (excluded by override)
  </acceptance_criteria>
  <done>All noConsole violations in core/src resolved; biome lint clean for noConsole; typecheck passes</done>
</task>

</tasks>

<verification>
1. `biome lint packages/core/src/ --diagnostic-level=error 2>&1 | grep noConsole` returns no matches
2. `biome lint packages/core/src/ --diagnostic-level=error 2>&1 | grep noExplicitAny` returns no matches (from Plan 01)
3. `bun run typecheck` passes
4. `grep -rn 'biome-ignore.*noConsole' packages/core/src/ | grep -v ':'` returns no lines (all have reasons)
</verification>

<success_criteria>
- Zero noConsole errors from biome lint on packages/core/src/
- All ~60 console calls either migrated to Logger or suppressed with biome-ignore + reason
- typecheck passes at workspace root
- Logger.ts and cli/ remain exempt (no changes needed there)
</success_criteria>

<output>
After completion, create `.planning/phases/26-documentation-and-tooling/26-02-SUMMARY.md`
</output>
