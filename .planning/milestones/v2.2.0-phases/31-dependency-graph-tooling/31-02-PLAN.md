---
phase: 31-dependency-graph-tooling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/photon/package.json
autonomous: true
requirements: [TOOL-01]
must_haves:
  truths:
    - "Running `bun run publint packages/photon` exits with 0 errors"
    - "Running `bun run publint packages/cli` exits with 0 errors"
    - "Running `bun run publint packages/core` exits with 0 errors"
    - "No ERR_PACKAGE_PATH_NOT_EXPORTED regressions exist in the three packages"
  artifacts:
    - path: "packages/photon/package.json"
      provides: "Corrected exports map with types as first condition in every entry"
      contains: '"types"'
  key_links:
    - from: "packages/photon/package.json"
      to: "TypeScript type resolution"
      via: "exports condition ordering"
      pattern: '"types".*"bun".*"default"'
---

<objective>
Fix `@gravito/photon` exports map so `types` is the first condition in every export entry, resolving all ~70 publint errors. Validate with publint on photon, cli, and core packages per D-07/D-08.

Purpose: Phase 31 milestone success criteria requires publint clean exit on all modified packages. Photon was modified in Phase 28 and currently has ~70 `types`-ordering errors.
Output: Clean `publint` run on photon/cli/core with zero errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-dependency-graph-tooling/31-CONTEXT.md
@.planning/phases/31-dependency-graph-tooling/31-RESEARCH.md

@packages/photon/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix photon exports map types ordering and validate publint</name>
  <files>packages/photon/package.json</files>
  <read_first>
    - packages/photon/package.json (current exports map with ~21 export entries where "bun" comes before "types")
  </read_first>
  <action>
    Edit `packages/photon/package.json` to reorder every export condition object so `"types"` is the FIRST key, followed by `"bun"`, then `"default"`.

    Current broken pattern (all 21 entries):
    ```json
    ".": {
      "bun": "./dist/index.js",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
    ```

    Correct pattern (apply to ALL 21 entries):
    ```json
    ".": {
      "types": "./dist/index.d.ts",
      "bun": "./dist/index.js",
      "default": "./dist/index.js"
    }
    ```

    The 21 export entries to fix are:
    - `"."`
    - `"./native"`
    - `"./adapter"`
    - `"./client"`
    - `"./logger"`
    - `"./jwt"`
    - `"./middleware/binary"`
    - `"./middleware/htmx"`
    - `"./middleware/ratelimit"`
    - `"./middleware/ratelimit-redis"`
    - `"./middleware/otel"`
    - `"./middleware/streaming"`
    - `"./middleware/security"`
    - `"./middleware/body"`
    - `"./middleware/cors"`
    - `"./middleware/rate-limit"`
    - `"./http-exception"`
    - `"./router/reg-exp-router"`
    - `"./router/trie-router"`
    - `"./adapter/cloudflare"`
    - `"./adapter/vercel"`
    - `"./adapter/deno"`

    After editing, run publint validation:
    ```bash
    bun run publint packages/photon
    bun run publint packages/cli
    bun run publint packages/core
    ```

    All three must exit cleanly with 0 errors. If any errors remain, fix them before marking complete.
  </action>
  <verify>
    <automated>bun run publint packages/photon && bun run publint packages/cli && bun run publint packages/core</automated>
  </verify>
  <acceptance_criteria>
    - packages/photon/package.json exports["."] has "types" as the first key (before "bun" and "default")
    - packages/photon/package.json exports["./native"] has "types" as the first key
    - packages/photon/package.json exports["./adapter"] has "types" as the first key
    - packages/photon/package.json exports["./jwt"] has "types" as the first key
    - packages/photon/package.json exports["./middleware/security"] has "types" as the first key
    - packages/photon/package.json exports["./adapter/cloudflare"] has "types" as the first key
    - `bun run publint packages/photon` exits with code 0 and reports no errors
    - `bun run publint packages/cli` exits with code 0
    - `bun run publint packages/core` exits with code 0
  </acceptance_criteria>
  <done>All 21 photon export entries have `types` as first condition key. publint passes with 0 errors on photon, cli, and core packages. No ERR_PACKAGE_PATH_NOT_EXPORTED regressions per D-08.</done>
</task>

</tasks>

<verification>
- `bun run publint packages/photon` — 0 errors
- `bun run publint packages/cli` — 0 errors
- `bun run publint packages/core` — 0 errors
</verification>

<success_criteria>
- All 21 photon export entries have `types` as first key in condition object
- publint clean on photon, cli, and core
- No ERR_PACKAGE_PATH_NOT_EXPORTED regressions
</success_criteria>

<output>
After completion, create `.planning/phases/31-dependency-graph-tooling/31-02-SUMMARY.md`
</output>
