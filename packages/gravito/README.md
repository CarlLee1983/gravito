# Gravito

The official CLI proxy for the [Gravito Framework](https://github.com/gravito-framework/gravito).

## Usage

```bash
# Initialize a new project
npx gr    "directory": "packages/gravito"
  }
}
