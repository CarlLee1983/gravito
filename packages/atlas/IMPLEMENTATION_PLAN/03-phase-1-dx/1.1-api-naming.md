# 1.1 統一 API 命名

**Problem Location:** `src/orm/model/Model.ts:78-79`  
**Estimated Time:** 2-3 小時

## 問題描述

**當前代碼：**
```typescript
static table: string
static tableName: string
```

**分析：**
- 兩個屬性並存，造成混淆
- `getTable()` 方法檢查兩者：`self.tableName || self.table`
- 代碼庫中用法不一致

## 實施步驟

### 1. 搜尋所有 `tableName` 使用

```bash
# 找出所有出現位置
grep -rn "tableName" packages/atlas/src
grep -rn "tableName" packages/atlas/tests
```

### 2. 更新 Model.ts

```typescript
// 只保留 'table'
static table: string

// 添加已棄用的 getter 以保持向後相容
/**
 * @deprecated Use Model.table instead
 */
static get tableName(): string {
  if (process.env.NODE_ENV !== 'production') {
    console.warn(
      `[Deprecation] ${this.name}.tableName is deprecated. Use ${this.name}.table instead.`
    )
  }
  return this.table
}
```

### 3. 更新 getTable() 方法 (line 524)

```typescript
static getTable(): string {
  const self = this as any
  // Priority: table > tableName (for backward compatibility)
  const table = self.table || self.tableName
  if (!table) {
    throw new Error(`Model ${this.name} has no table defined.`)
  }
  return table
}
```

### 4. 全域替換

```bash
# 在所有模型定義中替換
find packages/atlas -name "*.ts" -exec sed -i '' 's/static tableName =/static table =/g' {} \;
```

### 5. 更新測試

- 搜尋測試檔案：`grep -rn "tableName" packages/atlas/tests`
- 替換為 `table`
- 添加棄用警告測試

## 需要修改的檔案

- `src/orm/model/Model.ts` (添加已棄用的 getter)
- `tests/` 目錄中的所有模型檔案
- 更新文檔

## 成功標準

- ✅ 所有測試通過
- ✅ `grep -r "tableName" packages/atlas/src` 只顯示已棄用的 getter
- ✅ 在開發模式下使用 `tableName` 時顯示棄用警告
