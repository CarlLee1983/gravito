{
  "name": "@gravito/admin-sdk",
  "sideEffects": false,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm",
    "build:dts": "tsup src/index.ts --format esm --dts",
    "test": "true",
    "test:ci": "echo \"No unit tests for admin-sdk\" && exit 0",
    "typecheck": "bun tsc -p tsconfig.json --noEmit --skipLibCheck"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.9.3"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gravito-framework/gravito.git",
    "directory": "packages/admin-sdk"
  }
}
