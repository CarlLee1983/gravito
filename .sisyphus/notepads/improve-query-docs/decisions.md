# Decisions - Improve Query JSDoc

## JSDoc Strategy
- Every exported class, interface, and public/protected method will receive a JSDoc block.
- Use `@param`, `@returns`, `@example`, `@template`, and `@internal` as specified in the task.
- Focus on explaining the "why" and "how" of the fluent interface.
